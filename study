技术栈是你掌握的所有技术的组合。
一名嵌入式 MCU 软件工程师的技术栈：
┌─────────────────────────────────────────┐
│ 应用层知识 │
│ 产品业务逻辑、通信协议、用户交互 │
├─────────────────────────────────────────┤
│ 编程语言 │
│ C（主力）、少量 C++、汇编、Python（工具） │
├─────────────────────────────────────────┤
│ 操作系统 │
│ FreeRTOS / RT-Thread / 裸机 │
├─────────────────────────────────────────┤
│ 通信协议 │
│ WiFi、BLE、I2C、SPI、UART、MQTT、TCP/IP │
├─────────────────────────────────────────┤
│ 硬件平台 │
│ ESP32、ATBM6461、STM32、AIC8800 等 │ ← MCU 在这里
├─────────────────────────────────────────┤
│ 开发工具 │
│ GCC、Keil、VSCode、GDB、J-Link、示波器 │
├─────────────────────────────────────────┤
│ 工程管理 │
│ Git、Makefile/CMake、CI/CD │
└─────────────────────────────────────────┘

为什么MCU要上rtos呢？相对于裸机开发有什么优缺点
先看裸机的痛点：
核心问题：一个任务卡住，其他全部等着。
上了 RTOS 之后：
优缺点对比
RTOS 的优点
① 多任务并发，互不阻塞
② 代码结构清晰
③ 任务间通信机制现成
④ 延时不浪费 CPU
⑤ 优先级抢占
RTOS 的缺点
① 占用额外资源
② 调试更复杂
③ 引入并发问题
④ 栈溢出风险
⑤ 实时性有上限
裸机中断响应：几个时钟周期（最快）
RTOS 任务响应：中断 → 调度器 → 切换上下文 → 任务执行
多了几微秒的调度开销

RAM 完整布局：
┌─────────────────────────┐ RAM 顶部（高地址）
│ │
│ MSP 栈（主栈） │ ← 中断/异常用
│ ↓ 往下生长 │ 大小固定，链接文件里定义
│ │
├─────────────────────────┤
│ │
│ FreeRTOS 堆 │ ← configTOTAL_HEAP_SIZE
│ ┌───────────────────┐ │
│ │ TCB_A (≈80B) │ │
│ │ PSP_A 栈 (512B) │ │ ← 任务A 的 PSP 指向这里
│ │ ↓ 往下生长 │ │
│ ├───────────────────┤ │
│ │ TCB_B (≈80B) │ │
│ │ PSP_B 栈 (512B) │ │ ← 任务B 的 PSP 指向这里
│ │ ↓ 往下生长 │ │
│ ├───────────────────┤ │
│ │ TCB_C (≈80B) │ │
│ │ PSP_C 栈 (512B) │ │ ← 任务C 的 PSP 指向这里
│ │ ↓ 往下生长 │ │
│ ├───────────────────┤ │
│ │ Queue_1 │ │
│ │ Semaphore_1 │ │
│ ├───────────────────┤ │
│ │ 剩余空闲堆 │ │
│ └───────────────────┘ │
│ │
├─────────────────────────┤
│ .bss（未初始化全局变量）│
├─────────────────────────┤
│ .data（初始化全局变量） │
└─────────────────────────┘ RAM 底部（低地址）
MSP 和 PSP 的区别：
MSP（主栈指针）：
├── 位置：RAM 顶部，链接文件里固定分配
├── 谁用：所有中断和异常处理函数
├── 数量：整个系统只有 1 个
├── 大小：链接文件里定义（如 1KB）
└── 切换：进中断时硬件自动切到 MSP
PSP（进程栈指针）：
├── 位置：FreeRTOS 堆里面，xTaskCreate 时动态分配
├── 谁用：各个任务
├── 数量：每个任务一个独立的栈
├── 大小：xTaskCreate 的参数指定
└── 切换：PendSV 上下文切换时更新 PSP

运行时 PSP 怎么切换
当前运行任务A：
PSP 寄存器 → 指向 PSP_A 栈
调度器决定切到任务B：
PendSV 中断触发
├── 保存 R4-R11 到 PSP_A 栈
├── 把当前 PSP 值存到 TCB_A
├── 从 TCB_B 取出 PSP_B 的值
├── 恢复 R4-R11 从 PSP_B 栈
└── PSP 寄存器 → 现在指向 PSP_B 栈
任务B 继续运行

硬件寄存器：
MSP 寄存器 → 始终指向 RAM 顶部的中断栈（不变）
PSP 寄存器 → 随任务切换而变化
任务A运行时 → 指向堆里的 PSP_A 栈
任务B运行时 → 指向堆里的 PSP_B 栈

如何在pendsv中断中判断当前是PSP还是MSP？
通过 LR 寄存器中的 EXC_RETURN 值来判断。
EXC_RETURN 是什么
进入中断时，ARM 硬件会自动往 LR 里写一个特殊值（EXC_RETURN），记录进中断前的状态：
EXC_RETURN 常见值：
0xFFFFFFF1 → 返回 Handler 模式，用 MSP（中断嵌套）
0xFFFFFFF9 → 返回 Thread 模式，用 MSP（裸机主程序）
0xFFFFFFFD → 返回 Thread 模式，用 PSP（FreeRTOS 任务）
关键是 bit[2]：
LR 的 bit[2]：
0 → 进中断前用的是 MSP
1 → 进中断前用的是 PSP
一句话：通过 LR 寄存器的 bit[2] 判断，0 是 MSP，1 是 PSP。FreeRTOS 的 PendSV 不需要判断（任务一定用 PSP），但 HardFault 等通用异常处理必须判断。


一、内存泄漏的核心定义（先讲清楚本质）
内存泄漏是指程序动态分配的内存（堆内存）在使用完后未释放，导致这块内存永远无法被再次使用，最终耗尽系统所有可用内存，引发程序卡死、崩溃（嵌入式中表现为 RAM 不足、任务栈溢出、HardFault）。
二、嵌入式场景下内存泄漏的核心根因（按出现频率排序）
1. 最常见：malloc/free（pvPortMalloc/vPortFree）不配对
根因：动态分配内存后，因逻辑分支、异常处理等原因漏写释放代码，是嵌入式最核心的泄漏原因。
典型场景：


2. 隐蔽型：重复分配未释放旧内存
• 根因：指针被重新赋值前，未释放指向的旧内存，导致旧内存地址丢失，无法释放。
• 典型场景：


3. 边界型：条件释放遗漏（如循环 / 中断中）
根因：循环中多次分配内存，但仅在循环正常结束时释放，异常退出（如 break/return）时漏释放；或中断中分配内存未释放（中断中禁止用动态内存！）。
典型场景：

4. 设计型：全局 / 静态指针持有内存不释放
根因：全局指针长期持有动态内存，直到程序结束都不释放（嵌入式程序长期运行，等同于永久泄漏）。
典型场景：

5. 工具 / 库型：第三方库内部泄漏
根因：使用的第三方库（如网络库、文件系统库）内部存在内存泄漏，调用者无感知。

三、嵌入式内存泄漏的解决方法（按 “定位→修复→预防” 流程）
第一步：定位内存泄漏
嵌入式无 PC 端的 valgrind，但有专属定位方法，按优先级排序：
1. 内存统计法（最易落地）
原理：封装pvPortMalloc/vPortFree，统计 “分配 / 释放次数 + 内存大小”，打印泄漏统计：


分析：若 “泄漏字节数” 持续增长，说明存在泄漏；结合日志定位哪个函数调用后泄漏数增加。
2. 地址追踪法（精准定位泄漏点）
原理：分配内存时记录 “地址 + 调用函数 + 行号”，释放时删除记录，剩余记录即为泄漏地址：




3. 硬件调试法（终极手段）
原理：用 J-Link/ST-Link 连接 MCU，在 IDE 中查看 RAM 使用情况（如 STM32CubeIDE 的 “Memory View”），观察堆内存地址范围（如 0x20000000~0x20005000）的占用是否持续增长，结合断点定位分配点。

第二步：修复内存泄漏（核心原则：“配对释放 + 异常保护”）
针对不同根因的修复方法：
根因类型	修复方案
malloc/free 不配对	用 “goto 统一释放” 或 “RAII 思想（嵌入式模拟）”，确保所有分支都释放：c<br>void func(void) {<br> char *buf = my_malloc(100);<br> if (buf == NULL) return;<br> if (uart_send(buf, 100) != 0) {<br> goto exit; // 异常分支跳转到统一释放点<br> }<br>exit:<br> my_free(buf, 100); // 统一释放<br>}<br>
重复分配未释放旧内存	赋值前先释放旧内存：c<br>if (g_buf) my_free(g_buf, 200);<br>g_buf = my_malloc(300);<br>
循环 / 中断中泄漏	循环中用 “统一释放点”；中断中禁止使用动态内存（改用静态内存 / 全局缓冲区）
全局指针持有内存	设计 “释放接口”，在合适时机（如模块卸载、设备休眠）释放：c<br>void deinit_data(void) {<br> if (g_data) my_free(g_data, 1024);<br> g_data = NULL;<br>}<br>
第三方库泄漏	替换库 / 修改库源码；或限制库的调用次数（如定时重启模块）
第三步：预防内存泄漏（高级工程师的核心能力）
1.尽量不用动态内存：嵌入式优先用静态内存 / 栈内存（如static char buf[100]），仅在必要时用动态内存（如不确定大小的缓冲区）；
2.内存池替代 malloc：提前分配固定大小的内存池，从池中申请 / 释放，避免碎片化 + 便于统计：


·  代码规范：
动态内存分配后必须立即检查NULL；
分配和释放必须在 “同一层函数”（避免跨模块释放导致遗漏）；
禁止在中断 / 定时器中使用动态内存；
·  代码评审：新增动态内存相关代码时，强制评审，检查配对释放；
·  长期测试：设备上电连续运行 72 小时以上，监控内存占用是否稳定（嵌入式产品需通过 “老化测试”）。

一、内存泄漏跟内存碎片有关联吗？
核心结论：有间接关联，但本质是两个不同问题
1. 先明确两个概念的本质差异
维度	内存泄漏	内存碎片
核心定义	动态内存分配后未释放，内存永久丢失	多次 malloc/free 后，内存被拆分成大量不连续的小块，总空闲内存足够但无法分配大块内存
根本原因	代码逻辑漏洞（释放遗漏）	内存分配 / 释放的碎片化（如交替分配大小不同的内存）
表现形式	可用内存总量持续减少，最终耗尽	可用内存总量足够，但申请大块内存时失败（返回 NULL）
能否恢复	程序运行期间无法恢复（除非重启）	可通过内存池 / 规整算法缓解，无法完全消除
2. 关联点
间接因果：内存泄漏会加速内存碎片问题 —— 泄漏导致可用内存减少，剩余内存更易被拆分成小碎片；
叠加危害：嵌入式中二者叠加会让问题更严重：比如泄漏导致可用内存剩 5KB，碎片让这 5KB 拆成多个 1KB 小块，无法申请 2KB 的连续内存，最终程序卡死。

问题 1：A 调用 B 时，PC 等于 B 的入口地址吗？
是的，完全正确。 执行 BL B_func 后的瞬间：
LR = A 中 BL 指令的下一条指令地址（A 的返回地址）
PC = B_func 的入口地址（B 的第一条指令）
用具体地址演示：
A 函数：
0x1000  MOV  R0, #1
0x1004  BL   B_func       ; 执行这条指令时 ——
0x1008  ADD  R0, R0, #2   ;   LR = 0x1008（下一条）
                           ;   PC = 0x2000（B 的入口）

B 函数：
0x2000  PUSH {LR}          ; ← PC 跳到这里，开始执行 B
0x2004  ...
问题 2：返回地址和入口地址是同一个吗？
不是，它们是两个完全不同的概念，而且属于不同的函数。
         A 函数                              B 函数
    ┌──────────────┐                   ┌──────────────┐
    │ ...          │                   │              │
    │ BL B_func ───────────────────────→ 0x2000 PUSH  │ ← B 的入口地址
    │ 0x1008 ADD ← ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ BX LR        │
    │ ...          │   （函数返回）      │              │
    └──────────────┘                   └──────────────┘
           ↑                                  ↑
      A 的返回地址                         B 的入口地址
      （LR 保存的值）                     （BL 跳转的目标）
一句话总结
入口地址是"去哪里"（目标函数的起点），返回地址是"回哪里"（调用者的继续点）。
它们分别属于被调用方和调用方，永远不会是同一个地址。

每一个函数都具备入口地址和返回地址是吧
答：入口地址 — ✅ 每个函数都有，且是固定的
函数的入口地址是编译链接时就确定的，是函数的"身份证号"，不会变。
void func_A(void) { ... }   // 入口地址: 0x1000（固定）
void func_B(void) { ... }   // 入口地址: 0x2000（固定）
void func_C(void) { ... }   // 入口地址: 0x3000（固定）
返回地址 — ⚠️ 不是函数自身的属性，而是"谁调用它"决定的
返回地址不固定，取决于调用者。同一个函数被不同地方调用，返回地址不同：
void caller_1(void) {
    // 0x4000
    func_B();          // BL func_B → LR = 0x4004
    // 0x4004 ← 返回地址 1
}
void caller_2(void) {
    // 0x5000
    func_B();          // BL func_B → LR = 0x5004
    // 0x5004 ← 返回地址 2
}
void caller_3(void) {
    // 0x6000
    func_B();          // BL func_B → LR = 0x6004  
    // 0x6004 ← 返回地址 3
}

同一个 func_B，被调用了 3 次，产生了 3 个不同的返回地址。


如何结合 MAP 文件、CPU 寄存器组定位 crash 的地址？
核心逻辑：寄存器找 crash 地址 → MAP 文件转成 “函数 / 代码行” → 定位根因（嵌入式 crash 定位的核心方法）
1. 分步实战方法（按步骤说，体现落地能力）
步骤 1：抓取 crash 时的 CPU 寄存器（关键是 PC/LR 寄存器）
裸机：HardFault 中断中读取PC（程序计数器）、LR（返回地址）、SP（栈指针）（如之前讲的 HardFault_Handler 代码）；
LR（Link Register，链接寄存器）不是表示函数地址，而是存储函数的返回地址——即调用者的下一条指令地址。
在调试中的作用
在嵌入式开发中，LR 的值在 crash 分析中非常重要——它能告诉你"是从哪个函数调用过来的"，是栈回溯（backtrace）的关键信息之一。

总结：LR 存的是"调用完函数后要回到哪里"的地址，不是被调用函数本身的地址。简单记：LR = 回家的路。

RTOS：FreeRTOS 的 crash 钩子函数中保存寄存器，或用 J-Link 在线调试暂停在 crash 处，查看寄存器窗口。
核心：PC 寄存器的值就是 crash 的指令地址，LR 是调用该函数的返回地址。
步骤 2：解析 MAP 文件（编译器生成，记录 “地址→函数 / 文件 / 行号” 映射）
MAP 文件是编译时生成的文本文件（Keil/STM32CubeIDE 默认生成），包含：
o代码段（.text）、数据段（.data）、BSS 段的地址范围；
o每个函数 / 全局变量的起始地址、大小、所属文件。
关键操作：
1.打开 MAP 文件，搜索PC寄存器值所在的地址区间；
2.找到对应的函数名、源文件、行号（或接近的函数）。
步骤 3：精准定位（补充工具）
若 MAP 文件精度不够，用addr2line工具（ARM 编译器自带）：

→ crash 在 main.c 第 123 行的 func_name 函数中。
步骤 4：结合栈 / 寄存器分析根因
若 PC 地址指向 “LDR R0, [R1]” 指令，且 R1=0x00000000 → 空指针解引用；
若 PC 地址在栈区（SP 附近）→ 栈溢出；
若 LR 地址异常 → 函数调用栈损坏。

1.库仑计 + OCV 如何做电量预估？
我在项目里是库仑计（库仑积分）做实时估算 + OCV 做校准修正的融合方案：
（1）库仑计（实时）：
实时采集充放电电流，对时间积分：Ah=∫I⋅dt
用这个算出实时消耗 / 充入容量，反推当前剩余容量 SOC。
优点：实时性好、响应快。
缺点：时间长了会漂移、累积误差。
（2）OCV（校准）
OCV = 电池开路电压，和 SOC 有强对应关系，厂家会给 OCV-SOC 曲线。
满足条件时（静置 > 10min、电流很小、温度稳定），读取 OCV，查表得到基准 SOC。用这个基准去修正库仑计的累积误差。
（3）融合逻辑（实际代码逻辑）
正常充放电：库仑计为主，快速响应。
静置稳定：OCV 校准，拉回真实 SOC。
温度、电流大小做补偿。
加滤波，防止 SOC 跳变。
一句话总结：
库仑计保证实时性，OCV 消除长期漂移，两者融合得到稳定、准确的 SOC。

4. 内存泄漏（堆）为什么会导致任务栈溢出？
核心结论
堆和栈物理上同属 MCU RAM，堆泄漏会挤占栈空间，最终导致栈溢出：
通俗解释 + 实操逻辑
内存布局基础：
MCU 的 RAM 被划分：堆（heap）从低地址向上增长，栈（stack）从高地址向下增长，中间是空闲区域；
泄漏→栈溢出的过程：
堆泄漏：反复 malloc/pvPortMalloc 不释放，堆持续向上扩张；
堆侵占栈的空闲区域，任务栈可用空间越来越小；
当任务运行时（如函数调用、局部变量分配），栈空间不足，就会触发栈溢出，进而导致 HardFault；
极端情况：堆直接覆盖栈的关键数据（如任务返回地址），程序直接崩溃。
一句话总结
堆和栈共享 RAM 空间，堆泄漏持续挤占栈空间，最终导致任务栈空间不足，触发栈溢出。


SOH 健康状态：使用时长、温度系数、循环系数具体怎么做？
SOH 我是按容量衰减 + 内阻上升 + 循环累计 + 温度影响综合计算：
（1）容量法（最核心）
SOH = 当前满充容量 / 出厂标称容量SOH=Nominal_CapCurrent_Full_Cap​×100%
满充容量通过完整充放电循环学习得到。

（2）循环次数系数 Cycle Count
每完成一次充放电循环，Cycle+1。
查表 / 曲线：循环次数越多，容量衰减越多，SOH 越低。
（3）温度影响系数
高温加速老化 → 容量衰减更快。
软件做温度积分：Aging+=Temp_Coeff×Time
温度越高，系数越大，SOH 掉得越快。
（4）内阻法辅助
电芯内阻随老化上升。
内阻越大 → SOH 越低。
用交流阻抗或直流压降计算。
（5）最终 SOH 合成
主：容量衰减
辅：循环次数 + 温度累计 + 内阻
加权融合输出最终 SOH。
一句话总结
SOH 以实际可用容量为核心，结合循环次数、温度累计老化、内阻上升综合计算，反映电池真实健康程度。

嵌入式的交叉验证 = 多源互证，用不同的工具、数据、方法独立验证同一个结论，避免单一证据带来的误判。
实际工作中，分析一个疑难问题时，通常先从日志得到一个初步判断，然后用抓包、示波器、代码走读、换板测试等手段做交叉验证，多条证据链对齐了才能下结论。

如何判断充电芯片 SGM41519 硬件漏电？
（1）核心结论
不是靠软件猜，是硬件实测 + 软件辅助定位，分 3 步：
（2）具体做法
a基础对比测试
搭建空白板（只焊电源 + SGM41519，无其他外设），和正常样板对比静态功耗：
正常板：低功耗模式下整机电流≈芯片手册标称的休眠电流（如 < 10μA）；
异常板：电流远高于标称（如 > 100μA）。
b分段断电定位
断开 SGM41519 的供电引脚（VIN），整机静态电流立刻降到正常水平 → 锁定漏电源是该芯片。
进一步测芯片各引脚对地电流：比如 EN 引脚、BAT 引脚对地有异常漏电流，确认芯片本体 / 外围电路漏电。
c软件辅助验证
软件控制 SGM41519 进入深度休眠（拉低 EN 引脚），再测电流：
若电流仍高 → 硬件漏电（芯片 / 外围电阻电容漏电）；
若电流正常 → 软件控制逻辑问题。
一句话总结
通过「空白板对比 + 分段断电 + 软件控休眠」三步，实测电流异常且断电芯片后恢复正常，即可判定 SGM41519 硬件漏电。

2. 低功耗场景下串口 TX/RX 引脚如何配置不漏电？配置成输入可以吗？
核心结论
配置成输入不完全行，必须按「引脚类型 + 休眠逻辑」精准配置，具体：
具体做法
为什么输入不行？
若串口引脚是推挽输出，直接设为输入：引脚电平悬空，可能因外部干扰产生浮空电流；
若芯片 UART 外设未关闭，即使引脚设为输入，外设模块仍会耗电 → 双重漏电。
正确配置步骤① 先关闭 UART 外设时钟（核心！）：比如 STM32 中__HAL_RCC_USART1_CLK_DISABLE()，彻底切断外设供电；② 引脚配置：
TX 引脚：设为上拉 / 下拉输入（避免浮空），或直接设为模拟输入（STM32 的 Analog 模式，引脚完全断开数字电路，漏电最低）；
RX 引脚：同 TX，优先模拟输入，其次下拉输入（匹配串口空闲电平）；③ 若有外部电平转换芯片（如 RS232），需同时关闭转换芯片供电。
一句话总结
仅设输入不够，必须先关 UART 外设时钟，再将 TX/RX 设为模拟输入（或上下拉输入），彻底切断引脚和外设的耗电路径。

3. FreeRTOS 中 HardFault 保存任务上下文 / 寄存器，是系统自带还是自己加？
核心结论
FreeRTOS不会自动做，必须自己适配，这是 BMS 等高可靠性产品的必做优化：
具体做法
FreeRTOS 的原生能力：
仅提供任务切换时的上下文保存（如 pxPortTCB 中存寄存器），但 HardFault 是内核级异常，系统不会主动抓取此时的任务上下文；
自己添加的核心逻辑：① 改写 HardFault_Handler 中断函数：
进入中断后，先读取 MSP/PSP 寄存器（区分当前是内核态还是任务态）；
若为任务态（PSP 有效），从 PSP 中提取当前任务的 R0-R15、LR、PC 等寄存器；
将这些寄存器值 + 当前任务句柄（xTaskGetCurrentTaskHandle ()）保存到指定 RAM 区域（或 Flash）；② 配合 MAP 文件 /addr2line 工具，通过 PC 寄存器定位到触发 HardFault 的代码行；③ 进阶：保存 FreeRTOS 的任务列表、栈使用情况，方便定位是哪个任务出问题。
一句话总结
FreeRTOS 无原生支持，需自己改写 HardFault 中断函数，抓取 MSP/PSP 寄存器和当前任务上下文，这是电池产品稳定性排查的核心操作。


GTest 单元测试（嵌入式场景）
GTest（Google Test）是C++ 编写的开源单元测试框架，嵌入式中主要用于测试 “可移植的核心逻辑模块”（如协议解析、算法、数据结构），核心是 “模块化验证代码正确性”。
1. 嵌入式中 GTest 的核心适配点（区别于 PC 端）
无操作系统依赖：GTest 可编译为 “裸机版本”（无需 Linux/RTOS），仅依赖 C 标准库；
精简编译：关闭 GTest 的冗余功能（如彩色输出、多线程），适配 MCU 的有限 Flash/RAM；
交叉编译：针对 ARM 架构交叉编译 GTest 库，可在模拟器 / 真实 MCU 上运行。
2. 嵌入式 GTest 的实战流程
步骤 1：编写单元测试用例（核心逻辑模块）


步骤 2：交叉编译 GTest + 测试用例

步骤 3：运行测试（模拟器 / 真实 MCU）
模拟器运行：在 QEMU 中执行./test_crc32，输出测试结果；
真实 MCU 运行：将测试程序烧录到 MCU 的 Flash，通过串口输出测试结果。

三、虚拟地址到物理地址的映射怎么实现（嵌入式场景）
虚拟地址（VA）→ 物理地址（PA）的映射核心依赖 MMU（内存管理单元）（仅高端 MCU/MPU 支持，如 STM32MP1、RK3399），裸机 MCU（如 STM32F103）无 MMU，仅支持物理地址。
1. 核心原理
步骤 1：页表建立（核心数据结构）
• MMU 将内存划分为 “页”（如 4KB/1MB），操作系统（Linux/RTOS，包含kernel）在物理内存中建立页表（Page Table，由kernel完成），记录 “虚拟页地址 → 物理页地址” 的映射关系；
• 页表分多级（一级页表 + 二级页表），减少内存占用（如 ARMv7 架构的两级页表）。
步骤 2：MMU 地址转换（硬件自动完成）
• CPU 执行指令时，给出虚拟地址；
• MMU 根据页表将虚拟地址拆分为 “页目录号 + 页表项号 + 页内偏移”，查找物理页地址，拼接偏移得到最终物理地址；
• 若虚拟地址无映射（页缺失），触发缺页中断，操作系统分配物理页并更新页表。
2. 嵌入式 Linux 中的实战配置

. 嵌入式中虚实地址映射的核心价值
内存保护：不同进程的虚拟地址空间隔离，避免越界访问；
内存复用：多个进程共享同一块物理内存（如共享库）；
内存扩展：通过虚拟地址映射外部 RAM/Flash。

四、RTOS 上可以用 GDB 来读取 crash 时的 PC 寄存器吧？
核心结论：可以，但分场景（“在线调试” 和 “离线调试”）
1. 场景 1：在线调试（有调试器，实时读取）
硬件条件：MCU 连接 J-Link/ST-Link 调试器，调试器通过 SWD/JTAG 连接 PC；
操作流程：
1.启动 GDB（ARM 架构：arm-none-eabi-gdb），加载 ELF 文件；
2.连接调试器：(gdb) target remote :2331（J-Link 默认端口）；
3.RTOS crash 时，调试器暂停 MCU，直接读取 PC 寄存器：

适用场景：开发阶段，可复现的 crash 问题。
2. 场景 2：离线调试（无实时调试，crash 后读取）
核心方法：RTOS crash 时（如 HardFault），在中断处理函数中保存所有寄存器到 RAM/Flash，重启后通过 GDB 读取：

操作流程：
1.MCU crash 后，用 GDB 连接调试器；
2.读取保存的寄存器值：(gdb) x 0x20000000（g_crash_regs 的物理地址）；
3.结合 MAP 文件定位 crash 函数。
精简回答
可以用 GDB 读取 RTOS crash 时的 PC 寄存器，分两种场景：1）在线调试：MCU 接 J-Link，GDB 连接调试器，crash 时直接info registers pc读取；2）离线调试：HardFault 中断中保存 PC 寄存器到 RAM，重启后用 GDB 读取该内存地址；我常用这种方法定位 FreeRTOS 的栈溢出、空指针问题，比如某次 PC 寄存器指向 SPI 函数，结合 MAP 文件找到是数组越界导致的 crash，快速解决问题。

二、结合 MAP 文件 + CPU 寄存器定位 crash 地址（具体实例）
实战背景：
基于 STM32F103RCT6（256KB FLASH）+ FreeRTOS，设备运行中触发 HardFault，抓取到核心寄存器值：
PC 寄存器：0x08002568（crash 的指令地址）
LR 寄存器：0x08001F20（调用函数的返回地址）
SP 寄存器：0x20001800（栈指针）
步骤 1：抓取寄存器（HardFault 中断中保存）

步骤 2：解析 MAP 文件（关键片段）
编译生成的project.map文件核心内容：

·  从 MAP 文件可知：0x08002500 ~ 0x08002600 是sensor_process函数的地址区间；
·  崩溃 PC 地址0x08002568 落在该区间内 → 确定 crash 发生在sensor_process函数中。
步骤 3：精准定位代码行（用 addr2line 工具）

→ 精准定位到sensor.c第 189 行。
步骤 4：分析根因（结合代码）

根因：动态内存分配后未检查NULL，内存不足时data=NULL，解引用触发 crash。

什么叫交叉编译？（嵌入式核心概念）
核心定义：
交叉编译是指在一种架构的主机（如 x86_64 的 PC）上，编译出能在另一种架构的目标机（如 ARM Cortex-M/A）上运行的程序，嵌入式开发中 99% 的代码都是交叉编译生成的。
通俗理解：
主机：你的办公电脑（x86 架构，Windows/Linux）；
目标机：STM32/ARM 开发板（ARM 架构）；
交叉编译：在 PC 上编译代码，生成开发板能执行的二进制文件（.elf/.bin）。
嵌入式交叉编译的核心要点：
1.交叉编译器：必须用对应架构的编译器（如 ARM 架构用arm-none-eabi-gcc，MIPS 架构用mips-linux-gnu-gcc）；
2.编译选项：指定目标架构（-march=armv7-m）、浮点模式（-mfloat-abi=soft）、链接脚本（-Tlink.lds）；
3.核心价值：嵌入式 MCU / 开发板资源有限（无编译器），必须在 PC 上完成编译。
实战示例（交叉编译命令）：


“表项号” 指的是什么意思？（MMU 地址映射核心）
核心定义：
“表项号” 是 MMU（内存管理单元）做虚拟地址→物理地址映射时，对虚拟地址的分段编号，用于查找页表中对应的映射关系，核心是 “将大地址拆分为小索引，降低页表内存占用”。
通俗理解（以 ARMv7 4KB 页为例）：
ARMv7 的虚拟地址是 32 位，拆分为 3 部分：

·  页目录号：一级页表的索引；
·  页表项号：二级页表的索引（即 “表项号”）；
·  页内偏移：物理页内的字节偏移（4KB=2^12，故 12 位）
实例解析：
虚拟地址0xA0001234（32 位）拆分：
二进制：1010 0000 0000 0000 0001 0010 0011 0100；
拆分：
页目录号：前 12 位 → 1010 0000 0000（十进制 2560）；
页表项号：中间 8 位 → 0000 0001（十进制 1）；
页内偏移：后 12 位 → 0010 0011 0100（十进制 564）；
映射过程：
用 “页目录号 2560” 查一级页表，找到二级页表的物理地址；
用 “页表项号 1” 查二级页表，找到物理页地址（如0x40000000）；
拼接偏移：0x40000000 + 564 = 0x40000234（最终物理地址）。

空指针解引用是指：程序试图访问指针值为 NULL（通常是 0x00000000）的内存地址——NULL 是系统预留的无效内存地址（MCU 中该地址无物理内存 / 受 MPU 保护），访问该地址会直接触发硬件异常（如 ARM Cortex-M 的 HardFault、Linux 的段错误）。
二、嵌入式场景下的典型实例（按出现频率排序）
实例 1：动态内存分配后未检查 NULL（最常见）


实例 2：函数返回 NULL 未处理

实例 3：全局指针未初始化（隐蔽型）

实例 4：指针被错误赋值为 NULL

三、空指针解引用的危害（嵌入式场景下更致命）
1.直接触发 HardFault：MCU（如 STM32）会进入 HardFault 中断，程序卡死，设备死机；
2.调试难度高：无明确报错信息，需通过 PC 寄存器 / MAP 文件定位；
3.偶发性问题：内存充足时不触发，内存不足时才出现（如设备运行数月后因内存泄漏导致 malloc 返回 NULL），难以复现；
4.Linux 系统中触发段错误：嵌入式 Linux 会直接杀死进程，导致功能失效。
排查方法
• 寄存器定位：HardFault 时读取 PC 寄存器，结合 MAP 文件 /addr2line 定位解引用的代码行（如之前讲的 crash 定位方法）；
• 静态代码分析：用 Cppcheck/Clang Static Analyzer 扫描代码，提前发现 “未检查 NULL 的指针解引用”；
• 调试器断点：在指针使用前加断点，查看指针值是否为 NULL；
• 内存监控：封装 malloc/free，监控内存占用，提前预警内存不足。
空指针解引用是指访问值为 NULL 的指针指向的内存，嵌入式中会直接触发 HardFault 导致设备死机，是高频致命 bug：
典型场景：动态内存分配后未检查 NULL、函数返回 NULL 未处理、全局指针未初始化；
我在项目中通过 “强制 NULL 检查 + 静态代码分析 + 单元测试” 三类手段预防，排查时用 PC 寄存器 + MAP 文件定位解引用的代码行，比如某次定位到传感器数据处理函数中 malloc 返回 NULL 导致的空指针，加 NULL 检查后彻底解决；
核心原则：所有指针使用前必须检查是否为 NULL，尤其是嵌入式 MCU 内存资源有限，malloc 极易返回 NULL。

二、FreeRTOS 栈溢出检测钩子函数：是否保存寄存器？自己写还是系统自带？
1. 核心结论
FreeRTOS 的栈溢出钩子函数仅提供 “栈溢出事件触发”，不保存寄存器信息，所有寄存器 / 上下文保存逻辑都需要自己实现，具体拆解：
2. 先明确 FreeRTOS 的原生能力
栈溢出检测机制：FreeRTOS 提供两种栈溢出检测（配置configCHECK_FOR_STACK_OVERFLOW为 1/2）：① 模式 1：检测任务栈的 “末尾 2 个字节” 是否被改写（简单但不精准）；② 模式 2：每次任务切换时检查栈指针是否超出栈空间（更精准）；
钩子函数原生逻辑：触发栈溢出时，FreeRTOS 会调用vApplicationStackOverflowHook(xTaskHandle pxTask, char *pcTaskName)，但这个函数里：✅ 系统只传两个参数：出问题的任务句柄、任务名；❌ 完全不保存寄存器、PC/LR/SP 等核心信息；
3. 自己需要实现的逻辑
第一步：改写钩子函数在vApplicationStackOverflowHook中添加：① 通过任务句柄pxTask获取任务的 TCB（任务控制块），提取栈基地址、栈大小、当前栈指针；② 读取 MSP/PSP 寄存器（区分内核态 / 任务态），保存 R0-R15、LR、PC、PSR 等寄存器值；③ 将这些信息写入指定 RAM/Flash（或通过调试口输出），方便后续定位；
第二步：配合 HardFault 处理栈溢出大概率会触发 HardFault，因此需要把 “栈溢出钩子函数” 和 “HardFault 上下文保存” 联动：比如在钩子函数中先标记 “栈溢出任务”，再在 HardFault 中优先保存该任务的寄存器，精准定位问题。
4. 补充实战案例
我在电池充放电控制任务中做过优化：
开启 FreeRTOS 栈溢出检测（模式 2）；
在钩子函数中保存出问题的任务名 + 栈指针 + 核心寄存器；
配合 MAP 文件，快速定位到 “函数递归调用导致栈溢出” 的问题，修复后任务运行稳定无异常。
一句话总结
FreeRTOS 仅提供栈溢出的触发钩子，不保存任何寄存器信息，需自己在钩子函数中读取并保存寄存器 / 任务上下文，这是定位栈溢出根因的核心操作。

阶段	核心作用	输入文件	输出文件	嵌入式常用工具
预处理	处理宏 / 头文件 / 条件编译，生成纯 C 代码	.c/.h	.i（预处理文件）	arm-none-eabi-gcc -E
编译	将纯 C 代码转为汇编代码	.i	.s（汇编文件）	arm-none-eabi-gcc -S
汇编	将汇编代码转为机器码（目标文件）	.s/.S	.o（目标文件）	arm-none-eabi-as
链接	合并目标文件 + 库，生成可执行文件	.o/.a（库文件）	.elf/.axf	arm-none-eabi-ld/ld.exe


1、MEMS：微机电系统
IMU：惯性测量单元，用于测量物体三轴姿态角(或角速率)以及加速度。
维度	零偏（Zero Bias）	零漂（Zero Drift）
核心定义	惯导传感器（陀螺 / 加速度计）在无输入（静止）时，输出的固定偏移值（偏离真实 0 值的误差）。	零偏随时间、温度、电压、振动等因素缓慢变化的现象（本质是零偏的 “时变特性”）。
本质	静态误差（固定值 / 短期稳定）	动态误差（随环境 / 时间漂移的过程）
表现形式	比如陀螺静止时输出 0.05°/s（真实应为 0），这个 0.05°/s 就是零偏。	上述陀螺的零偏，1 小时后变成 0.08°/s，温度升高 10℃后变成 0.10°/s，这个变化过程就是零漂。
产生原因	1. 传感器硬件制造误差（如陀螺转子不平衡、加速度计摆片偏移）；2. 电路失调（运放偏置、ADC 零点误差）；3. 安装误差（传感器轴与惯导坐标系不重合）。	1. 温度变化导致硬件参数漂移（如电阻 / 电容温漂）；2. 器件老化（长期使用后性能衰减）；3. 供电电压波动；4. 测试环境振动 / 电磁干扰；5. 传感器内部噪声累积。
关联性	零漂是零偏的 “动态变化”，零偏是零漂的 “某一时刻的静态结果”。	零漂的最终体现是零偏值随时间 / 环境变化，无零偏则无零漂。
测试系统关注点	需校准（消除固定偏移）	需补偿 / 抑制（降低随时间 / 环境的变化幅度）
惯导测试系统中 “零漂” 的解决方法（软件层面）：
（1）测试校准：精准测量零漂特性（软件补偿的前提）
测试系统需先通过标准化流程，获取零漂的 “时间 / 温度特性曲线”，为补偿提供数据基础：
a、静态零漂测试（关键步骤）：
惯导上电后预热 30 分钟（稳定器件参数），置于静止状态；
MCU 按固定频率（如 100Hz）采集陀螺 / 加速度计输出，持续采集 1~24 小时；
对采集数据做滑动平均滤波（MCU 端轻量化实现），剔除随机噪声，提取纯零漂趋势；
记录零漂随时间的变化曲线（时间漂移）、随温度的变化曲线（温度漂移）。
b、数据拟合：
对温度 - 零漂数据做多项式拟合（如二次拟合：零偏 = k0+k1T+k2T²，T 为温度），得到温度补偿模型；
对时间 - 零漂数据做线性拟合（零偏 = k0+k3*t，t 为时间），得到时间补偿模型。
3. 软件算法：MCU 端实时补偿零漂（核心落地）
基于测试得到的零漂模型，在 MCU 中实现轻量化补偿算法，实时消除零漂影响，适配嵌入式资源限制：


2、硬件PWM和软件PWM的区别：
类型	是否依赖定时器	核心逻辑	精度	资源占用	适用场景
硬件 PWM	✅（必须）	定时器硬件自动生成方波	极高	无 CPU 占用	电机控制、LED 调光、伺服驱动
软件 PWM（中断版）	✅（主流）	定时器中断触发 CPU 切换 GPIO 电平	较高	占用 CPU 中断	低成本 MCU、无硬件 PWM 通道时
软件 PWM（延时版）	❌（极简易）	CPU 忙等延时切换电平	极低	占用 CPU 全部资源	仅测试 / 演示，无工程价值

3、步进电机调速，本质就是：改变发脉冲的频率（快慢）。频率越高，转速越快；频率越低，转速越慢。
（1）步进电机调速 = 改变发脉冲频率
（2）频率越高 → 转速越快
（3）工程实现：
硬件 PWM（匀速）
定时器中断发脉冲（加减速、定位）
（4）转速由 脉冲频率、步距角、细分 共同决定
硬件 PWM 之所以是 “匀速”，核心是它生成的脉冲频率从配置完成后就完全固定（由定时器 ARR+PSC 决定），只要不主动修改参数，脉冲就会以恒定频率持续输出，步进电机自然匀速转动；而定时器中断模式下，你可以在中断外动态改 ARR，甚至在加减速逻辑中逐次微调 ARR，实现非匀速（加速 / 减速）。

CPU寄存器组都是保存在栈中的吗？并且是保存在PSP中的吧
答：对，任务的寄存器都保存在各自任务的栈（PSP） 中，但保存过程分两步，一半硬件自动保存，一半软件手动保存。
ARM Cortex-M 寄存器全集
通用寄存器：R0 ~ R12（13个）
特殊寄存器：SP(R13)、LR(R14)、PC(R15)、xPSR
两步保存机制
第一步：硬件自动保存（进入中断时 CPU 自动做）
当发生中断（包括 PendSV），CPU 硬件自动把 8 个寄存器压入当前任务的 PSP 栈：
PSP 栈（硬件自动压入）
┌──────────────┐ ← PSP 原始位置
│ xPSR │ 程序状态
│ PC │ 返回地址（被打断的位置）
│ LR (R14) │ 链接寄存器
│ R12 │ 
│ R3 │ 
│ R2 │ 这 8 个寄存器是 ARM 调用约定中的
│ R1 │ "调用者保存" 寄存器
│ R0 │ 
└──────────────┘ ← PSP 新位置（自动下移）
这一步不需要写任何代码，CPU 硬件自动完成。
第二步：软件手动保存（PendSV 代码里做）
剩下的 R4~R11 硬件不会自动保存，需要 PendSV 手动压栈：
PendSV_Handler:
; 获取当前任务的 PSP
mrs r0, psp
; 手动保存 R4-R11 到 PSP 栈
stmdb r0!, {r4-r11} ; r0 先减再存（DB = Decrement Before）
; 保存更新后的 PSP 到任务控制块（TCB）
str r0, [当前任务TCB的栈指针字段]


2、MCU 选型怎么考虑？
答：MCU 选型不是只看参数，而是从项目需求倒推，整体从这几个维度综合评估：
（1）核心性能与算力
根据业务复杂度选择架构：8/16/32 位，ARM Cortex-M0/M0+/M3/M4/M7/H7 等。
看主频、Flash、RAM 是否满足算法、协议栈、UI、RTOS 等需求。
是否需要浮点单元 FPU、DSP 指令、硬件加速器。
（2）外设资源是否匹配
串口、SPI、I2C、CAN、ETH、USB、ADC、PWM 等数量与类型。
引脚数量、封装大小、是否支持 QSPI、LCD 控制器、定时器数量。
避免资源过剩或不够用，导致后期改板或浪费成本。
（3）功耗与供电场景
电池供电产品重点看：运行功耗、休眠功耗、唤醒时间。
选择支持低功耗模式（Stop、Standby、VBAT 等）的型号。
（4）工作环境与可靠性
温度等级：商业级、工业级、车规级。
ESD（静电放电）、EMC（电磁兼容性） 性能、抗干扰能力，是否适合现场环境。
（5）成本与供应链
单价、批量价格、最小包装、交期。
是否容易缺货、是否有国产替代、是否 PIN2PIN 兼容。
（6）开发生态与工具链
编译器、调试器、SDK、HAL 库、Demo 丰富度。
社区资料、技术支持、是否容易招人维护。
（7）可扩展性与兼容性
同一平台是否有高低配型号，方便后续升级 / 降级。
避免一次性、封闭、无延续性的芯片。
（8）安全与信息安全
是否支持加密、安全启动、读写保护、OTA 安全升级等。
总结：MCU 选型的本质，是在性能、功耗、成本、稳定性、开发效率之间做最优平衡，而不是盲目选最强或最便宜的。

3、V模型开发流程指的是什么
答：V 模型是嵌入式软件开发里最经典、最常用的瀑布式改进模型，核心思想是：开发阶段和测试阶段一一对应，左边开发，右边测试，尽早发现缺陷。
它的结构像一个 “V” 字，分成左右两部分：
左侧（开发阶段，从上到下）
1.需求分析
2.概要设计（系统设计）
3.详细设计（模块 / 软件设计）
4.编码实现
右侧（测试阶段，从下到上）
1.单元测试 —— 对应 编码 & 详细设计
2.集成测试 —— 对应 概要设计
3.系统测试 —— 对应 需求分析
4.验收测试 —— 对应 用户需求

4、异步广播：


发布 / 订阅（Pub/Sub）的本质是通过一个「中间层（事件总线 / 消息中心）」，让发布者和订阅者完全解耦：
订阅者：把自己要监听的「事件名」和「处理函数」注册到中间层；
发布者：只需要向中间层「发布」某个事件名 + 数据，不用管谁订阅；
中间层：收到发布的事件后，遍历该事件下所有注册的处理函数，逐个执行。
简单说，中间层就像一个「事件注册表」，核心就是「注册 - 发布 - 触发」三步。

RISC-V 就是 ATBM6461 这颗芯片的 CPU 架构，决定了它用什么编译器（riscv32-elf-gcc）、什么指令集、什么汇编语法。对你写 C 代码基本没影响，但启动文件和链接脚本是 RISC-V 专用的。
1、架构（Architecture / ISA）
指令集规范，是一份"纸上的标准"，定义了：
CPU 能执行哪些指令（加减乘除、跳转、访存...）
有哪些寄存器（x0-x31）
指令编码格式（32-bit 定长）
内存模型、中断机制
RISC-V 是架构，就像一份"语法规则书"
2、内核（Core）
架构的具体硬件实现，是一个实际的电路设计：
同一个架构可以有完全不同的内核实现，性能、面积、功耗都不一样。

.rodata — Read-Only Data（只读数据段）
编译时确定的是内容（值和大小）
const char *msg = "Hello"; // 编译时确定：内容是 H,e,l,l,o,\0，大小 6 字节 
const uint8_t table[] = {1,2,3}; // 编译时确定：内容是 1,2,3，大小 3 字节
编译器生成 .o 文件时，这些数据的每一个字节的值都已经确定了，不会再变。
链接时确定的是地址（放在 Flash 哪里）
编译后 (.o 文件)：
.rodata: "Hello" → 地址=待定（偏移 0x00 开始，相对地址）
链接后 (.elf 文件)：
.rodata: "Hello" → 地址=0x08004000（Flash 中的绝对地址，由 .ld 决定）

各阶段职责
源码 (.c)
   │
   ▼  ① 编译阶段 (gcc -c)
目标文件 (.o)
   │     此时 .rodata 已经存在于 .o 文件中
   │     但地址是"待定"的（相对偏移，还没分配最终地址）
   │
   ▼  ② 链接阶段 (ld, 读取链接脚本 .ld)
可执行文件 (.elf)
   │     此时 .rodata 被分配了 Flash 中的具体地址
   │     链接脚本决定了它放在 Flash 的哪个区域
   │
   ▼  ③ 生成二进制 (objcopy)
固件 (.bin / .hex)
   │     .rodata 的内容和地址都固定了
   │
   ▼  ④ 烧录
Flash
一句话纠正
所以准确的说法是：
.rodata 的内容（值和大小）在编译时确定，地址在链接时确定，烧录时写入 Flash。


在内存中的位置
Flash
┌──────────────┐
│  .text       │ ← 代码（函数体）
├──────────────┤
│  .rodata     │ ← 只读数据（字符串、const 表）
├──────────────┤
│  .data 初值  │ ← 启动时拷贝到 RAM
└──────────────┘

RAM
┌──────────────┐
│  .data       │ ← 从 Flash 拷贝过来的
├──────────────┤
│  .bss        │ ← 清零
├──────────────┤
│  Heap / Stack│
└──────────────┘
rodata 只在 Flash 里，不占 RAM。这就是为什么嵌入式开发中尽量用 const 修饰不变的数据 — 省 RAM。
一句话总结
rodata = 编译时写死、运行时只读的数据。 字符串、const 表、查找表都在这里，存 Flash 不占 RAM。

flash中的.data初值在启动时拷贝到RAM，那这个占用的flash空间可以得到释放吗？
不能释放。
Flash 不像 RAM 那样有动态分配/释放的概念，它是静态分区的：
Flash 布局（固定不变）
┌─────────────────────┐ 0x00000000
│      .text          │  代码段
│      (程序指令)      │
├─────────────────────┤
│      .rodata        │  只读数据
│      (常量)          │
├─────────────────────┤
│    .data 初值        │  ← 这块空间永远在这里
│   (拷贝到RAM后       │     不会被"释放"
│    仍然占着Flash)     │
├─────────────────────┤
│                     │
│    剩余Flash空间     │  未使用
│                     │
└─────────────────────┘
为什么不能释放：
Flash 没有 malloc/free 机制 — 不像 RAM 堆内存可以动态管理
分区在链接阶段就固定了 — 烧录后布局不会变
Flash 擦写需要特殊操作 — 即使"擦掉"了，也没有运行时机制把这块空间分配给别的用途

实际影响：
一个 int g_value = 100; 会同时占用：
4 字节 Flash（存初值 100）
4 字节 RAM（运行时变量）
而 int g_value;（未初始化，放 .bss，.bss中存放的是未初始化和初始化为0的静态变量和全局变量，静态变量包括静态全局变量和局部静态变量）只占：
0 字节 Flash（.bss 不存初值，启动时清零就行）
4 字节 RAM
省 Flash 的实用技巧：
// ❌ 占 Flash + RAM 
static uint8_t buffer[1024] = {0}; // 1KB初值存在Flash里（全是0） 
// ✅ 只占 RAM（.bss段，启动自动清零） 
static uint8_t buffer[1024]; // 效果一样，省1KB Flash 
// ✅ 运行时赋值代替编译时初始化 
static int config_value; // .bss，不占Flash 
void init(void) { 
config_value = read_from_nv(); // 运行时赋值 
}



链接文件
具体职责：
定义 Flash 和 RAM 的起始地址和大小
指定中断向量表放在哪里
指定代码段、数据段、BSS 段的存放位置
定义栈顶地址和堆大小
常见文件名：*.ld（GCC）、*.icf（IAR）、*.sct（Keil）

用 STM32F103C8T6（最经典的"蓝色药丸"）举例：
芯片硬件参数
STM32F103C8T6：
├── Flash: 64KB，起始地址 0x08000000
└── RAM: 20KB，起始地址 0x20000000
链接文件示例（STM32F103C8T6.ld）
/* ============================================
* 1️⃣ 定义 Flash 和 RAM 的起始地址和大小
* ============================================ */
MEMORY
{
FLASH (rx) : ORIGIN = 0x08000000, LENGTH = 64K
RAM (rwx) : ORIGIN = 0x20000000, LENGTH = 20K
}
/* 栈顶 = RAM起始 + RAM大小 = 0x20005000 */
_estack = ORIGIN(RAM) + LENGTH(RAM);
SECTIONS
{
/* ============================================
* 2️⃣ 中断向量表 — 必须放在 Flash 最开头
* ============================================ */
.isr_vector :
{
. = ALIGN(4);
KEEP(*(.isr_vector)) /* 中断向量表，KEEP防止被优化掉 */
. = ALIGN(4);
} > FLASH
/* ============================================
* 3️⃣ 代码段 (.text) — 紧跟向量表后面
* ============================================ */
.text :
{
. = ALIGN(4);
*(.text) /* 所有 .c 文件编译出的函数指令 */
*(.text*) /* 包括编译器生成的子段 */
*(.glue_7) /* ARM/Thumb 互调用代码 */
*(.glue_7t)
. = ALIGN(4);
_etext = .; /* 记录代码段结束地址 */
} > FLASH
/* 只读数据段 (.rodata) — 也在 Flash */
.rodata :
{
. = ALIGN(4);
*(.rodata) /* const 变量、字符串常量 */
*(.rodata*)
. = ALIGN(4);
} > FLASH
/* ============================================
* 3️⃣ 数据段 (.data) — 初值存Flash，运行时在RAM
* ============================================ */
_sidata = LOADADDR(.data); /* Flash中.data初值的起始地址 */
.data :
{
. = ALIGN(4);
_sdata = .; /* RAM中.data的起始地址 */
*(.data)
*(.data*)
. = ALIGN(4);
_edata = .; /* RAM中.data的结束地址 */
} > RAM AT> FLASH
/* ↑运行时在RAM ↑加载时存在Flash */
/* ============================================
* 3️⃣ BSS 段 — 只在 RAM，启动时清零
* ============================================ */
.bss :
{
. = ALIGN(4);
_sbss = .; /* BSS 起始地址 */
*(.bss)
*(.bss*)
*(COMMON)
. = ALIGN(4);
_ebss = .; /* BSS 结束地址 */
} > RAM
/* ============================================
* 4️⃣ 堆和栈
* ============================================ */
_heap_start = _ebss; /* 堆从 BSS 后面开始 */
_heap_size = 0x200; /* 堆大小 512 字节 */
_stack_size = 0x400; /* 栈大小 1024 字节 */
}
最终 Flash 和 RAM 布局

中断向量表放在 Flash 最开头的位置（地址 0x08000000）。
它的内容就两样东西：
Flash 起始地址 0x08000000
┌─────────────────────────────────┐
│ 第0个字：栈顶初始值 (_estack) │ → 0x20005000
├─────────────────────────────────┤
│ 第1个字：Reset_Handler 地址 │ → 上电/复位后跳转到这里
├─────────────────────────────────┤
│ 第2个字：NMI_Handler 地址 │
├─────────────────────────────────┤
│ 第3个字：HardFault_Handler 地址 │
├─────────────────────────────────┤
│ 第4个字：MemManage_Handler │
├─────────────────────────────────┤
│ ... │
├─────────────────────────────────┤
│ 第N个字：UART1_IRQHandler 地址 │
├─────────────────────────────────┤
│ ...更多外设中断入口... │
└─────────────────────────────────┘
STM32 上电过程就靠这张表：
1、CPU 从 0x08000000 读第一个字 → 写入 MSP（主栈指针）→ 栈就有了
2、CPU 从 0x08000004 读第二个字 → 跳转执行 Reset_Handler → 程序开始跑
3、之后发生中断时，CPU 根据中断号去这张表里查对应的处理函数地址，然后跳过去执行
所以整个启动链路就是：
上电 → CPU读Flash[0x00]设栈顶 → 读Flash[0x04]跳Reset_Handler → 调 SystemInit()（时钟配置）→ 拷.data → 清.bss → 调main() → 程序正式运行
SystemInit()为什么要在搬运数据之前做？
原因：时钟速度影响后面所有操作的效率。
上电时 STM32 默认用内部 HSI 8MHz 跑
SystemInit 把时钟拉到 72MHz
之后拷 .data、清 .bss、跑 main 都是 72MHz 速度执行，快了 9 倍
如果放在 main() 之后才配时钟，前面所有初始化代码都在 8MHz 下慢悠悠跑。

为什么 SystemInit 可以放在 .data/.bss 之前
你可能会疑惑：.data 还没拷贝，全局变量还没初始化，SystemInit 能用吗？
能，因为 SystemInit 只操作硬件寄存器，不依赖全局变量：

中断向量表本质就是一个函数指针数组，固定烧在 Flash 开头，CPU 硬件约定从这个地址读取启动信息。

中断向量表在 Flash 起始地址：0x08000000
答：严格来说，不完全准确。需要分两层理解：
CPU 实际从哪里读
ARM Cortex-M 内核上电后，硬件固定从 0x00000000 开始读取向量表，不是 0x08000000。
CPU 硬件行为（不可改变）：
读 0x00000000 → 栈顶初值
读 0x00000004 → Reset_Handler 地址
那 0x08000000 是怎么回事
STM32 在 CPU 和 Flash 之间做了一层地址映射：

BOOT 引脚决定 0x00000000 映射到哪里：
所以完整的说法
中断向量表烧录在 Flash 起始地址 0x08000000， 但 CPU 上电是从 0x00000000 读取的， 通过 BOOT 引脚配置，STM32 把 0x08000000 映射到了 0x00000000




启动文件（Startup File）
作用：MCU 上电后第一段执行的代码，负责初始化 CPU 到一个能跑 C 语言的状态。
MCU 上电/复位
│
▼
① 设置栈指针 (SP)
│
▼
② 调用 SystemInit()（配置时钟） ← 调到前面来
│
▼
③ 将 .data 段从 Flash 拷贝到 RAM
│
▼
④ 将 .bss 段清零
│
▼
⑤ 跳转到 main()
具体职责：
1、设置初始栈指针
2、定义中断向量表（所有中断处理函数的入口地址）
3、调用 SystemInit()（配置时钟）
4、初始化 .data 段（从 Flash 拷贝初始化值到 RAM）
5、清零 .bss 段
6、main()

__main 是 ARM 编译器（ARMCC / Keil） 特有的，我前面用的是 GCC 的流程，所以没出现它。
两套工具链的启动流程对比：
GCC 工具链（我前面写的）
上电 → Reset_Handler（你自己写的启动代码）
├── 手动拷贝 .data（用 _sidata/_sdata/_edata）
├── 手动清零 .bss（用 _sbss/_ebss）
└── bl main ← 直接跳 main()
一切都是你自己在 startup.s 里手写的，没有中间商。
ARMCC 工具链（Keil MDK）
上电 → Reset_Handler（你写的，但很短）
└── bl __main ← 跳到 ARM C 库提供的 __main
├── 拷贝 .data（__main 帮你做）
├── 清零 .bss （__main 帮你做）
├── 初始化 C 库（stdio、堆等）
└── bl main() ← 最后才调你的 main
Keil 的 startup.s 里 Reset_Handler 通常就这么几行：
Reset_Handler 	PROC
EXPORT 	Reset_Handler
IMPORT 	__main ; 声明外部符号，由 ARM C 库提供
IMPORT 	SystemInit ; CMSIS 标准的时钟初始化
LDR R0, =SystemInit
BLX R0 ; 先初始化时钟（配 PLL、Flash 等待周期）
LDR R0, =__main
BX R0 ; 跳到 __main，剩下的事它全包了
ENDP

常见文件名：startup_xxx.s（汇编）或 startup_xxx.c

两者的关系
链接文件 (ld)                    启动文件 (startup.s)
─────────────                   ──────────────────
定义"在哪里放"          ←→       执行"怎么初始化"

例如：                           例如：
_etext = Flash中data的源地址     从 _etext 拷贝到 _sdata
_sdata = RAM中data的目标起始     循环拷贝直到 _edata
_edata = RAM中data的目标结束     从 _sbss 到 _ebss 全部写0
_sbss  = BSS段起始
_ebss  = BSS段结束
链接文件定义了这些符号的地址，启动文件使用这些符号来完成初始化。两者必须配合。

每个 MCU 都有吗？
是的，每个 MCU 项目都需要这两个文件，但形式可能不同：
你的 ATBM6441 项目（RISC-V）这两个文件在：
链接文件：通常在 toolchains/ 或项目根目录，后缀 .ld
启动文件：通常在 src/ 或 startup/ 目录，后缀 .s 或 .S
MCU上某个任务如何去调整它的执行周期，让它能更快响应？
答：
1. RTOS 延时型（最常见）
2. 软件定时器型
3. 硬件定时器型
4. 事件驱动型（最快响应）



CFLAGS = C language FLAGS（C 语言编译标志）
就是传给 C 编译器的参数列表，告诉编译器"怎么编译"。
在 Makefile 里的用法
# 定义 CFLAGS
CFLAGS = -Os -g -Wall -I./include

# 编译时自动带上这些参数
gcc $(CFLAGS) -c main.c -o main.o
等价于手动敲：
gcc -Os -g -Wall -I./include -c main.c -o main.o

常见的 CFLAGS 参数
CFLAGS = -Os # 优化体积 
CFLAGS += -g # 生成调试信息（给 GDB 用） 
CFLAGS += -Wall # 开启所有常见警告 
CFLAGS += -Werror # 把警告当错误，必须修掉 
CFLAGS += -I./include # 头文件搜索路径 
CFLAGS += -DDEBUG # 定义宏 DEBUG（等价于代码里 #define DEBUG） 
CFLAGS += -std=c99 # 使用 C99 标准 
CFLAGS += -ffunction-sections # 每个函数单独段（配合 gc-sections 省 Flash） 
CFLAGS += -fdata-sections # 每个变量单独段

MCU上临界区在文件中除了中断服务函数使用外，其它地方可以使用它吗？
可以，而且临界区主要就是在普通任务代码里用的，不是在中断里用的。
临界区的本质
taskENTER_CRITICAL(); // 关中断（或屏蔽优先级低于阈值的中断） 
// ... 这段代码不会被中断打断，也不会被其他任务抢占 ... 
taskEXIT_CRITICAL(); // 恢复中断
1、任务代码中（最常用）
void task_a(void *param) 
{ 
while (1) { 
taskENTER_CRITICAL(); 
// 操作共享变量，不担心被 task_b 或中断打断 
shared_counter++; 
shared_buffer[index] = data; 
taskEXIT_CRITICAL(); 
vTaskDelay(pdMS_TO_TICKS(100)); 
} 
}
2、普通函数中
// 驱动层函数，可能被多个任务调用 
int uart_send(uint8_t *data, int len) 
{ 
taskENTER_CRITICAL(); 
for (int i = 0; i < len; i++) { 
UART->DR = data[i]; 
while (!(UART->SR & TX_EMPTY)); 
} 
taskEXIT_CRITICAL(); 
return len; 
}
3、中断服务函数中（要用专用版本）
// ⚠️ 中断里不能用 taskENTER_CRITICAL() 
// 必须用 FROM_ISR 版本 
void UART_IRQHandler(void) 
{ 
UBaseType_t saved = taskENTER_CRITICAL_FROM_ISR(); 
// 操作共享资源 
ring_buffer_push(UART->DR); 
taskEXIT_CRITICAL_FROM_ISR(saved); 
}


MCU的flash资源比较紧张，如何在软件层面去节省flash资源？
1. 编译器优化
最直接，改一个选项就能省几 KB：
# GCC 优化等级
-Os # 优化代码体积（推荐，通常比 -O2 小 10~30%）
-Oz # 更激进的体积优化（部分 GCC 版本支持）
# 对比
-O0 → 最大（无优化，调试用）
-O2 → 优化速度，体积中等
-Os → 优化体积 ← 量产用这个
2. 去掉未使用的函数和变量
# 编译时：每个函数/变量单独放一个 section 
CFLAGS += -ffunction-sections # 每个函数一个段 
CFLAGS += -fdata-sections # 每个变量一个段 
# 链接时：删掉没有被引用的段 
LDFLAGS += --gc-sections # 垃圾回收未引用的段
效果：没被调用的函数、没被引用的全局变量，链接时直接丢弃，不占 Flash。
// 假设这个函数没人调用 
void debug_dump_all(void) { // 100 字节 
// ... 
} 
// 加了 gc-sections 后，这 100 字节不会烧进 Flash
3. 减少 .data 段，多用 .bss
4. 字符串和 printf 是大户
5. 用 const 让编译器优化
这条虽然不直接省 Flash，但能省 RAM，间接让你有更多空间腾挪。
6. 数据类型别浪费
// ❌ 随手用 int（4 字节） 
int battery_level = 85; // 值范围 0~100，用 4 字节浪费 
int sensor_id = 3; // 值范围 0~10 
// ✅ 按实际范围选类型 
uint8_t battery_level = 85; // 1 字节够了 
uint8_t sensor_id = 3; // 1 字节够了
结构体也注意对齐浪费：
7. 避免重复代码
8. 链接时优化（LTO）
CFLAGS += -flto # Link-Time Optimization 
LDFLAGS += -flto 
# 效果：编译器在链接阶段跨文件优化 
# - 跨文件内联小函数 
# - 跨文件消除重复代码 
# - 通常额外省 5~15%



1、GDB支持打断点吗？
调试类型	核心特征	代表工具 / 方式
在线调试（实时）	调试器与目标程序实时连接，可暂停 / 恢复执行、实时查看 / 修改内存 / 寄存器 / 变量	GDB、LLDB、IDE 集成的调试器
离线调试	不实时连接程序，通过日志、核心转储（core dump）、打印信息事后分析	printf / 日志、gdb 分析 core 文件
（1）动态控制程序执行
a、通过断点（b）暂停程序，单步执行（n/s）、逐过程执行，实时观察每一步的变量 / 内存变化；
b、支持 “修改变量继续执行”（如set var i=10），快速验证问题原因，无需重新编译运行。
（2）实时监控程序状态
在线查看寄存器（info registers）、内存（x/10xw 0x12345678）、栈调用（bt）、线程 / 进程状态；
1.调试本地启动的程序（最基础）

·  GDB 并非只用于 Linux 应用程序：MCU 端也能用（配合 OpenOCD/JLink），只是不是 “原生运行”，而是通过远程调试实现；
场景	GDB 运行位置	调试对象	核心依赖
Linux 应用程序	Linux 主机	本地进程	程序带 - g 调试信息
MCU 固件	PC 机	远程 MCU	OpenOCD/JLink + SWD/JTAG

·  Linux 内核 / 驱动调试：必须用 KGDB（GDB 的内核调试扩展），普通 GDB 无法直接调试内核；
·  GDB 是分析应用程序 crash / 跑飞的核心工具：不管是实时调试还是离线分析 core 文件，都是最高效的手段。
不管是 Linux 应用还是嵌入式 Linux 应用，crash（如段错误Segmentation fault）、跑飞（如死循环、野指针），GDB 都是最高效的分析手段，分两种场景：
1. 实时调试（程序还能启动 / 运行）
适用于可复现的 crash / 跑飞问题，直接用 GDB 启动程序，实时捕捉错误：

2. 离线分析（程序已崩溃，生成 core 文件）
适用于线上程序 crash（无法实时调试），通过core dump文件离线分析：
步骤 1：开启 core 文件生成

步骤 2：程序 crash 后生成 core 文件，用 GDB 分析

常见 crash 场景的 GDB 分析示例
段错误（Segmentation fault）：bt看调用栈，p看访问的指针是否为 NULL / 越界；
死循环 / 跑飞：info threads看线程状态，x $pc看 PC 寄存器指向的非法地址，定位跑飞的代码段；
内存越界 / 踩踏：用watch观察变量，当变量被意外修改时暂停，定位修改源。

2、MCU从上电加载跑到main的过程；
（1）初始化堆栈指针SP=_initial_sp
（2）初始化PC 指针=Reset_Handler
（3）初始化中断向量表
（4）配置系统时钟
（5）调用C 库函数_main 初始化用户堆栈，从而最终调用main 函数去到C 的世界
你的流程步骤	是否匹配	修正 / 补充说明
1. 初始化堆栈指针 SP=_initial_sp	✅ 完全匹配	这是 MCU 上电后硬件自动完成的第一步，_initial_sp是链接脚本定义的栈顶地址，对应中断向量表第一个值
2. 初始化 PC 指针 = Reset_Handler	✅ 完全匹配	硬件复位后 PC 自动指向中断向量表的复位中断入口（Reset_Handler），是启动代码的入口
3. SystemInit函数	✅ 	不是 “单独步骤”：① 中断向量表是固化在 Flash 起始地址的，无需 “初始化”，仅需确认映射（如 STM32 可通过 SCB->VTOR 寄存器重映射）；② 该动作通常在 “配置系统时钟” 前（SystemInit 函数中），而非独立步骤
4. 配置系统时钟	✅ 完全匹配	对应标准流程的 SystemInit 函数，是启动代码中关键的硬件初始化步骤
5. 调用 C 库函数_main 初始化用户堆栈，最终调用 main	✅ 核心匹配，但表述不精准	不是 “_main”，而是__libc_init_array（ARM 架构）/_start（其他架构）；核心动作是初始化 C 运行时（.data/.bss/ 堆），而非仅 “初始化堆栈”（堆栈已在步骤 1 初始化）
3、bootloader的起始地址是固定的吧；
硬件层面：bootloader 的 “默认起始地址是固定的”
MCU 上电后，硬件逻辑决定了必须从固定地址取第一条指令，这是 bootloader “默认起始地址固定” 的核心原因：
1. 核心硬件规则（以 Cortex-M 系列为例）
MCU 上电复位后，程序计数器（PC）会自动指向 0x00000000（ARM 架构默认），而这个地址会映射到「片内 Flash 的起始地址」（如 STM32F103 是 0x08000000，STM32F407 是 0x08000000）；
中断向量表必须放在这个 “默认起始地址”（第一行是栈顶地址，第二行是复位中断入口），而 bootloader 的代码（包括中断向量表）必须覆盖这个地址，否则 MCU 上电后无法执行 bootloader。
2. 举例（STM32F103）
硬件默认：上电后执行 0x08000000 地址的代码；
因此 bootloader 的「默认起始地址必须是 0x08000000」，否则 MCU 上电后找不到 bootloader，直接执行不到。
关键：这个 “固定地址” 是 MCU 硬件设计决定的，无法通过代码修改，是 bootloader 的 “硬性约束”。
二、工程层面：bootloader 的起始地址 “可灵活配置”
虽然硬件默认地址固定，但通过「内存重映射」「代码跳转」「链接脚本配置」，可以让 bootloader 放在非默认地址，核心有 2 种场景：
场景 1：单芯片同时放 bootloader + 应用程序（最常用）
此时需要把 Flash 划分成两个区域，bootloader 占 “默认起始地址”，应用程序占后续地址：
区域	起始地址	大小	作用
Bootloader	0x08000000	64KB（示例）	固定在默认地址，上电先执行
应用程序	0x08010000	剩余 Flash	由 bootloader 跳转执行
核心配置（工程层面）
bootloader 的链接脚本（.ld）：明确指定起始地址为 0x08000000：

应用程序的链接脚本：指定起始地址为 0x08010000，并修改中断向量表偏移（避免和 bootloader 冲突）：

场景 2：通过硬件 / 软件重映射，让 bootloader 放在非默认地址
部分高端 MCU 支持「内存重映射」，可修改 “0x00000000” 映射的物理地址，从而让 bootloader 放在非默认地址：
硬件重映射：通过 BOOT 引脚配置（如 STM32 的 BOOT0/BOOT1）：
BOOT0=0：0x00000000 映射到片内 Flash（0x08000000）→ 默认；
BOOT0=1：0x00000000 映射到 SRAM（0x20000000）→ 可把 bootloader 放在 SRAM 执行；
软件重映射：通过寄存器修改映射关系（如 STM32 的 SYSCFG->MEMRMP）：


4、MCU上电后到底是先执行bootloader还是启动代码？
MCU 上电后本质先执行 “启动代码” —— 如果芯片中烧录了 bootloader，那么 bootloader 内部包含它自己的启动代码，是上电后第一个执行的程序；如果没有 bootloader，执行的就是应用程序的启动代码。

场景 2：芯片中烧录了 bootloader + 应用程序（工程常用）
这是实际项目中最常见的场景，核心是 “bootloader 先运行，再跳转到应用程序”：
MCU上电 → 硬件复位 → 执行bootloader的启动代码 → bootloader的main → 校验/升级应用程序 → 跳转到应用程序 → 执行应用程序的启动代码 → 应用程序main
分步骤拆解（关键细节）：
第一步：bootloader 的启动代码先执行bootloader 本质是 “一段独立的程序”，它也有自己的启动代码（汇编）、SystemInit、C 运行时初始化，甚至自己的 main 函数：
硬件上电后，PC 指针指向 Flash 默认地址（0x08000000），这里存放的是 bootloader 的中断向量表；
先执行 bootloader 的启动代码（初始化栈、时钟、C 运行时），再进入 bootloader 的 main 函数。
第二步：bootloader 完成核心工作bootloader 的 main 函数中会执行它的核心逻辑：
校验应用程序的完整性（如 CRC 校验）；
检测是否需要 OTA 升级（如有新固件则烧录）；
确认应用程序合法后，执行 “跳转指令”。
第三步：跳转到应用程序，执行应用程序的启动代码
bootloader 跳转时，会重新配置栈指针（MSP）为应用程序的栈顶地址，然后跳转到应用程序的复位中断入口：
此时 MCU 开始执行应用程序的启动代码（而非直接进入应用程序 main）；
应用程序的启动代码重复 “初始化栈、时钟、C 运行时” 的流程，最后进入应用程序 main。

4、Linux应用程序虚拟内存到内核实际物理内存是怎么映射的；
Linux 应用程序的虚拟内存不会直接映射到物理内存，而是通过「MMU（内存管理单元）+ 页表 + 内核地址空间映射」三层机制实现 —— 应用层操作的虚拟地址，先经 MMU 查页表转成物理地址，内核则通过 “页表隔离 + 内核共享页表” 的方式，管理所有进程的虚拟 - 物理映射，且用户态虚拟地址和内核态虚拟地址有严格的地址空间划分。
一、先理清核心架构：用户态 / 内核态地址空间隔离
Linux 为每个进程分配独立的 4GB 虚拟地址空间（32 位系统），且严格划分两部分，这是映射的基础：
地址范围（32 位）	所属空间	核心特点
0x00000000 ~ 0xBFFFFFFF（3GB）	用户态	每个进程独立，互不干扰；仅当前进程运行时可见
0xC0000000 ~ 0xFFFFFFFF（1GB）	内核态	所有进程共享同一段内核虚拟地址空间；映射到物理内存的内核区域
“Linux 应用程序的虚拟内存到物理内存的映射，核心是 MMU + 页表 + 内核管理的三层机制：
每个进程有独立的 4GB 虚拟地址空间，3GB 用户态 + 1GB 内核态，内核态是所有进程共享的；
内核为每个进程维护页表，页表记录‘虚拟页→物理页框’的映射关系；
应用程序访问虚拟地址时，CPU 的 MMU 硬件自动查页表，把虚拟地址转成物理地址；
若虚拟地址未映射（缺页），会触发内核的缺页中断，内核分配物理内存并更新页表；
内核态虚拟地址映射到同一段内核物理内存，所有进程共用，保证系统调用时能访问内核资源。关键是应用程序不能直接操作物理地址，全程由 MMU 和内核页表完成转换，且用户态和内核态地址严格隔离。”

5、程序调用某一个函数时是怎么执行的？
“程序调用函数的核心是栈帧切换和现场保存，分 7 步：
（1）调用方按约定把参数传给寄存器 / 栈；
（2）CPU 保存返回地址（ARM 存 LR，x86 压栈），跳转到函数入口；
（3）被调用函数创建栈帧：保存调用方的栈指针，分配栈空间存局部变量；
（4）执行函数逻辑，计算结果后将返回值存入指定寄存器；
（5）被调用函数销毁栈帧，恢复调用方的栈指针；
（6）执行返回指令，回到调用方的下一行代码；
（7）调用方读取返回值，继续执行。核心是栈帧管理 —— 每个函数有独立的栈帧，保证函数执行的隔离性，执行完后销毁栈帧恢复现场。

6、广播机制还需再学习深入；
7、进行OTA升级时，除了设置APP的地址，接收上层发来的固件包后，还需要做什么？
“OTA 升级接收固件包后，核心要做 6 步，核心是保证固件合法、烧录正确：
（1）固件校验：校验 CRC/SHA256、签名、版本、兼容性，确保固件没被篡改且适配设备；
（2）擦除 Flash：擦除 APP 分区的 Flash（Flash 先擦后写）；
（3）烧录固件：按 Flash 写入规则将固件写入 APP 分区，边写边校验；
（4）二次验证：烧录完成后重新校验整个固件，确保写入无错误；
（5）设置标记：在 Flash 标记区写入‘升级成功’标记，供 Bootloader 识别；
（6）重启设备：触发软复位，Bootloader 读取标记后跳转到新 APP 运行。关键还要处理异常，比如升级中断、烧录失败时保留旧固件，防止设备变砖，同时上报升级状态。”

8、C语言的字节对齐规则；
“C 语言字节对齐核心是‘自然对齐 + 结构体补齐’：
基础类型有自然对齐数（如 char=1、int=4），变量起始地址必须是对齐数的整数倍；
结构体对齐分两步：① 每个成员偏移量是自身对齐数的整数倍（不足填充）；② 总大小是最大成员对齐数的整数倍（不足补齐）；
可通过 #pragma pack (n) 手动修改对齐规则（n=1 时无填充），适合网络通信 / 内存优化场景；对齐的目的是提升 CPU 访问效率，嵌入式中 DMA / 寄存器操作必须保证对齐，否则会出错。”

9、DMA传输与cache一致性的问题；
DMA 传输与 Cache 一致性问题的核心原因是：CPU 通过 Cache 访问内存，DMA 直接访问物理内存，二者路径不同导致数据不同步，主要出现在带 Cache 的内核（如 Cortex-M7）。
解决方法分两类：
（1）手动操作 Cache：CPU 写数据后刷新 Cache（同步到物理内存）再启动 DMA；DMA 写数据完成后，失效 Cache（丢弃旧数据）再让 CPU 读取；
（2）硬件层面：将 DMA 缓冲区设为非 Cacheable，或用硬件一致性 DMA 控制器（高端 MCU）。低端 MCU（如 STM32F4）无 Cache，因此不存在这个问题；核心原则是让 Cache 和物理内存的数据同步，保证 DMA 和 CPU 拿到的是同一份数据。”

9、crash问题如何定位；
·  连接调试器到 MCU，在 IDE 中开启 “硬件断点” 或 “实时调试”；
·  复现 crash，IDE 会暂停在 crash 指令处，查看：
寄存器：PC（程序计数器）→ crash 的指令地址；LR（返回地址）→ 调用方函数；SP（栈指针）→ 栈状态；
栈回溯：查看栈中的函数调用链（从 crash 函数倒推到 main）；
内存：检查 crash 处访问的内存地址（如空指针 0x00000000）。
crash 类型	典型现象	定位关键点
空指针解引用	HardFault / 段错误，PC 指向内存访问指令	查看 crash 指令的操作数（如 R1=0x0），找指针赋值处
栈溢出	RTOS 任务挂死 / 裸机复位	查看栈剩余空间（FreeRTOS）、增大栈大小验证
数组越界	内存数据错乱 / 段错误	valgrind 检测、查看数组索引是否超出范围
死锁（RTOS/Linux）	进程 / 任务卡死，CPU 占用低	查看互斥锁持有顺序、任务状态
内存泄漏（Linux）	内存占用持续升高，最终 OOM	valgrind 检测、查看 malloc/free 是否配对
硬件异常（裸机）	HardFault，外设无响应	查看外设寄存器状态、DMA / 中断配置

PendSV 不是 FreeRTOS 特有的
PendSV 是 ARM Cortex-M 内核的硬件特性，跟 FreeRTOS 没有绑定关系。
1️⃣ PendSV 的本质
PendSV（Pendable Service Call）是 ARM Cortex-M 架构定义的 系统异常之一，异常编号为 14：
这些都是写在 ARM 架构手册 里的，跟你用不用 RTOS 没关系，只要是 Cortex-M 内核的芯片，这些异常就存在。
2️⃣ 为什么 FreeRTOS 选择用 PendSV？
PendSV 有一个关键特性：可以被"挂起"触发。
普通中断：硬件事件 → 立刻触发
PendSV：软件写寄存器 → 延迟到没有其它中断时再执行
流程：
SysTick 中断触发
→ 发现需要切换任务
→ 设置 PendSV 挂起位（不是立刻切换！）
→ SysTick 退出
→ 如果没有其它中断在处理
→ PendSV 执行，完成任务切换
这样做的好处是：任务切换永远不会打断正在处理的中断，避免了中断嵌套中切换任务的复杂性。



5、注册机制请你介绍一下：
一、先明确：嵌入式中 “注册机制” 的核心定义
注册机制（也叫 “回调注册 / 注册 - 通知模式”）：
把业务逻辑 / 处理函数通过 “注册接口” 提前 “登记” 到核心模块中，当特定事件触发时，核心模块自动调用已注册的函数，实现 “事件触发 - 逻辑执行” 的解耦。
核心三要素：
要素	作用
注册接口	提供给业务模块的 “登记入口”（如 xxx_register_cb()），保存回调函数 + 上下文
回调函数	业务模块实现的具体逻辑（如广播接收回调、中断处理回调）
触发逻辑	核心模块在事件发生时（如收到广播、外设中断），遍历并调用已注册的回调函数
解耦：核心模块与业务模块无硬编码依赖，新增业务逻辑仅需注册回调，无需修改核心框架；
可扩展：支持动态注册 / 注销，适配多业务、多设备的扩展需求（如新增广播订阅者、新增传感器）；
可维护：回调函数由业务模块实现，核心框架专注于 “事件分发”，代码职责清晰；
线程安全：通过关中断 / 互斥锁保护注册表，适配 RTOS 多任务并发场景；
灵活度高：可通过上下文传递自定义参数，适配不同业务的个性化需求。

4、线程之间常用的同步机制有哪些？
FreeRTOS 中任务同步的核心是「保证多任务按预期顺序执行、避免资源竞争」，常用机制有信号量、互斥锁、事件组、消息队列、任务通知 5 类。
（1）二进制信号量（Binary Semaphore）—— 最基础的 “任务同步”
核心定义：
信号量本质是一个 “计数器”，二进制信号量计数器只有 0 和 1 两种状态（空 / 满），核心用于「任务与任务 / 任务与中断间的同步」（不是用于保护共享资源）。
核心作用：
触发式同步：比如 “串口接收数据后，触发处理任务执行”；
中断与任务同步：中断中释放信号量，任务中等待信号量（替代轮询，降低 CPU 占用）。
（2）互斥锁（Mutex）—— 保护共享资源的 “专属工具”
核心定义：
互斥锁是特殊的二进制信号量，支持「优先级继承」（解决优先级反转问题），核心用于「保护共享资源」（如全局变量、外设、硬件寄存器），防止多任务同时访问导致数据错乱。
核心作用：
独占式访问：同一时间只有一个任务能持有互斥锁，访问共享资源；
优先级继承：低优先级任务持有锁时，高优先级任务等待，会临时提升低优先级任务的优先级，避免优先级反转。
适用场景：
多任务访问全局变量（如uint32_t g_count）；
多任务操作同一外设（如 SPI 显示屏、I2C 传感器）。
（3）事件组（Event Group）—— 多条件同步的 “高效工具”
核心定义：
事件组是一组二进制位（最多 24 位），每个位代表一个 “事件”，任务可以等待「单个事件」或「多个事件（与 / 或）」，核心用于「多任务 / 多条件同步」。
核心作用：
多条件触发：比如 “任务等待‘按键按下’且‘串口数据接收完成’两个事件后执行”；
一对多通知：一个事件可唤醒多个等待的任务。
适用场景：
任务需要满足多个条件后执行（如 “传感器数据就绪 + 网络连接成功”）；
多个任务等待同一个事件（如 “系统初始化完成”）
（4）消息队列（Queue）—— 带数据传递的 “同步工具”
核心定义：
队列是 FIFO（先进先出）的缓冲区，任务 / 中断可向队列发送数据，其他任务可从队列接收数据，核心用于「任务间同步 + 数据传递」（区别于无数据的信号量）。
核心作用：
同步 + 数据：不仅通知任务执行，还传递具体数据（如串口接收的字节、传感器数值）；
异步通信：发送方和接收方无需同时就绪，数据暂存队列中。
（5）任务通知（Task Notification）—— 轻量级的 “同步替代方案”
核心定义：
任务通知是 FreeRTOS 优化后的轻量级同步机制，直接向任务发送 “通知值”，无需创建额外的信号量 / 队列对象，核心用于「替代简单的信号量 / 队列」，减少内存占用。
核心作用：
替代二进制信号量：任务通知的 “通知计数” 可模拟信号量；
替代队列（单数据）：通知值可传递单个整数 / 指针数据；
更高效：无需创建对象，减少内存开销和 API 调用耗时。
适用场景：
简单的任务同步（替代二进制信号量）；
传递单个数据（替代长度为 1 的队列）。
同步需求	推荐机制	避坑点
中断唤醒任务	二进制信号量	中断中必须用 FromISR API
保护共享资源（全局变量 / 外设）	互斥锁	避免在中断中使用，防止死锁
多条件同步（与 / 或）	事件组	事件位最多 24 位，按需设置
同步 + 数据传递	消息队列	队列长度要适配数据量，避免溢出
轻量级同步（简单场景）	任务通知	仅支持一对一，无法一对多

4、为什么选 RTOS 开发而非裸机？
裸机（前后台）和 RTOS 的核心差异是「任务调度方式」，选择 RTOS 的本质是解决裸机在复杂场景下的 “实时性差、代码维护难、资源竞争失控” 问题，先看核心对比：
维度	裸机（前后台）	RTOS（如 FreeRTOS）
任务管理	轮询执行（大循环 + 中断）	抢占式调度（高优先级任务优先）
实时性	差（低优先级任务阻塞高优先级）	高（毫秒级响应，满足工业 / 医疗要求）
代码复杂度	简单（适合单功能）	中等（但模块化，易维护）
资源竞争	手动处理（易出错）	同步机制（信号量 / 互斥锁）保护
开发效率	快（小项目）	快（复杂项目，模块化复用）
内存占用	极低（几 KB RAM）	稍高（内核 + 任务栈，几十 KB RAM）
1. 选 RTOS 的核心场景（裸机搞不定 / 搞不好的情况）
场景 1：多任务需要 “实时响应”
裸机是 “大循环 + 中断”：比如循环里有「传感器采集（100ms）+ 显示屏刷新（500ms）+ 串口处理（实时）」，若显示屏刷新耗时 10ms，串口数据会延迟 10ms 处理，实时性差；
RTOS 下：串口处理任务设为高优先级，一旦有串口中断，立即抢占显示屏任务执行，保证实时响应（比如工业控制中 “紧急停止信号” 必须毫秒级响应）。
场景 2：多任务共享资源（避免数据错乱）
裸机中多中断 / 多函数访问全局变量，需手动加 “关中断 / 开中断”，容易漏加导致数据错乱；
RTOS 中用「互斥锁」保护共享资源，同一时间只有一个任务访问，无需手动关中断，代码更健壮。
场景 3：复杂项目（代码模块化 / 易维护）
裸机的大循环会越写越乱（“面条代码”），新增功能要改核心循环；
RTOS 将功能拆分为独立任务（如传感器任务、显示任务、网络任务），每个任务独立开发 / 调试，新增功能只需加新任务，维护成本低。
场景 4：低功耗设计（精准控制 CPU 休眠）
裸机需手动管理休眠 / 唤醒，容易遗漏；RTOS 可通过「任务阻塞」让 CPU 自动休眠（无就绪任务时），有任务就绪时自动唤醒，功耗控制更精准（比如可穿戴设备续航提升 30%）。

1. PV 操作的本质
P 操作（Wait/Take）：申请资源，信号量计数器 - 1；若计数器≤0，则任务阻塞，等待资源释放；
V 操作（Give/Post）：释放资源，信号量计数器 + 1；若有任务阻塞，则唤醒一个等待的任务。

1. 死锁的核心定义
死锁是多个任务循环等待对方持有的资源，导致所有任务都无法继续执行，比如：
任务 A 持有锁 1，等待锁 2；
任务 B 持有锁 2，等待锁 1；
双方都不释放已持有的锁，永久阻塞。
2. 死锁的解决方法（按优先级排序）
方法 1：统一资源获取顺序（最常用，打破 “循环等待”）
所有任务按「固定顺序」获取锁（如先锁 1，后锁 2），彻底避免循环等待：
方法 2：设置超时时间（打破 “请求与保持”）
获取锁时不永久等待，超时则释放已持有的锁，重新尝试：
方法 3：一次性获取所有资源（打破 “请求与保持”）
任务在执行前，一次性申请所有需要的锁，要么都拿到，要么都不拿：
方法 4：使用可剥夺资源（打破 “不可剥夺”）
FreeRTOS 的互斥锁支持「优先级继承」，但无法强制剥夺；高端 RTOS（如 VxWorks）支持 “资源剥夺”，高优先级任务可剥夺低优先级任务的锁（嵌入式中少用）。
方法 5：减少资源持有时间（辅助）
任务拿到锁后，快速执行核心逻辑，立即释放锁，降低死锁概率（不解决根本问题，但减少触发概率）。

4、extern的核心作用是声明 “变量 / 函数” 在其他文件中定义，实现跨文件访问，先明确基础用法：
声明变量：extern uint32_t g_count;（告诉编译器：g_count 在其他.c 文件中定义）；
声明函数：extern void func(void);（函数默认 extern，可省略）。
（1） extern 的核心优点（工程价值）
优点 1：实现代码模块化（嵌入式工程必备）
将代码按功能拆分到多个.c 文件，通过 extern 共享全局变量 / 函数，避免所有代码写在一个文件（“面条代码”）：
优点 2：减少重复定义，节省内存
全局变量 / 函数只需在一个文件中定义，其他文件用 extern 声明即可，避免重复定义（否则编译器报 “multiple definition” 错误）。
优点 3：灵活的跨模块通信
嵌入式中常通过 extern 实现 “驱动层→应用层” 的数据传递（如驱动层采集的传感器数据，应用层通过 extern 访问）。
（2）extern 的核心缺点（实战避坑）
缺点 1：全局变量滥用，导致代码耦合度高
过多使用 extern 共享全局变量，会让模块间依赖严重（如修改 file1.c 的 g_sys_status 类型，所有 extern 声明的文件都要改），后期维护困难。
缺点 2：数据安全风险（无访问控制）
extern 声明的变量可被任意文件修改，容易因误操作导致数据错乱（如多个任务同时修改 extern 全局变量，未加互斥锁）：
缺点 3：编译错误难定位
若 extern 声明的变量 / 函数 “只声明未定义”（如漏写 file1.c 的 g_sys_status 定义），编译器不报错，链接时才报 “undefined reference”，增加调试成本。
缺点 4：内存占用问题
extern 声明的是全局变量（存 RAM），若滥用会导致 RAM 占用过高（STM32F103C8T6 仅 20KB RAM，全局变量过多易溢出）。

4、系统属性组件：
属性组件是一个线程安全的键值对存储系统，参考 Android 系统属性设计，提供系统级的数据共享和配置管理功能。
  核心特性：
  - ✅ 线程安全（基于互斥锁）
  - ✅ 支持多种数据类型（INT32、字节数组、字符串）
  - ✅ 支持持久化存储（内存/用户区/工厂区）
  - ✅ 支持属性变化触发器（同步/异步回调）
  - ✅ 动态内存管理（短数据/长数据智能切换）
内存管理策略:
  属性组件采用智能内存管理策略：
  #define PROP_PAYLOAD_LEN  (12)   // 短数据阈值
  // 短数据 (≤12字节): 使用内置数组，无额外分配
  // 长数据 (>12字节): 使用动态分配内存
  存储策略：
  属性节点创建
      │
      ├─→ 数据 ≤ 12字节 ──→ 使用 node->val.payload[12]
      │                    (无动态分配，节省内存)
      │
      └─→ 数据 > 12字节 ──→ 使用 node->val.pkg.pdata
                           (动态分配，malloc/free管理)

系统属性组件主要分为以下几个功能模块：
（1）属性管理模块：负责属性的增删改查等基本操作
（2）持久化存储模块：负责属性数据的存储与恢复
（3）回调通知模块：负责属性变化的通知机制
（4）命令行接口模块：提供Shell命令用于调试和管理

4、eFuse 是什么？
eFuse = electronic fuse，电子熔丝，是 MCU/SoC 内部一段一次性可编程（OTP）的非易失性存储单元。
嵌入式里主要用来做什么？
1、芯片唯一 ID / 序列号出厂固化，用来做设备身份识别、加密绑定。
2、密钥存储（安全启动、加密）存 AES 密钥、加密秘钥，防止固件被破解、盗版。
3、版本 / 配置信息比如硬件版本、校准参数、功能使能位

5、STM32最小系统电路：
（1）电源电路，24V转5V，LM2596，5V转3.3V，LM1117；
（2）复位电路：
高电平复位电路：RST高电平有效
实际上，上电复位电路本质上就是RC串联充电电路，因此我们借助RC电路的工作原理来分析。单片机上电，由于电容两端电压不能突变，上电后的一瞬间电容等效为短路，因此RST为高电平，电位等于VCC，单片机复位。之后通过电阻R1对电容充电，与此同时电阻两端电压开始减小，根据时间常数，对于上图中的电路而言，大概需要4~5，也就是经过0.4~0.5s左右可近似认为电容充电完毕，此时电容两端电压近似为VCC，电阻两端电压近似于0V，从而RST就为低电平，近似为0V，单片机开始正常工作。
为什么充电时电压会变低？
1. 刚上电（t=0）
电容电压不能突变，VCC 刚上来 → 电容左边是 VCC，右边（RESET）瞬间也被拉到接近 VCC。
→ RESET = 高电平 → 芯片复位
2. 电容开始充电
电流方向：VCC → 电容 → RESET → 电阻 → GND
电容在充电，它两端的电压慢慢变大：
电容左边：VCC
电容右边（RESET）电压 = VCC - 电容电压
电容电压越充越高 → RESET 电压就越来越低。
3. 充满电后
电容相当于开路，没有电流。RESET 只通过电阻接 GND。
→ RESET = 0V（低电平）→ 复位结束，芯片启动。


低电平复位电路：RST低电平有效
与高电平复位电路类似，单片机上电后，由于电容两端电压不能突变的特性，上电后的一瞬间，RST端电位近似为GND，即低电平，单片机复位。之后，经过4~5的时间后，电容充电完毕，电容两端电压近似为VCC，电阻两端电压近似于0V，相当于导线，从而RST端电位近似于VCC，单片机开始正常工作。

（3）时钟电路：倍频和分频
由于内部时钟准确度不高，一般选用外部晶振，常用的有11.0592MHz，之所以采用这个数值，是为了让串口通讯波特率为2400的倍数，以便适配国际流行的各种串行通讯协议；32.768K，2的15次方正好等于32768，反过来讲，如果要把32.768K的时钟频率经过15次分频的话，得到的频率正好是1Hz，正好就是1秒钟时间，这样就可以计时了。
（4）烧录电路：SWD/JTAG烧录
SWD烧录通常需要4根线。这些线分别是VCC、GND、SWCLK和SWDIO。其中，VCC是电源线，GND是地线，SWCLK是时钟信号线，SWDIO是双向数据线。在某些情况下，SWD烧录可能只需要两根线，即SWDIO和SWCLK，但这取决于具体的硬件和烧录方式
JTAG烧录通常需要四根线，它们分别是：VDD、DIO、GND、CLK。其中，VDD和GND是电源线，DIO和CLK是信号线
（5）外围接口电路：用于将MCU引脚引出，方便后续接口调试
（6）boot启动电路：Boot0和Boot1
Boot0和Boot1是用来选择STM32单片机的启动方式的。

简单来说，大家要想正常跑程序就要把Boot0和Boot1都接到地，就是正常工作模式。
三种启动方式分别为： 从主闪存存储器、系统存储器、内置SRAM启动。所谓主闪存就是STM32 自带的片内Flash。系统储存器为STM32片内自带的ROM，该区域由STM32生产时固化，用户无权且无须改动。SRAM即STM32自带的64K片内SRAM，正常使用中SRAM一般是储存程序运行中的一些变量。 
主闪存是用户程序运行区，系统存储器是芯片出厂固化的 Bootloader 区，两者共同构成 MCU 的启动与程序存储体系。
所谓STM32的启动方式，其实质为：STM32把代码下载到什么地方的问题。KEIL编译的程序最终转换为二进制下载到STM32的存储器件（STM32只有SRAM和FLASH能放代码）。如何告知STM32内部电路应该将程序下载到何处？此时就是通过BOOT引脚告知STM32内部电路该把程序下载到何处；

启动实操
3.1 主闪存存储器启动
    这个启动方式即为FLASH启动。该方式很常见：使用ST-LINK连接开发板，然后在KEIL(MDK)设置Options for Target等，设置完毕后点击菜单栏 Download绿色箭头，然后就可以下载到FLASH了。把鼠标放在绿色箭头会出现一行解释：Download code to flash memory。该方式需要把BOOT0置0，BOOT1随意。
3.2、系统存储器启动
    在系统存储器放置着出厂设置的下载引导电路：这个电路你可以理解为一段固定作用的代码。该代码的名字就叫BootLoader，也叫自举程序。他的作用就是把通过串口发送过来的代码放到FLASH。该启动方式就是大家所熟悉的串口下载。此启动方式的流程：用户编译产生二进制文件 ->二进制文件通过串口发送到stm32 ->stm32自举程序将二进制文件写入FLASH。所以实质也是把代码下载到FLASH。
3.3、SRAM启动
一般情况下，用户代码都是放在FLASH，SRAM则用来存放一些用户定义的变量等。所以想要从SRAM启动，那么必须要使SRAM既保留其原来的功能，又要使其作为FLASH使用。这里就涉及到将SRAM分区，一部分作为ROM固定存放用户代码，一部分用作RAM存放用户变量。 SRAM分区需要用户自己写代码完成。

1. 系统存储器里的 Bootloader（厂家 Bootloader）
位置：System Memory，MCU 出厂固化
性质：ROM，只读，用户永远改不了
功能：只做一件事 ——串口 / USB 等方式下载程序到 Main Flash
特点：
厂家写死
不可擦、不可写、不可升级
只能通过 BOOT 引脚选择启动

2. 用户自己写的 Bootloader（升级用 Bootloader）
位置：主闪存 Main Flash 里单独分一个区
性质：你自己写的代码，可修改、可升级
功能：
读取外部 Flash / 串口 / 网口的新固件
把新固件写到 App 区
然后跳转到 App 运行
这才是 IAP、OTA、在线升级 里说的 Bootloader

Bootloader是一段启动程序，它在芯片启动时最先被执行，用于硬件的初始化或固件的OTA。在STM32中，bootloader程序需要通过下载器烧写到芯片中，而应用程序（APP）则可以通过有线或无线方式（如UART、IIC、USB、SPI、WiFi、Bluetooth等）通过bootloader来更新。

ISP 是依靠芯片出厂固化的 Bootloader，用于首次程序烧录；
IAP 是用户自己实现的 Bootloader，用于产品在线升级；
串口烧录只是一种通信接口，既可以用于 ISP，也可以用于 IAP，不能和前两者并列。
为什么 IAP 中 Bootloader 和 App 必须分开存放，不能重叠？
核心原因只有一句话：Bootloader 负责更新 App，如果两者在同一块 Flash 区域，更新时擦除 Flash 就会把自己也删掉，程序直接报废。
展开说：
（1）Flash 擦除是按 “页 / 扇区” 操作
写新固件前，必须先擦除 Flash。
如果 Bootloader 和 App 重叠或在同一个扇区：
擦除 App 代码时 → 连 Bootloader 一起删掉。
芯片下次启动就没程序可跑，变 “砖”。
（2）启动流程依赖分区
芯片复位后，先从固定起始地址启动（一般是 0x08000000）。
我们把这个区域放 Bootloader。
把后面一段区域放 App。
分区明确，互不干扰。
（3）升级安全
Bootloader 是 “兜底程序”，一旦损坏无法再升级。
必须放在独立、不被覆盖的区域。

4、为什么 App 必须重定向中断向量表？
（1）先讲默认情况
A.正常情况下，MCU 认为中断向量表在 Flash 起始地址：0x08000000
B.中断来的时候，硬件直接去这个地址找入口。
（2）但 IAP 里 App 不在起始位置
比如：
A.Bootloader 放在：0x08000000 ~ 0x08003FFF
B.App 放在：0x08004000 开始
这时：中断来了，硬件还去 0x08000000 找向量表，但那里是 Bootloader 的向量表，不是 App 的！
结果：
中断跑错
死机、跑飞、进不了中断
解决方法：中断向量表重定向
在 App 初始化时，告诉 MCU：
“我的中断向量表不在默认位置，请去 0x08004000 找！”
代码里就是这句（以 STM32 为例）：
运行
SCB->VTOR = 0x08004000;
一句话总结：
App 被搬到了新地址，中断向量表也要同步搬家，否则中断找不到入口，程序崩溃。

5、OTA升级完整流程（无风险）：
（1）设备正常从 APP1 运行，检测到 “升级指令 / 新固件包”；
（2）App 仅做一件事：把升级指令 / 固件包的 “起始信息”（比如固件长度、CRC）存到指定 Flash 区，然后主动复位；
（3）重启后进入 Bootloader（不再跳 APP1）；
（4）Bootloader 读取升级信息，从串口 / 网口 / Flash 接收完整新固件 → 写入 APP2；
（5）Bootloader 校验 APP2 的新固件（CRC/MD5）→ 确认完整正确；
（6）Bootloader 修改「启动标志位」→ 标记下次从 APP2 启动；
（7）Bootloader 重启设备 → 再次进入 Bootloader → 读取标志位 → 跳转到 APP2 运行；
（8）若 APP2 运行正常，Bootloader 可同步更新 APP1（可选）。

6、编码器 + 步进电机 为什么要配合使用？
答：步进电机是开环执行元件，无法自知实际位置；编码器提供实时位置反馈，两者配合构成闭环控制系统，解决丢步、失步、定位不准问题，让运动控制更精准、更可靠。

3、MPU6050：
组成：是一款六轴（三轴加速度+三轴角速度）传感器。内部主要构件：陀螺仪、加速度计、数字运动处理器DMP，MPU6050含有两个IIC接口，第一IIC接口可作为主接口给单片机传输数据；第二IIC接口用于连接一个第三方数字传感器（如外部磁力传感器等），然后通过这个IIC接口输出完整的9轴信号，否则只有6轴。
加速度计用于测量物体的加速度，陀螺仪用于测量物体的角速度，磁力计用于测量物体的磁场强度和方向。
工作原理：通过MPU6050读取加速度和角速度的原始数据，数据管理平台（DMP）将原始数据转化为四元数，进而完成欧拉角的计算。MPU6050自带数字运动处理器（DMP），通过主IIC接口，可以向CPU提供四元数，CPU可利用四元数得到欧拉角。
DMP模块：利用 MPU6050 芯片内部的 DMP 模块（Digital Motion Processor 数字运动处理器）， 可对传感器数据进行滤波、融合处理，直接通过 IIC 接口向主控器输出姿态解算后的数据，降低主控器的运算量。
四元数
四元数就是简单的超复数，由实数加上三个虚数单位 i、j和k 组成，
就是形如a + bi+ cj + dk的数，其中a、b、c 、d是实数，
i² = j² = k² = -1， iº = jº = kº = 1 。
姿态角可以说是其中一对特殊的欧拉角。导航系是惯性导航算法的基本参考系，运动物体在导航坐标系内进行位置，速度，姿态确定；载体系，捷联惯导硬件内部定义了坐标系，其原点一般位于硬件中心，而惯性元件安装在三个正交方向上形成XYZ 轴。 
在惯性导航中，陀螺仪和加速度计的信号都需要积分，而积分往往会积累误差，因此这两者的误差模型就显得更重要一些。磁强计的主要误差来源是环境中的磁场干扰
IMU误差模型：
随机性误差：一般地，MEMS IMU主要表现在角度（速度）随机游走和零偏不稳定性。即：随机游走误差表现为高斯白噪声，其积分过程近似为维纳过程；而零偏不稳定性认为是零偏bias会随着时间缓慢变化。
确定性误差：零偏和标度因数，零偏为静止放置，其输出不为0；标度因数：三轴传感器各轴灵敏度不完全一致，即各个轴的输入输出因子不为1。
卡尔曼滤波：
在卡尔曼滤波器中，任务就是估计出物体最佳的期望以及方差出来，均值代表你的最佳估计，方差代表你的不确定性。

对于随机性误差的处理方法，有以下几种常见的方式：
（1）重复测量：通过多次重复测量同一物理量，然后计算平均值来减小随机误差的影响。重复测量可以使随机误差在一定程度上互相抵消。
（2）使用大量样本：增加样本数量可以减小随机误差的影响。当样本数量足够大时，随机误差的影响将变得较小。这有助于提高数据的稳定性和可靠性。
（3）数据处理：使用统计方法对数据进行处理，如计算标准差、方差等。这些统计方法可以量化随机误差的大小，从而更好地评估测量结果的准确性。
（4）控制实验条件：尽量减少实验中的干扰因素，比如保持实验环境的稳定，控制变量等，可以减少随机误差的影响。
（5）使用统计分析方法：通过统计方法来识别和消除随机误差，比如使用均值、标准差、方差等统计指标来评估数据的稳定性。
另外，如果知道随机误差的来源，还可以采取一些针对性的措施来减小其影响，例如校正仪器、采用补偿方法等。

1、时域（时间域）（time domain）
自变量是时间,即横轴是时间,纵轴是信号的变化。其动态信号x（t）是描述信号在不同时刻取值的函数。

图1是正弦波的时域图，示出了振幅与时间的关系。在时域图中，横轴是时间，纵轴是振幅。时域图显示振幅随时间的变化，可以看出峰值振幅为5V，可以算出频率f=6 Hz。
2、频域（频率域）（frequency domain）
自变量是频率,即横轴是频率,纵轴是该频率信号的幅度,也就是通常说的频谱图。

图2是图1中正弦波的频域图，在频域图中，横轴是频率，纵轴是峰值振幅。频域图仅仅示出峰值振幅与频率，而不显示振幅随时间的变化。
从频域图可以看出，正弦波的频率为6Hz，这个6Hz的正弦波的峰值振幅为5V 。
频域图的优点是，从频域图中，可以一眼看出正弦波的频率和峰值振幅。整个正弦波在频域图上只是一个立柱立柱的位置显示了正弦波的频率，立柱的高度显示了正弦波的峰值振幅
3、时域和频域
将时域频域一起放在一个三维的坐标系中，如图所示，与时域和频域的轴表示振幅：

4、时域变换到频域的意义
动态信号从时间域变换到频率域主要通过拉普拉斯变换和傅立叶变换等来实现。很简单时域分析的函数是参数是t，也就是y=f(t)，频域分析时，参数是w，也就是y=F(w)两者之间可以互相转化。时域函数通过傅立叶或者拉普拉斯变换就变成了频域函数。


4、串口通信：UART和USART都是全双工


USART 是 UART 的超集，既支持无时钟线的异步通信（日常串口用法），也支持带 SCLK 时钟线的同步通信；而 UART 仅支持异步。
异步串行通信：所谓异步通信，是指数据传送以字符为单位，字符与字符间的传送是完全异步的，位与位之间的传送基本上是同步的。
异步串行通信的数据格式如图1所示，每个字符（每帧信息）由4个部分组成：
①1位起始位，规定为低电平0；
②5～8位数据位，即要传送的有效信息。通常我们使用 8 位数据包，总是在 START 位之后发送。
③0~1位奇偶校验位；
④1～2位停止位，规定为高电平1。

同步串行通信：所谓同步通信，是指数据传送是以数据块（一组字符）为单位，字符与字符之间、字符内部的位与位之间都同步。要求发送和接收方的时钟必须一致。

奇校验（odd parity）：让传输的数据（包含校验位）中1的个数为奇数。即：如果传输字节中1的个数是偶数，则校验位为“1”，奇数相反。以发送字符：10101010为例

偶校验（even parity）：让传输的数据（包含校验位）中1的个数为偶数。即：如果传输字节中1的个数是偶数，则校验位为“0”，奇数相反。

5、单工、半双工、全双工
单工：数据只能在一个方向上传输。
半双工：数据可以在两个方向上进行传输，但是同一时刻，只允许数据在一个方向上传输，相当于单工通信的分时复用。
全双工：数据可以同时在两个方向上传输

USART是一个全双工通用同步/异步串行收发模块。
USART与UART的差别是有没有同步时钟，UART是没有同步时钟的，仅依靠协议进行传输。

6、IIC通信原理（PHILIPS公司开发）半双工
传输速度100-400kHz
每个设备都有自己的一个地址，当主机要与多个外围设备中的一个设备通信时，首先要发送要通信的器件地址以确定通信目标。
IIC空闲信号，起始信号和终止信号
空闲信号：SCL和SDA都为高电平期间。
起始信号：SCL为高电平期间，SDA产生一个下降沿。
终止信号：SCL为高电平期间，SDA产生一个上升沿。
注：起始信号和终止信号都是通过边沿触发而非电平触发
如图：


在给一个起始信号后开始传送一字节的数据（一字节为8位）。
而每一位的传送就是在SCL的一个周期期间完成（对应时序图中SCL的1就是一个周期），而在一个SCL周期内当SCL为高电平期间SDA数据不能变的保持原数据，而只有在SCL为低电平期间SDA的数据才能变

单片机与外围设备IIC通信过程：
在起始信号后必须传送一个从机的地址(7位)，第8位是数据的传送方向位(R/T)，用“0”表示主机发送数据(T)，“1”表示主机接收数据(R)。每次数据传送总是由主机产生的终止信号来结束。
在总线的一次数据传输中，可以有以下几种组合方式：
 (1)、主机向从机发送数据，数据传送方向在整个传递过程中不变：

(2)、主机在第一个字节后，立即从从机读数据(传输方向不变)：

(3)、在传送过程中，当需要改变传递方向时，起始信号和从机地址都被重复一次，但两次读/写方向位正好相反

注：主机做的都是编程控制，从机做的都是自主控制，也可以说是硬件控制，如主机给应答信号是编程控制，但是从机给应答信号是硬件控制。

7、SPI通信原理（Motorola公司）全双工
数据传输速率很高可达到50Mhz
SPI的通信原理是以主从方式工作，这种模式通常有一个主设备和一个或多个从设备。SPI接口经常被称为4线串行总线，分别是SDI(数据输入)、SDO(数据输出)、SCLK(时钟)、CS(片选)。
（a）SDO/MOSI –主设备数据输出，从设备数据输入；
（b）SDI/MISO –主设备数据输入，从设备数据输出；
（c）SCLK –时钟信号，由主设备产生；
（d）CS/SS –从设备使能信号，由主设备控制。
在一个SPI时钟周期内，会完成如下操作：
1) 主设备通过MOSI线发送1位数据，从设备通过该线读取这1位数据；
2) 从设备通过MISO线发送1位数据，主设备通过该线读取这1位数据。
这是通过移位寄存器来实现的。
SPI的数据传输是与时钟信号同步的，但具体一个时钟周期传输多少位数据取决于SPI的配置和使用的模式。SPI可以使用不同的时钟极性和时钟相位配置，从而在一个时钟周期内传输多位数据。
IIC使用SDA信号线来传输数据，使用SCL信号线进行数据同步。在I²C通信中，数据是在SCL的每个时钟周期的上升沿或下降沿传输的，但通常每个时钟周期只传输一位数据。这意味着I²C确实是在每个时钟周期传输一位数据。

8、CAN通信原理（BOSCH公司）半双工
在CAN总线中，数据通过两根导线（CAN_H和CAN_L）进行传输，两根导线之间的电压差表示数据的逻辑状态。
逻辑1（隐性电平）：当CAN总线处于逻辑1的状态时，也被称作隐性电平。此时，CAN_H和CAN_L两根导线上的电压都是静态的，大约为2.5V，因此它们之间的电压差为0V。这种状态在CAN通信中表示逻辑1。
逻辑0（显性电平）：当CAN总线处于逻辑0的状态时，也被称作显性电平。此时，CAN_H和CAN_L两根导线上的电压不再相同，通常CAN_H的电压为3.5V，而CAN_L的电压为1.5V，因此它们之间的电压差为2V（或稍大于2V，一般在2~3V之间）。这种状态在CAN通信中表示逻辑0。
CAN通信采用帧结构进行数据传输。帧是CAN通信的基本单位，包括起始位、标识符、控制位、数据域、校验位和结束位等部分。数据帧用于实际数据的传输，而远程帧用于请求数据。

9、UART（Universal Asynchronous Receiver/Transmitter，通用异步收发传输器）的TX（发送）和RX（接收）引脚在空闲时通常保持高电平状态。
当UART开始发送数据时，TX引脚会首先拉低一个低电平并持续一个周期，这表示传输的开始。然后，它会按照设定的数据位长度（可以是5、6、7或8位）发送数据。在数据发送完成后，TX引脚会恢复高电平状态。
同样地，当UART接收数据时，RX引脚会检测到来自通信线路的低电平信号，这表示有数据正在传输。然后，它会按照与发送方相同的数据位长度接收数据，并在接收完成后恢复高电平状态。
UART通信一般使用4根线进行连接，分别是：
（1）VCC：供电引脚，一般接3.3V或5V的电源，用于给UART模块提供电力。
（2）GND：接地引脚，用于将UART模块的参考地与其他设备或系统连接在一起，确保信号传输的稳定性。两个设备通过UART通信时，GND引脚最好共地，否则数据接收可能会出问题。
（3）RX（Receive）：数据接收引脚，用于接收来自另一设备（或模块）发送的数据。在连接时，这个引脚应该与另一设备的TX引脚相连。
（4）TX（Transmit）：数据发送引脚，用于将本设备（或模块）的数据发送给另一设备。在连接时，这个引脚应该与另一设备的RX引脚相连。

在单片机中，串口发送的周期主要由几个关键因素决定：
（1）波特率：波特率（baud rate）是串口通信中最重要的参数之一，它定义了每秒传输的二进制位数。例如，如果波特率是9600，那么每秒将发送9600个比特。因此，每个比特（即一个位的数据）的发送周期是1/波特率。
（2）数据格式：串口通信的数据格式通常包括起始位、数据位、停止位等。一个完整的数据包（也称为一个帧）的发送时间将是这些位的发送时间之和。例如，一个常见的配置是1个起始位、8个数据位、无奇偶校验位和1个停止位，总共10位。在这种情况下，一个数据包的发送时间将是10个比特周期。
（3）硬件和软件实现：
硬件：单片机的串口硬件（如UART）会根据设置的波特率生成相应的时钟信号，以控制数据的发送和接收。
软件：在软件层面，你可以通过配置单片机的寄存器来设置波特率和其他串口参数。此外，你还需要编写代码来将要发送的数据放入串口的发送缓冲区，并等待发送完成。

10、IIC（也称为I²C，Inter-Integrated Circuit）和SPI（Serial Peripheral Interface）在功能上既有相同点也有不同点。以下是它们之间的比较：
相同点：
（1）两者都是串行、同步的通信方式，都使用TTL电平进行通信。
（2）传输距离和应用场景类似，通常用于微控制器与其他外设之间的通信。
（3）都采用主从方式工作，即有一个主设备（通常是微控制器）和一个或多个从设备（通常是外设）。
不同点：
（1）工作模式与线的数目：
I²C是半双工通信，即在一个时刻只能发送或接收数据，不能同时进行。它使用两根线进行通信：SCL（时钟线）和SDA（数据线）。
SPI是全双工通信，即可以同时发送和接收数据。它使用四根线进行通信：SCK（时钟线）、CS（片选线）、MOSI（主出从入线）和MISO（主入从出线）。
（2）速度：
SPI的速度通常比I²C快。SPI的速度可以达到几MHz到几十MHz，而I²C的速度通常在100Kbps到3.4Mbps之间。
（3）流控方面：
SPI缺乏流控机制，即主设备和从设备之间不进行消息的确认。因此，主设备无法知道从设备是否繁忙。
I²C存在ACK应答信号，即有流控机制。当从设备成功接收到数据后，会向主设备发送一个应答信号（ACK）。
（4）设备的选择：
I²C总线支持多主机总线，一主多从，即可以有多个主设备。它通过SDA上的地址信息来锁定从设备。
SPI总线只有一个主设备。主设备通过CS片选来确定与哪个从设备进行通信。
（5）驱动编写方面：
I²C总线读写时序比较固定统一，因此设备驱动编写相对简单。
SPI总线不同从设备的读写时序差别比较大，因此必须根据具体的设备datasheet来实现读写，相对复杂一些。

SPI和I²C在空闲状态下的电平状态并不都是高电平。
对于SPI（Serial Peripheral Interface）通信，空闲状态下的电平状态取决于时钟极性（CPOL）的设置。CPOL=0时，空闲状态下SCLK（时钟信号线）保持低电平；CPOL=1时，空闲状态下SCLK保持高电平。
而对于I²C（Inter-Integrated Circuit）通信，在空闲状态下，SDA（数据线）和SCL（时钟线）都为高电平。具体的高电平电压值取决于I²C总线的电气特性，一般来说，高电平电压会在电源电压的一定范围内，具体值会根据具体的I²C设备和系统设计而有所不同。
至于高电平的电压值，它取决于具体的I²C总线标准和设备规格。在标准I²C总线中，高电平电压（Vih）通常被定义为电源电压（Vcc）的一定比例，如70%的Vcc。然而，这个值可能会因不同的I²C设备和系统设计而有所变化。

10、
TTL电平的标准电压范围如下，单端输入：
0～3.3V 是 LVTTL，属于 TTL 电平范畴，和 5V TTL 只是电压不同，电气规范一脉相承。
逻辑“1”（高电平）：电压范围为2.4V~5V（或0~5V，取决于具体实现）。
逻辑“0”（低电平）：电压范围为0V~0.8V（或0~0.5V，取决于具体实现）。

RS232电平范围，差分输入：
逻辑“1”表示为负电平，电平范围通常在-3V至-25V之间。
逻辑“0”表示为正电平，电平范围通常在+3V至+25V之间。
这种不对称的电平表示方式使得RS232能够在长距离和嘈杂环境下进行可靠通信。

RS422电平范围，差分输入：
通讯数据4线方式（TX+,TX-,RX+,RX-），采用单独的发送和接收通道,支持全双工；
电平值，正电平+2~+6v，负电平-2~-6v（TX+和TX-的压差）；它们两者形成“1”或“0”两种状态；
两对双绞线（每对线信号组成T+&R+,T-&R-)，信号地线C，使能端（可用可不用）。对于接收端当T+和T-之间压差大于200mv时是正逻辑电平；小于-200mv时是负逻辑电平。

CMOS电平范围：
在CMOS电路中，输出电平范围为0到VDD。当输出电压减小到接近0时，可以将其视为低电平（L）；当输出电压接近VDD时，则可以将其视为高电平（H）。

通过 FreeRTOS 的 uxTaskGetStackHighWaterMark API 可获取线程栈的剩余高水位值，减去总栈大小即得已使用量；调试阶段可通过 IDE 的 RTOS 视图直观查看，核心原理是栈填充标记值的边界检测。

10、单片机的输入方式主要有以下几种：
（1）浮空输入：引脚悬空，电平状态不确定。这种输入方式通常用于没有特定输入要求的引脚。
（2）上拉输入：内部默认接上拉电阻，即默认输入高电平。当引脚未连接外部信号时，由于上拉电阻的作用，引脚电平为高。
（3）下拉输入：内部默认接下拉电阻，即默认输入低电平。当引脚未连接外部信号时，由于下拉电阻的作用，引脚电平为低。
（4）模拟输入：引脚直接连接片上AD模块，用于接收外部模拟信号并进行模数转换。

单片机的输出方式主要有以下几种：
（1）推挽输出：可认为是有输出高电平能力的输出，IO口具有输出高电平的能力。推挽输出结构由两个互补的晶体管（或MOSFET）组成，当输入信号变化时，一个晶体管导通，另一个截止，从而控制输出电平的高低。
（2）开漏输出：需连接上拉电阻才可输出高电平。开漏输出在输出高电平时，输出引脚与电源之间处于断开状态，需要外部上拉电阻将输出引脚拉至高电平。
（3）复用推挽输出：类似推挽输出，但输出由片上外设而非IO口寄存器定义，如SPI、IIC等外设。
（4）复用开漏输出：类似开漏输出，但输出由片上外设而非IO口寄存器定义，如SPI、IIC等外设。
个人理解：当这个IO被复用为比如IIC功能，这个时候的输出就由IIC外设决定，而不是IO寄存器决定
开漏输出 & 推挽输出 区别
推挽输出有上下两个 MOS 管，可强推高、强推低，不能共线；开漏输出只有下拉MOS管，只能拉低，高电平靠外部上拉，支持多设备线与，常用于 I2C 等总线。

10、freeRTOS任务死锁问题：
在FreeRTOS中，任务死锁是一个常见的并发编程问题，它发生在两个或多个任务互相等待对方释放资源，从而形成一个循环等待的状态，导致所有涉及的任务都无法继续执行。以下是一些导致FreeRTOS中任务死锁的常见原因及预防策略：
常见原因：
（1）循环等待资源：如前面信息所述，当任务A持有资源X并等待资源Y，而任务B持有资源Y却等待资源X时，就会形成死锁。
（2）不可抢占的资源持有：任务在持有锁或其他共享资源时被阻塞，且没有释放已持有的资源，导致其他需要这些资源的任务也无法执行。
（3）递归锁的不当使用：如果一个任务在已经持有一个互斥锁的情况下，再次尝试获取同一个锁，可能会导致该任务自我死锁。
（4）优先级翻转：高优先级任务等待低优先级任务释放资源，而低优先级任务因无法获得CPU时间（因为高优先级任务正在等待）而无法释放资源。

预防策略：
（1）避免循环等待：确保任务按照相同的顺序请求资源，或者使用层次化资源分配策略，避免形成循环等待链。
（2）使用超时机制：在尝试获取资源时加入超时判断，如果在指定时间内未获取到资源，则任务应释放已持有的所有资源并回退，避免永久阻塞。
（3）优先级继承或天花板优先级：使用FreeRTOS的优先级继承特性，当低优先级任务持有高优先级任务需要的资源时，暂时提升低优先级任务的优先级，防止优先级翻转。

10、在单片机编程中，特别是在使用基于ARM Cortex-M架构的处理器时，MSP（主堆栈指针，Main Stack Pointer）和PSP（进程堆栈指针，Process Stack Pointer）是两个重要的概念。
MSP（主堆栈指针）：
MSP是复位后默认使用的堆栈指针。
它主要用于操作系统内核以及异常处理例程，包括中断服务例程。
当没有操作系统时，程序复位后运行使用的总是MSP，异常或中断、程序运行也都使用MSP。
MSP的存在主要是为了确保操作系统和异常处理程序有一个独立的堆栈，从而避免被用户程序意外访问或占用。
PSP（进程堆栈指针）：
PSP主要由用户的应用程序代码使用。
在操作系统初始化时，会对PSP进行初始化。
在FreeRTOS等操作系统中，PSP用于任务栈，所有栈空间的使用都是通过PSP指针进行指向的。
同一时刻，MSP和PSP只能使用其中一个。
当操作系统开始执行任务时，会将任务栈push到PSP，并切换到PSP。
通过设置CONTROL寄存器的bit[1]可以选择使用哪个堆栈指针。当该位设置为1时，进入线程模式，使用PSP；否则，使用MSP。
总的来说，MSP和PSP的设计是为了提高程序的健壮性和安全性，确保操作系统的运行和异常处理不会受到用户程序的影响。

10、精度和分辨率有什么区别
	（1）精度是接近真实值的程度, 即绝对误差或相对误差的⼤⼩.精度做到0.01%极其难
	（2）分辩率是量化刻度的细度⼤⼩。
绝对误差和相对误差是测量和计算中常用的两个概念，用于评估测量值或计算值与真实值之间的差异。以下是这两个概念的详细解释：
（1）绝对误差：
定义：绝对误差是测量值或计算值与真实值之间的差的绝对值。
公式：设真实值为 (X)，测量值或计算值为 (x)，则绝对误差 (\Delta X) 为：
（2）相对误差：
定义：相对误差是测量值或计算值与真实值之间的差的绝对值与真实值的比值（通常用百分数表示）。

11、局部变量和全局变量分别存放在哪⾥？
	C语⾔中局部变量存在栈⾥，全局变量存静态存储区

12、串⼝和并⼝有什么区别
	并⾏接⼝是指数据的各位同时进⾏传送，其特点是传输速度快，但当传输距离较远、位数⼜多时，导致了通信线路复杂且成本提⾼；
	串⾏接⼝是指数据⼀位位地顺序传送，其特点是通信线路简单，只要⼀对传输线就可以实现双向通信，并可以利⽤电话线，从⽽⼤⼤降低了成本，特别适⽤于远距离通信，但传送速度较慢；

13、485有⼏根线
RS-485通信可以是半双工的，仅需要两根信号线（A和B），也可以是全双工的，需要四根信号线。具体几根线取决于通信方式的选择。
两根线的RS-485通信中，A和B线用于差分信号传输。
在四线全双工配置中，额外的两根线将用于支持同时发送和接收数据。

14、举你⽤过的单⽚机的型号和它的⼀些主要参数
STM32F103VCT6：
内核
-ARM 32位 Cortex - M 3处理器
-⾼达72 MHz
内存
-256 KB 的 Flash
-48 KB 的 SRAM
3个12位AD
7个通道DMA
8个定时器
12个通信接⼝
-2个 I 2 C 接⼝
-5 个 UART
-3 个 SPI
-1 个 USB
-1 个 CAN

15、假如⼀个单⽚机上电之后不运⾏？这是为什么？
（1）检查供电：直接⽤万⽤表测量VCC和GND的电平，是否符合要求。如果VCC偏离5V或3.3V过多，检查7805或其他稳压、滤波电路的输出。
（2）检查晶振：⼀般是多换⼏个晶振上电试试。
（3）检查RESET引脚电平逻辑，注意所⽤机型是⾼电平复位还是低电平复位的，防⽌MCU⼀直处于反复被复位状态。
（4）如果设计时，程序是从扩展的外部ROM开始运⾏的，还需检查EA脚。
注：在经典的8051微控制器和其他一些兼容的微控制器中，EA（External Access Enable）引脚用于控制程序是从内部ROM还是外部ROM启动。然而，STM32微控制器并不直接具有与8051微控制器相同的EA引脚，因为STM32是基于ARM Cortex-M内核的，而不是8051内核。
（5）检查MCU是否损坏或flash⽆法下载，最好换块新的芯⽚试试。
（6）如果测试程序运⾏正常。那就基本确定是控制程序的问题了。
个⼈经验：⾸先检查MCU供电是否正常，再判断擦除和烧录是否正常，再判断控制程序是否正常，⼀般情况下3、4不会出现，对于STM32来说可以换内部晶振来排除2。

16、讲⼀下stm32的时钟系统
时钟在MCU中的作⽤，就好⽐于⼈类的⼼脏⼀样不可或缺。STM32有多个时钟源可以使⽤。因为STM32拥有丰富的外设资源，不同外设使⽤的时钟也会不⼀样，同时我们要知道时钟越快，功耗就越⼤，抗电磁⼲扰的能⼒就会减弱，因此，⽐较复杂的MCU 都会采⽤多时钟来解决这些问题。
STM32 有5个时钟源:HSI、HSE、LSI、LSE、PLL。
①、HSI是⾼速内部时钟，RC振荡器，频率为16MHz，精度不⾼。可以直接作为系统时钟或者⽤作PLL时钟输⼊。
②、HSE是⾼速外部时钟，可接⽯英/陶瓷谐振器，或者接外部时钟源，频率范围为4MHz~26MHz。
③、LSI是低速内部时钟，RC振荡器，频率为32kHz，提供低功耗时钟。主要供独⽴看门狗和⾃动唤醒单元使⽤。
④、LSE是低速外部时钟，接频率为32.768kHz的⽯英晶体。作为RTC时钟；
⑤、PLL为锁相环倍频输出。提供2-16倍的倍频输出。
一、进程与线程
17、进程和线程的区别
进程是操作系统资源分配的基本单位，线程是操作系统调度和执行的基本单位，pid是进程身份标识符(唯一的数字)，每个进程都对应一个pid。
同一时间，如果CPU是单核，只有一个进程在执行，所谓的并发执行，也是顺序执行，只不过由于切换速度太快，你以为这些进程在同步执行而已。多核CPU可以同一时间点有多个进程在执行。

根本区别：进程是操作系统资源分配的基本单位，而线程是任务调度和执行的基本单位
在开销方面：每个进程都有独立的代码和数据空间（程序上下文），程序之间的切换会有较大的开销；线程可以看做轻量级的进程，同一类线程共享代码和数据空间，每个线程都有自己独立的运行栈和程序计数器（PC），线程之间切换的开销小。
所处环境：在操作系统中能同时运行多个进程（程序）；而在同一个进程（程序）中有多个线程同时执行（通过CPU调度，在每个时间片中只有一个线程执行）
内存分配方面：系统在运行的时候会为每个进程分配不同的内存空间；而对线程而言，除了CPU外，系统不会为线程分配内存（线程所使用的资源来自其所属进程的资源），线程组之间只能共享资源。
包含关系：没有线程的进程可以看做是单线程的，如果一个进程内有多个线程，则执行过程不是一条线的，而是多条线（线程）共同完成的；线程是进程的一部分，所以线程也被称为轻权进程或者轻量级进程。

总结一下：
进程：1、操作系统会分配地址空间 2、一个进程当中可以有多个线程
线程：1、没有独立的地址空间 2、一个进程里的多个线程可以共享该进程的所有资源 3、线程有自己的栈，没有堆4、线程是不能独立执行的

补充：
同步和互斥的区别：
当有多个线程的时候，经常需要去同步这些线程以访问同一个数据或资源。例如，假设有一个程序，其中一个线程用于把文件读到内存，而另一个线程用于统计文件中的字符数。当然，在把整个文件调入内存之前，统计它的计数是没有意义的。但是，由于每个操作都有自己的线程，操作系统会把两个线程当作是互不相干的任务分别执行，这样就可能在没有把整个文件装入内存时统计字数。为解决此问题，你必须使两个线程同步工作。

所谓同步，是指散步在不同进程之间的若干程序片断，它们的运行必须严格按照规定的某种先后次序来运行，这种先后次序依赖于要完成的特定的任务。如果用对资源的访问来定义的话，同步是指在互斥的基础上（大多数情况），通过其它机制实现访问者对资源的有序访问。在大多数情况下，同步已经实现了互斥，特别是所有写入资源的情况必定是互斥的。少数情况是指可以允许多个访问者同时访问资源。

所谓互斥，是指散布在不同进程之间的若干程序片断，当某个进程运行其中一个程序片段时，其它进程就不能运行它们之中的任一程序片段，只能等到该进程运行完这个程序片段后才可以运行。如果用对资源的访问来定义的话，互斥某一资源同时只允许一个访问者对其进行访问，具有唯一性和排它性。但互斥无法限制访问者对资源的访问顺序，即访问是无序的。

18、多进程、多线程的优缺点
说明：一个进程由进程控制块、数据段、代码段组成，进程本身不可以运行程序，而是像一个容器一样，先创建出一个主线程，分配给主线程一定的系统资源，这时候就可以在主线程开始实现各种功能。当我们需要实现更复杂的功能时，可以在主线程里创建多个子线程，多个线程在同一个进程里，利用这个进程所拥有的系统资源合作完成某些功能。
优缺点：
1）一个进程死了不影响其他进程，一个线程崩溃很可能影响到它本身所处的整个进程。
2）创建多进程的系统花销大于创建多线程。
3）多进程通讯因为需要跨越进程边界，不适合大量数据的传送，适合小数据或者密集数据的传送。多线程无需跨越进程边界，适合各线程间大量数据的传送。并且多线程可以共享同一进程里的共享内存和变量。

19、什么时候用进程，什么时候用线程
1）创建和销毁较频繁使用线程，因为创建进程花销大。
2）需要大量数据传送使用线程，因为多线程切换速度快，不需要跨越进程边界。
3）安全稳定选进程；快速频繁选线程；

20、多进程、多线程同步（通讯）的方法
进程间通讯：
（1）有名管道/无名管道（2）信号（3）共享内存（4）消息队列（5）信号量（6）socket
线程通讯（锁）：
（1）信号量（2）读写锁（3）条件变量（4）互斥锁（5）自旋锁

通常导致进程阻塞的典型事件有：请求I/O，申请缓冲空间等。

进程间通信的方式
（1）管道（pipe）及有名管道（named pipe）：管道可用于具有亲缘关系的父子进程间的通信，有名管道除了具有管道所具有的功能外，它还允许无亲缘关系进程间的通信。
（2）信号（signal）：信号是在软件层次上对中断机制的一种模拟，它是比较复杂的通信方式，用于通知进程有某事件发生，一个进程收到一个信号与处理器收到一个中断请求效果上可以说是一致的。
（3）消息队列（message queue）：消息队列是消息的链接表，它克服了上两种通信方式中信号量有限的缺点，具有写权限的进程可以按照一定的规则向消息队列中添加新信息；对消息队列有读权限的进程则可以从消息队列中读取信息。
（4）共享内存（shared memory）：可以说这是最有用的进程间通信方式。它使得多个进程可以访问同一块内存空间，不同进程可以及时看到对方进程中对共享内存中数据的更新。这种方式需要依靠某种同步操作，如互斥锁和信号量等。
（5）信号量（semaphore）：主要作为进程之间及同一种进程的不同线程之间的同步和互斥手段。
（6）套接字（socket）：这是一种更为一般的进程间通信机制，它可用于网络中不同机器之间的进程间通信，应用非常广泛。

21、父进程、子进程
父进程调用fork()以后，克隆出一个子进程，子进程和父进程拥有相同内容的代码段、数据段和用户堆栈。父进程和子进程谁先执行不一定，看CPU。所以我们一般会设置父进程等待子进程执行完毕。

22、进程上下文和中断上下文。
进程上下文：一个进程在执行的时候，CPU的所有寄存器中的值、进程的状态以及堆栈中的内容，当内核需要切换到另一个进程时，它需要保存当前进程的所有状态，即保存当前进程的进程上下文，以便再次执行该进程时，能够恢复切换时的状态，继续执行。
中断上下文：由于触发信号，导致CPU中断当前进程，转而去执行另外的程序。那么当前进程的所有资源要保存，比如堆栈和指针。保存过后转而去执行中断处理程序，执行完毕返回，返回后恢复上一个进程的资源，继续执行。这就是中断的上下文。

23、new和malloc
1）malloc和free是c++/c语言的库函数，需要头文件支持stdlib.h；new和delete是C++的关键字，不需要头文件，需要编译器支持；
2）使用new操作符申请内存分配时，无需指定内存块的大小，编译器会根据类型信息自行计算。而malloc则需要显式地指示所需内存的大小。
3）new操作符内存分配成功时，返回的是对象类型的指针，类型严格与对象匹配，无需进行类型转换，故new是符合类型安全性的操作符。而malloc内存分配成功则是返回void*，需要通过强制类型转换将void*指针转换成我们需要的类型。
4）new内存分配失败时，会抛出bad_alloc异常。malloc分配内存失败时返回NULL。

24、在1G内存的计算机中能否malloc(1.2G)？为什么？（2021浙江大华二面问题）
答：是有可能申请1.2G的内存的。
解析：回答这个问题前需要知道malloc的作用和原理，应用程序通过malloc函数可以向程序的虚拟空间申请一块虚拟地址空间，与物理内存没有直接关系，得到的是在虚拟地址空间中的地址，之后程序运行所提供的物理内存是由操作系统完成的。
（1）malloc 的本质是向操作系统申请「虚拟地址空间」，而非直接占用物理内存；现代操作系统为程序分配的虚拟地址空间远大于 1.2G（比如 32 位程序 4G、64 位程序更大），因此 1G 物理内存不影响虚拟地址的申请，malloc 会返回成功。
（2）物理内存的分配是「按需映射」的：只有程序实际读写这段虚拟地址时，操作系统才会通过缺页中断分配物理内存页；若程序仅申请不使用，1.2G 虚拟地址不会占用物理内存；若程序要使用全部 1.2G，会因物理内存不足触发 OOM，导致程序崩溃。

25 、extern”C” 的作用
我们可以在C++中使用C的已编译好的函数模块，这时候就需要用到extern”C”。也就是extern“C” 都是在c++文件里添加的。
extern在链接阶段起作用（四大阶段：预处理--编译--汇编--链接）。
Volatile在编译阶段起作用

26、static的用法（定义和用途）（必考）
1）用static修饰局部变量：使其变为静态存储方式(静态数据区)，那么这个局部变量在函数执行完成之后不会被释放，而是继续保留在内存中。
2）用static修饰全局变量：使其只在本文件内部有效，而其他文件不可连接或引用该变量。
3）用static修饰函数：对函数的连接方式产生影响，使得函数只在本文件内部有效，对其他文件是不可见的（这一点在大工程中很重要很重要，避免很多麻烦，很常见）。这样的函数又叫作静态函数。使用静态函数的好处是，不用担心与其他文件的同名函数产生干扰，另外也是对函数本身的一种保护机制。

27、const的用法（定义和用途）（必考）
const主要用来修饰变量、函数形参和类成员函数：
1）用const修饰常量：定义时就初始化，以后不能更改。
2）用const修饰形参：func(const int a){};该形参在函数里不能改变
3）用const修饰类成员函数：该函数对成员变量只能进行只读操作，就是const类成员函数是不能修改成员变量的数值的。
被const修饰的东西都受到强制保护，可以预防意外的变动，能提高程序的健壮性。

1、定义常量
(1)const(修饰的变量不可变)
修饰变量，以下两种定义形式在本质上是一样的。它的含义是：const修饰的类型为TYPE的变量value是不可变的,readonly。
TYPE const ValueName = value;
const TYPE ValueName = value;

(2)将const改为外部连接，作用域扩大至全局，编译时会分配内存，并且可以不进行初始化，仅仅作为声明，编译器认为在程序其他地方进行了定义. (修饰的外部链接可以不用定义直接声明使用)
Extern const int ValueName;

2、指针使用const（）
(1)指针本身是常量不可变
char * const pContent;
const (char*) pContent;
(2)指针所指向的内容是常量不可变
const char *pContent;
char const *pContent;
(3)两者都不可变
const char* const pContent;
(4)还有其中区别方法，沿着*号划一条线：如果const位于*的左侧，则const就是用来修饰指针所指向的变量，即指针指向为常量；如果const位于*的右侧，const就是修饰指针本身，即指针本身是常量。（以上面的第一行示例为准）



内存碎片的本质：频繁分配 / 释放不同大小的内存块，导致堆中出现大量 “小块空闲内存”，无法拼接成连续大内存块，最终 malloc 失败。

28、volatile作用和用法
一个定义为volatile的变量是说这变量可能会被意想不到地改变，这样，编译器就不会去假设这个变量的值了。精确地说就是，优化器在用到这个变量时必须每次都小心地重新读取这个变量在内存中的值，而不是使用保存在寄存器里的备份（虽然读写寄存器比读写内存快）。
回答不出这个问题的人是不会被雇佣的。这是区分C程序员和嵌入式系统程序员的最基本的问题。搞嵌入式的家伙们经常同硬件、中断、RTOS等等打交道，所有这些都要求用到volatile变量。不懂得volatile的内容将会带来灾难。
以下几种情况都会用到volatile：
1、并行设备的硬件寄存器（如：状态寄存器）
2、一个中断服务子程序中会访问到的非自动变量
注：非自动变量（Non-automatic variables）通常指的是那些不会在其作用域结束时自动释放其存储空间的变量。在编程中，主要有两种类型的非自动变量：全局变量（Global variables）和静态变量（Static variables）。
3、多线程应用中被几个任务共享的变量

29、const常量和#define的区别（编译阶段、安全性、内存占用等）
用#define max 100 ; 定义的常量是没有类型的（不进行类型安全检查，可能会产生意想不到的错误），所给出的是一个立即数，编译器只是把所定义的常量值与所定义的常量的名字联系起来，define所定义的宏变量在预处理阶段的时候进行替换，在程序中使用到该常量的地方都要进行拷贝替换；
用const int max = 255 ; 定义的常量有类型（编译时会进行类型检查）名字，存放在内存的静态区域中，在编译时确定其值。在程序运行过程中const变量只有一个拷贝，而#define所定义的宏变量却有多个拷贝，所以宏定义在程序运行过程中所消耗的内存要比const变量的大得多；

30、变量的作用域（全局变量和局部变量）
全局变量：在所有函数体的外部定义的，程序的所在部分（甚至其它文件中的代码）都可以使用。全局变量不受作用域的影响（也就是说，全局变量的生命期一直到程序的结束）。
局部变量：出现在一个作用域内，它们是局限于一个函数的。局部变量经常被称为自动变量，因为它们在进入作用域时自动生成，离开作用域时自动消失。关键字auto可以显式地说明这个问题，但是局部变量默认为auto，所以没有必要声明为auto。
局部变量可以和全局变量重名，在局部变量作用域范围内，全局变量失效，采用的是局部变量的值。

31、sizeof与strlen （字符串，数组）
（1）如果是数组
#include<stdio.h>
int main()
{
    int a[5]={1,2,3,4,5};
    printf(“sizeof  数组名=%d\n”,sizeof(a));
    printf(“sizeof *数组名=%d\n”,sizeof(*a));
}
运行结果
sizeof  数组名=20
sizeof *数组名=4

（2）如果是指针，sizeof只会检测到是指针的类型，指针都是占用4个字节的空间（32位机）。sizeof是什么？是一个操作符,也是关键字，就不是一个函数，这和strlen()不同，strlen()是一个函数。
那么sizeof的作用是什么？返回一个对象或者类型所占的内存字节数。我们会对sizeof()中的数据或者指针做运算吗？基本不会。例如sizeof(1+2.0)，直接检测到其中类型是double,即是sizeof(double) = 8。如果是指针，sizeof只会检测到是指针的类型，指针都是占用4个字节的空间（32位机）。
char *p = "sadasdasd";
sizeof(p):4
sizeof(*p):1//指向一个char类型的

除非使用strlen()，仅对字符串有效，直到'\0'为止了，计数结果不包括\0。
要是非要使用sizeof来得到指向内容的大小，就得使用数组名才行， 如
char a[10];
sizeof(a):10  //检测到a是一个数组的类型。

关于strlen()，它是一个函数，考察的比较简单：
strlen “\n\t\tag\AAtang”
答案：11

32、经典的sizeof(struct)和sizeof(union)内存对齐
内存对齐作用：
（1）平台原因(移植原因)：不是所有的硬件平台都能访问任意地址上的任意数据的；某些硬件平台只能在某些地址处取某些特定类型的数据，否则抛出硬件异常。
（2）性能原因：数据结构(尤其是栈)应该尽可能地在自然边界上对齐。原因在于，为了访问未对齐的内存，处理器需要作两次内存访问；而对齐的内存访问仅需要一次访问。

结构体struct内存对齐的3大规则:
（1）对于结构体的各个成员，第一个成员的偏移量是0，排列在后面的成员其当前偏移量必须是当前成员类型的整数倍；
（2）结构体内所有数据成员各自内存对齐后，结构体本身还要进行一次内存对齐，保证整个结构体占用内存大小是结构体内最大数据成员的最小整数倍；
（3）如程序中有#pragma pack(n)预编译指令，则所有成员对齐以n字节为准(即偏移量是n的整数倍)，不再考虑当前类型以及最大结构体内类型。
#pragma pack(1)
struct fun{
  int i;
  double d;
  char c;
};
sizeof(fun) = 13

struct CAT_s
{
    int ld;
    char Color;
    unsigned short Age;
    char *Name;
    void(*Jump)(void);
}Garfield;

（1）使用32位编译，int占4， char 占1， unsigned short 占2，char* 占4，函数指针占4个，由于是32位编译是4字节对齐，所以该结构体占16个字节。（说明：按几字节对齐，是根据结构体的最长类型决定的，这里是int是最长的字节，所以按4字节对齐）；
（2）使用64位编译 ，int占4， char 占1， unsigned short 占2，char* 占8，函数指针占8个，由于是64位编译是8字节对齐（说明：按几字节对齐，是根据结构体的最长类型决定的，这里是函数指针是最长的字节，所以按8字节对齐）所以该结构体占24个字节。

//64位
struct C 
{ 
 double t;   //8   1111 1111
 char b;  //1      1
 int a;   //4      0001111  
 short c;  //2     11000000
};  
 sizeof(C) = 24;  //注意：1 4 2 不能拼在一起

char是1，然后在int之前，地址偏移量得是4的倍数，所以char后面补三个字节，也就是char占了4个字节，然后int四个字节，最后是short，只占两个字节，但是总的偏移量得是double的倍数，也就是8的倍数，所以short后面补六个字节
联合体union内存对齐的2大规则:
1.找到占用字节最多的成员；
2.union的字节数必须是占用字节最多的成员的字节的倍数，而且需要能够容纳其他的成员

//x64
typedef union {
    long i;
    int k[5];
    char c;
}D
要计算union的大小,首先要找到占用字节最多的成员,本例中是long,占用8个字节,int k[5]中都是int类型,仍然是占用4个字节的，然后union的字节数必须是占用字节最多的成员的字节的倍数,而且需要能够容纳其他的成员,为了要容纳k(20个字节),就必须要保证是8的倍数的同时还要大于20个字节,所以是24个字节。

引申：位域（大疆笔试题）
C语言允许在一个结构体中以位为单位来指定其成员所占内存长度，这种以位为单位的成员称为“位段”或称“位域”( bit field) 。利用位段能够用较少的位数存储数据。一个位段必须存储在同一存储单元中，不能跨两个单元。如果第一个单元空间不能容纳下一个位段，则该空间不用，而从下一个单元起存放该位段。
1.位段声明和结构体类似
2.位段的成员必须是int、unsigned int、signed int
3.位段的成员名后边有一个冒号和一个数字

typedef struct_data{
   char m:3;
   char n:5;
   short s;
   union{
   int a;
   char b;
   };
   int h;
}_attribute_((packed)) data_t;
答案12

m和n一起，刚好占用一个字节内存，因为后面是short类型变量，所以在short s之前，应该补一个字节。所以m和n其实是占了两个字节的，然后是short两个字节，加起来就4个字节，然后联合体占了四个字节，总共8个字节了，最后int h占了四个字节，就是12个字节了
attribute((packed))   取消对齐
GNU C的一大特色就是__attribute__机制。__attribute__可以设置函数属性（Function Attribute）、变量属性（Variable Attribute）和类型属性（Type Attribute）。
__attribute__书写特征是：__attribute__前后都有两个下划线，并且后面会紧跟一对括弧，括弧里面是相应的__attribute__参数。
跨平台通信时用到。不同平台内存对齐方式不同。如果使用结构体进行平台间的通信，会有问题。例如，发送消息的平台上，结构体为24字节，接受消息的平台上，此结构体为32字节（只是随便举个例子），那么每个变量对应的值就不对了。不同框架的处理器对齐方式会有不同，这个时候不指定对齐的话，会产生错误结果。

33、inline函数
在C语言中，如果一些函数被频繁调用，不断地有函数入栈，即函数栈，会造成栈空间或栈内存的大量消耗。为了解决这个问题，特别的引入了inline修饰符，表示为内联函数。

当你提到函数被频繁调用时，这确实可能会导致栈空间的大量消耗，但这并不意味着这是一个持续的问题，因为一旦函数执行完毕并返回，它的栈帧（stack frame）就会被从栈中弹出（或说“释放”），从而释放掉之前占用的栈空间。
然而，在以下情况下，频繁的函数调用可能会导致栈空间耗尽：
（1）递归调用过深：如果一个函数直接或间接地递归调用自身，并且没有适当的终止条件或递归深度过大，那么调用栈将会不断增长，直到耗尽所有可用的栈空间。这被称为“栈溢出”（stack overflow）。
（2）大量嵌套调用：即使不是递归调用，如果程序中存在大量的嵌套函数调用（即一个函数调用另一个函数，后者又调用另一个函数，依此类推），并且嵌套深度很大，那么也可能导致栈空间耗尽。
（3）栈空间限制：不同的操作系统、编译器或环境可能对栈空间的大小有不同的限制。如果程序试图使用的栈空间超过了这些限制，那么即使函数调用最终会释放栈空间，也可能在达到限制之前就已经发生了栈溢出。

34、linux/windows内存模型
栈是有栈指针寄存器，在单片机启动文件中SP指的是初始化栈大小，执行函数时入栈保存函数的上下文，退出函数时出栈恢复函数的上下文。
下图中的静态区的大小在编译时由使用的.bss（未初始化的全局变量/静态变量）和已初始化数据.data（初始化的全局变量/静态变量）确定；



文字常量区，叫ro-data，不可以改变，改变会导致段错误
int a0=1;
static int a1;
const static a2=0;
extern int a3;

void fun(void)
{
 int a4;
 volatile int a5;

 return;
}

a0 ：全局初始化变量；生命周期为整个程序运行期间；作用域为所有文件；存储位置为data段。
a1 ：全局静态未初始化变量；生命周期为整个程序运行期间；作用域为当前文件；储存位置为BSS段。
a2 ：全局静态变量
a3 ：全局初始化变量；其他同a0。
a4 ：局部变量；生命周期为fun函数运行期间；作用域为fun函数内部；储存位置为栈。
a5 ：局部易变变量；

35、使用32位编译情况下，给出判断所使用机器大小端的方法。



35、一个结合PWM（脉宽调制）和PID（比例-积分-微分）控制的温控实例可以是这样的：
假设我们有一个需要精确控制温度的设备，例如一个加热炉。我们的目标是将加热炉的温度稳定地维持在设定的目标温度上。
首先，我们需要一个温度传感器来实时测量加热炉的当前温度，并将其转化为电信号供控制系统使用。这个温度信号会作为PID控制器的输入之一。
接下来，我们设定一个目标温度值，这个值也是我们期望加热炉维持的温度。目标温度与当前温度的差值会被用作PID控制器的另一个输入。
PID控制器会根据这个温差来计算出一个输出值。这个输出值是基于比例、积分和微分三个部分的计算结果的加权和。比例部分直接反映当前的温差，积分部分则考虑了过去一段时间内温差的累积效应，微分部分则预测了温差未来的变化趋势。
然后，我们将PID控制器的输出值转化为PWM信号的占空比。占空比决定了在一个PWM周期内，高电平信号持续的时间长短。通过调整占空比，我们可以控制加热炉的加热功率，从而影响其温度。
具体地，当温差较大时，PID控制器会输出一个较大的值，转化为PWM信号后，占空比增大，加热炉的加热功率增加，从而快速减小温差。当温差较小时，PID控制器会输出一个较小的值，占空比减小，加热功率降低，避免过度加热。

在PID控制中，P（比例）、I（积分）、D（微分）三个参数各自扮演着不同的角色，共同影响着控制系统的响应特性。下面是对每个参数作用的简要说明，以澄清您的疑问：
•P（比例项）：P参数主要负责快速响应误差，并且直接影响输出信号的大小。增加比例增益（P值）可以加快系统响应速度，减小稳态误差，但过大的P值会导致系统响应振荡加剧，可能会引起不稳定性。因此，P不是直接用来“调曲线的振荡程度”，而是影响了响应速度和稳态误差，过大的P值是造成振荡的一个因素。
•I（积分项）：I参数主要用于消除稳态误差，即系统在长期运行后趋于的目标值与实际输出值之间的差异。它通过累计历史误差来逐步调整输出，以确保最终达到无稳态误差。如果I值设置得过高，可能会导致系统响应缓慢，出现超调量增大和响应过程时间延长的问题，但I参数本身不是直接用来调节超调量的，它更多地是帮助消除稳态误差。
•D（微分项）：D参数是预测未来的趋势，基于当前误差变化率进行调节，可以有效减少超调量并提高系统的稳定性。通过提前响应误差的变化趋势，微分项有助于减缓输出的变化速率，从而减少或防止振荡，改善系统的动态性能。
总结来说，PID控制中，P值的调整确实会影响到系统的振荡特性，I值主要解决稳态误差问题，而D值则是用来减少超调量和提高响应速度的关键。

36、操作系统与前后台程序（没有操作系统）区别
前台程序通常是一个实时中断服务程序，后台程序则是一个循环执行的程序，负责一些实时性要求不高的功能，如输入译码、数据处理等插入准备工作和管理程序等。后台程序可以叫作任务级。在背景程序循环运行过程中，前台的实时终端程序不断定时插入，二者密切配合，共同完成零件的加工任务。
   前后台程序：各个模块顺序在一个死循环循环执行！除了中断，各个模块之间是不会相互打断，上个模块执行完了才到下个模块！
   实时操作系统：各个任务是个独立死循环，时间片轮到了，不管任务是否执行完，根据任务优先级有可能会打断。



34、单片机为什么要使用RTOS？
（1）多任务处理：
RTOS：并发执行		单片机：顺序执行
（2）服务：
RTOS：内存管理，任务调度和通信机制，可以简化复杂应用程序的开发。
（3）可靠性：
RTOS：提供一定程度的容错来提高嵌入式系统的可靠性。
（4）易于开发：
RTOS：为开发人员提供更高级别的接口

34、使用示波器测试过程中，方形电波不稳如何解决？
调节触发电平，可能是选择的触发电平太高导致触发不到；
·  探头 ×1 和 ×10 什么区别？
×1：不衰减，直接测
×10：衰减 10 倍，测高压、减小负载影响
注意：示波器要和探头档位一致，否则读数不准。
·  示波器怎么调才能看清波形？
调垂直档位（电压）
调水平时基（时间 / 频率）
调触发（稳定波形）

35、对于你给出的代码 xTaskNotify(ex_TaskNandTestHand, CHECK_BADBLOCK_FLAG, eSetBits);，我们可以这样理解：
ex_TaskNandTestHand：这是任务句柄（TaskHandle_t 类型），指向要接收通知的任务。也就是说，通知将会发送给 ex_TaskNandTestHand 指向的任务。
CHECK_BADBLOCK_FLAG：这是要发送的通知值。通常，这个值可以是任何整数值，它包含了发送任务想要传达给接收任务的信息。在这个例子中，CHECK_BADBLOCK_FLAG 可能是一个宏定义或者枚举值，用于标识特定的通知事件或条件，比如检查坏块。
eSetBits：这是通知动作。FreeRTOS 提供了几种通知动作，包括 eSetBits、eIncrement、eSetValueWithOverwrite 等。在这个例子中，eSetBits 表示将 CHECK_BADBLOCK_FLAG 的值设置为任务通知的对应位上。如果之前这些位上有值，它们将不会被清除，而是直接设置新的位。

35、采样频率满足奈奎斯特采样定理，即fs>=2f（fs为采样频率）。
实际使用过程中，通常要求示波器设定的带宽为被测信号频率的5倍以上才能保证信号波形不失真，同时要求示波器自身上升沿速度为信号上升沿速度的5倍以上。例如用一个带宽为1Ghz的示波器测量方波，要求被测信号不失真，那么可测方波的最高频率为100MHz（测到二次谐波）。
示波器带宽 = 它能准确测量的最高信号频率
示波器的带宽是用频率来表示的。具体来说，示波器的带宽是指示波器能够准确显示信号频率的范围。在测量过程中，示波器能够保持准确度和稳定性的信号频率范围就是其带宽。带宽通常以赫兹（Hz）为单位表示，表示示波器能够测量和显示的信号频率的最大值。
例如，一个带宽为100 MHz的示波器可以测量和显示100兆赫范围内的信号。如果被测信号的频率超过了示波器的带宽范围，示波器将无法准确地显示该信号的波形。
在选择示波器时，需要根据被测信号的频率来选择合适的示波器带宽。如果选择的示波器带宽不足，可能无法看到信号的真实特征，从而导致误解或错误的测量结果。

34、Unix和Linux有什么区别？
开源性
Linux是一款开源操作系统，不需要付费，即可使用；Unix是一款对源码实行知识产权保护的传统商业软件，使用需要付费授权使用。
跨平台性
Linux操作系统具有良好的跨平台性能，可运行在多种硬件平台上；Unix操作系统跨平台性能较弱，大多需与硬件配套使用。
可视化界面
Linux除了进行命令行操作，还有窗体管理系统；Unix只是命令行下的系统。

34、Linux从上电开始运行的过程主要可以分为以下几个阶段：
（1）主机上电自检，加载BIOS硬件信息：这是BIOS功能的一个主要部分，负责完成对CPU、主板、内存、软硬盘子系统、显示子系统、串并行接口、键盘、CD-ROM光驱等的检测。同时，BIOS中的系统设置程序完成CMOS参数的设置，CMOS RAM芯片靠后备电池供电，即使系统掉电后信息也不会丢失。
（2）读取MBR的引导文件：如GRUB或LILO，这是引导过程的一个重要步骤。
（3）引导Linux内核：操作系统接管硬件以后，首先读入/boot目录下的内核文件。Kernel会以只读方式挂载根文件系统，当根文件系统被挂载后，开始装载第一个进程（用户空间的进程），执行/sbin/init。
（4）运行第一个进程init：它是系统所有进程的起点，负责初始化系统环境。
（5）进入相应的运行级别：根据系统的配置和需要，进入不同的运行级别。
（6）运行终端，输入用户名和密码：最后，系统启动终端，等待用户输入用户名和密码进行登录。
BIOS 自检 → GRUB（BootLoader）引导 → 加载内核 → 挂载根文件系统 → 启动 init/systemd → 启动服务 → 登录系统

35、stm32的任务优先级是数值越小其优先级越高

35、在FreeRTOS中，确保任务不会超过其设定的栈大小是一个重要的考虑因素，因为栈溢出可能会导致程序崩溃或不可预测的行为。以下是一些建议和实践，以确保任务不会超过其栈大小：
（1）合理估算栈大小：
在创建任务时，为每个任务分配合理的栈大小。这需要根据任务的功能和预期行为来估算。
考虑任务中使用的局部变量、函数调用深度以及中断服务例程（如果任务中有ISR）的栈使用情况。
（2）避免递归调用：
递归调用会显著增加栈的使用量，尤其是在深层递归时。尽量避免在FreeRTOS任务中使用递归。
（3）减少局部变量：
优化任务代码，减少不必要的局部变量，特别是大型数组或结构体。
尝试将局部变量声明为静态，以便它们只在任务初始化时分配一次，而不是每次任务运行时都分配。
（4）使用栈检查功能：
FreeRTOS提供了一些用于检查任务栈使用情况的机制，例如uxTaskGetStackHighWaterMark函数。可以在任务中定期调用此函数来检查栈的使用情况，并在接近设定的栈大小时采取适当的措施。
（5）任务间通信：
使用FreeRTOS提供的队列、信号量或互斥量等机制进行任务间通信，而不是通过全局变量或共享内存。这可以减少因多个任务同时访问共享资源而导致的栈使用增加。
（6）避免在任务中创建动态内存：
动态内存分配（如使用malloc或new）可能会导致栈溢出，特别是当分配失败时。尽量在任务之外进行动态内存分配，或者在任务初始化时一次性分配所需内存。
使用栈分析工具：
利用专门的栈分析工具或调试器来检查任务的栈使用情况。这些工具可以帮助您识别哪些函数或代码段占用了大量栈空间，从而进行针对性的优化。
设置适当的栈溢出钩子：
在FreeRTOS配置中启用栈溢出检测，并设置一个栈溢出钩子函数。当检测到栈溢出时，该钩子函数将被调用，您可以在其中执行适当的错误处理或记录操作。

35、QSPI（Quad SPI）是一种专用的通信接口，它主要用于连接单、双或四（条数据线）SPI FLASH存储器。这里的“单、双或四（条数据线）”是指QSPI接口支持的数据线数量，可以根据具体的应用需求来选择使用一条、两条还是四条数据线。

QSPI作为SPI协议的扩展，不仅继承了SPI的基本特性，还在某些方面进行了优化和增强。例如，QSPI支持三种工作模式：间接模式、状态轮询模式和内存映射模式。同时，QSPI还支持双闪存模式，可以并行访问两个FLASH，从而提高了吞吐量和容量。

37、I2C死锁是指I2C总线由于某种原因陷入无法通信的状态。以下是可能导致I2C死锁的一些情况：
（1）总线冲突：当两个或多个从属设备尝试与主控设备进行通信时，如果它们同时发送了消息，可能会导致总线冲突，进而引发死锁。
（2）时序问题：I2C总线上的通信需要遵循精确的时序，如果时序不正确，可能会导致通信失败，从而进入死锁状态。
（3）电气问题：如果从属设备的电源电压发生波动或干扰，可能会导致通信异常，进而引发死锁。
（4）硬件问题：从属设备的硬件故障或损坏也可能导致通信失败，进而进入死锁状态。
针对I2C死锁问题，可以采取以下解决方法：
（1）优化时序：确保I2C总线上的通信时序准确，遵循I2C标准协议，以确保从属设备能够正确响应主控设备的请求。
（2）使用超时机制：为每个设备设置一个超时计数器，当设备在规定时间内无法完成通信时，自动退出并重新尝试。
（3）硬件复位：如果软件复位无法解决问题，可以尝试对整个系统进行硬件复位，以恢复正常工作状态。

36、当CAN通信过程中出现CANH信号线短暂的短路，之后又恢复正常的情况，需要采取一系列措施来确保系统的稳定性和安全性。以下是一些建议的处理步骤：
（1）故障记录与分析：
记录短路发生的时间、持续时长以及恢复正常的过程。
分析是否伴随有其他故障现象，如通信异常、数据传输错误等。
（2）节点测试法：
如果短路现象频繁发生，可以采用节点测试法来定位故障点。
逐个断开节点，观察CAN总线电压变化，判断是否有节点设备导致短路。
（3）软件优化：
在软件层面，可以优化通信协议，增加对短路等异常情况的检测和处理机制。
例如，设置重试机制、错误恢复策略等，以提高通信的可靠性。
对于第（3）点中提到的软件优化，以下是一些具体的例子来说明如何在软件层面优化CAN通信协议以应对短路等异常情况：
1. 错误检测和重试机制：
在CAN通信协议中，可以实施一种错误检测和重试机制。当主节点发送一个消息后，如果没有在规定的时间内收到从节点的应答（ACK），或者检测到传输错误（例如由于CANH信号线短路导致的错误帧），主节点可以启动重试机制。这意味着主节点会重新发送消息，直到成功接收到ACK或者达到最大重试次数。这种机制可以大大提高通信的可靠性。
2. 错误恢复策略：
当检测到CANH信号线短路或其他通信错误时，软件可以实施一种错误恢复策略。例如，当检测到短路时，软件可以暂停与该节点的通信，并尝试重新建立连接。如果连续多次尝试均失败，软件可以标记该节点为故障状态，并通知系统管理员或采取其他补救措施。
3. 心跳检测：
软件可以定期向所有节点发送心跳消息，并要求节点在规定时间内回复。如果某个节点长时间未响应心跳消息，软件可以认为该节点出现故障或离线，并采取相应的处理措施。这种机制可以及时发现由于CANH信号线短路或其他原因导致的节点故障。

38、在Linux应用程序进行串口通信时，选择设置为阻塞模式还是非阻塞模式主要取决于应用程序的具体需求以及通信过程中的实时性、效率性和可靠性要求。
阻塞模式：
当需要确保数据的完整性和顺序性时，可以考虑使用阻塞模式。在这种模式下，如果串口接收缓冲区没有数据可读，read函数会阻塞在那里，直到有数据可读。同样，如果串口发送缓冲区满，write函数也会阻塞，直到缓冲区有足够的空间可以写入。这种模式的优点是可以确保数据按照预期的顺序被读取或写入，但缺点是可能导致程序在等待数据的过程中无法执行其他任务，从而降低了程序的并发性和响应速度。
非阻塞模式：
当需要提高程序的响应速度、避免程序在等待串口操作时阻塞，或者需要同时处理多个串口通信任务时，可以考虑使用非阻塞模式。在非阻塞模式下，无论串口接收缓冲区是否有数据可读，read函数都会立即返回，同样，write函数也会在无法立即写入数据时立即返回。这种模式的优点是可以提高程序的并发性和响应速度，使程序在等待串口操作时能够继续执行其他任务。然而，由于数据可能不是按照预期的顺序到达或发送，因此可能需要更复杂的逻辑来处理数据的顺序和完整性。
在Linux下的应用编程中，堵塞（Blocking）和非堵塞（Non-blocking）是两种处理I/O操作的重要模式。它们的主要区别在于当进行I/O操作时，程序的行为如何。
堵塞I/O
在堵塞I/O模式下，当一个进程发起一个I/O操作（如读文件、网络接收等）时，如果数据没有立即可用，那么这个进程会被操作系统挂起，直到数据可用为止。换句话说，进程会停止执行，等待I/O操作完成。
这种模式的优点是简单，程序员不需要处理复杂的异步事件。但是，它的缺点也很明显：如果I/O操作需要很长时间才能完成（比如网络延迟），那么进程就会长时间处于挂起状态，浪费CPU资源。
非堵塞I/O
与堵塞I/O不同，非堵塞I/O允许进程在发起I/O操作后立即继续执行其他任务，而不会被挂起。
如何设置非堵塞I/O
在Linux中，可以使用fcntl函数或ioctl函数来设置文件描述符为非堵塞模式。对于套接字编程，可以使用socket函数的SOCK_NONBLOCK标志来创建非堵塞套接字。

37、TCP做客户端通信流程：
（1）// 创建客户端套接字
LCDSocketClientTCP = socket(AF_INET, SOCK_STREAM, 0);
（2）// 设置地址结构
	tSocketServerAddr.sin_family = AF_INET;
	tSocketServerAddr.sin_port = htons(LCD_TCU_TCP_PORT);  //host to net short,将主机的字节序转化为16位（端口是16位的）的网络字节序
	tSocketServerAddr.sin_addr.s_addr = inet_addr(LCD_SERVER_IP);
	memset(tSocketServerAddr.sin_zero, 0, 8);
（3）//建立连接
connect(LCDSocketClientTCP, (const struct sockaddr *)& tSocketServerAddr, sizeof(struct sockaddr));
（4）// 阻塞接收数据
iRecvLen = recv(LCDSocketClientTCP,recv_buf,sizeof(recv_buf),0);

38、TCP做服务端通信流程：
（1）// 创建服务器套接字
	serverSocket = socket(AF_INET, SOCK_STREAM, 0);
（2）// 设置端口复用：即使端口处于TIME_WAIT状态，也能重新绑定
	setsockopt(serverSocket, SOL_SOCKET, SO_REUSEADDR, &reuse, sizeof(reuse)) 
服务端重启时，若旧连接还在 TIME_WAIT 状态，端口会被占用，加这个选项能快速重启，避免 “Address already in use” 错误。
（3）// 设置服务器地址结构
	serverAddr.sin_family = AF_INET;
	serverAddr.sin_addr.s_addr = INADDR_ANY;
	serverAddr.sin_port = htons(SMTEST_SERVER_TCP_PORT);
（4）// 将套接字绑定到指定地址和端口
bind(serverSocket, (struct sockaddr*)&serverAddr, sizeof(serverAddr)
（5）// 开始监听连接
	listen(serverSocket, 5)
（6）初始化select监听集合 
fd_set read_fds; 
FD_ZERO(&read_fds); // 清空集合 
FD_SET(listen_fd, &read_fds); // 加入监听套接字 
int max_fd = listen_fd; // 记录最大fd
（7）// 阻塞监听读事件（新连接/客户端发数据）
fd_set tmp_fds = read_fds; // 每次select要传临时集合（内核会修改）
select(SmtestSocketServerTCP + 1,&tmp_fds,NULL,NULL,NULL)
select 是IO 多路复用函数，解决传统 accept() 阻塞导致 “只能处理一个客户端” 的问题 —— 用 select 可以同时监听多个套接字（监听套接字 + 已连接套接字），哪个有数据就处理哪个。
if(FD_ISSET(listen_fd, &tmp_fds)) { // 有新客户端连接
（8）// 接收数据
fd_client_smtest_tcu = accept(SmtestSocketServerTCP, (struct sockaddr *)&tSocketClientAddr, &iAddrLen))
select 的缺点？① 最大监听 fd 有限制（默认 1024）；② 每次要遍历所有 fd，效率低；③ 每次要重新传入 fd 集合，开销大；
1. 客户端可以 bind 吗？
可以，但一般不 bind。bind 是指定自己的端口，客户端由系统自动分配临时端口即可。
2. connect 失败常见原因？
服务器 IP / 端口错
服务器没启动
防火墙
服务端 backlog 满了
3. accept 返回的 fd 是什么？
是已连接套接字，专门用来和这个客户端收发数据。原来的 socket 叫监听套接字，只负责等连接。
socket()
 → setsockopt() // 端口复用 
→ bind() 
→ listen() 
→ 初始化select集合（加入listen_fd） 
→ 循环： select() // 监听所有fd 
→ 若listen_fd有事件：accept() → 加入select集合 
→ 若conn_fd有事件：recv()/send() → 断开则移除集合 
→ close()
select 能监听的最大 fd 数量由系统内核参数 FD_SETSIZE 决定（默认 1024），要扩大最大可连接数，需修改该参数 + 代码适配，且最大不超过系统对进程的 fd 总数限制。

38、内存条的SLT测试是指系统级测试（System Level Test）。这种测试在生产环境中执行，需要构建一个系统来进行。

39、TCP通信中客户端执行完connect函数后，这个时候就已经建立起TCP连接了吗？
在TCP通信中，当客户端执行connect函数时，它会发起一个TCP连接请求到服务器。但是，仅仅执行了connect函数并不意味着TCP连接已经完全建立。以下是TCP连接建立的简要过程：
（1）客户端发起连接请求：客户端通过connect函数向服务器发送一个SYN（同步）包，请求建立连接。
（2）服务器响应连接请求：服务器收到SYN包后，会回复一个SYN-ACK（同步-应答）包给客户端，表示已准备好建立连接。
（3）客户端确认连接建立：客户端收到SYN-ACK包后，会再发送一个ACK（应答）包给服务器，表示连接已成功建立。
只有当这三个步骤都成功完成后，TCP连接才算真正建立。因此，仅仅执行connect函数只是开始了这个过程，并不意味着连接已经建立。
另外，值得注意的是，connect函数在TCP连接成功建立或发生错误时才会返回。如果连接成功建立，connect函数会返回0。如果连接失败（例如，由于网络问题或服务器无响应），connect函数会返回一个错误码，并可以通过相关的系统调用（如errno）来获取更多关于错误的信息。

38、// 状态机状态转换表
typedef struct FsmTable_s {
	int event;				// 事件
	int CurState;			// 当前状态
	void (*eventActFun)();	// 事件执行函数指针
	int NextState;			// 下一个状态
}FsmTable_t;

40、TCP通信的三次握手机制：
TCP通信的三次握手机制是为了在客户端和服务器之间建立一个可靠的连接。这个机制主要包括以下三个步骤：
第一次握手：客户端向服务器发送SYN报文（SYN=j），并进入SYN_SEND状态，等待服务器的确认。这个SYN报文中的序列号（seq）是客户端随机生成的，用于后续的数据传输中确认数据包。
第二次握手：服务器收到客户端的SYN报文后，会向客户端回复一个确认报文（ACK），这个ACK报文的确认号（ack）是客户端的初始序列号加1（即ack=j+1），表示服务器已经收到了客户端的SYN报文。同时，服务器也会发送一个自己的SYN报文（SYN=k），即SYN+ACK报文，并进入SYN_RECV状态。
第三次握手：客户端收到服务器的SYN+ACK报文后，会向服务器发送一个确认报文（ACK），这个ACK报文的确认号是服务器的初始序列号加1（即ack=k+1）。当这个ACK报文发送完毕后，客户端和服务器都进入ESTABLISHED状态，表示三次握手完成，TCP连接已经建立。
在整个三次握手过程中，传送的包里不包含实际的数据，它们主要用于确认双方的初始序列号，以保证后续数据传输的正确性。完成三次握手后，客户端与服务器开始传送数据。理想状态下，TCP连接一旦建立，在通信双方中的任何一方主动关闭连接之前，TCP连接都将被一直保持下去。
这个机制能够确保数据的可靠传输，因为在每一步中，发送方都会等待接收方的确认，只有在确认收到后，才会进行下一步的操作。这种机制有效地避免了数据包丢失或乱序等问题，提高了数据传输的可靠性。

35、RTOS（实时操作系统）进行任务切换的过程可以简要描述为以下几个关键步骤：
保存当前任务的上下文：当任务切换发生时，RTOS需要暂停当前任务的执行，并将其CPU寄存器的状态（即当前任务的上下文）保存到任务堆栈中。这个过程称为保存现场。
恢复下一个任务的上下文：RTOS将下一个任务（即将要执行的任务）的各个寄存器值（这些值被保存在该任务的任务堆栈中）恢复到CPU寄存器中。这个过程称为恢复现场。
实际上，任务切换的本质就是CPU寄存器的切换。这个切换过程通常在一个特定的中断服务函数（如PendSV中断服务函数）中完成。
为了实现有效的任务切换，RTOS内核通常使用任务控制块（TCB）来管理每个任务的状态和上下文信息。TCB通常采用链表形式管理，以便快速查找和切换任务。

36、PendSV中断服务函数的作用
PendSV（Pendable System Vector），中文名可以翻译为“可悬起的系统调用”或“可挂起的系统服务”，是ARM Cortex系列处理器中的一个特殊中断。在实时操作系统（如FreeRTOS）中，PendSV中断服务函数常被用作任务切换的上下文切换点。当需要进行任务切换时，操作系统会触发PendSV中断，并在其中断服务函数中完成寄存器的保存和恢复操作。这样，任务切换的过程就被封装在了一个中断处理程序中，使得切换过程更加高效和可靠。
总结
因此，这句话的意思是，任务切换的本质是通过切换CPU寄存器的状态来实现的，而这个过程通常是在PendSV这样的特定中断服务函数中完成的。
以 FreeRTOS 为例，可按这个逻辑答：
（1）触发 PendSV 异常（任务切换请求）；
（2）硬件自动压栈：R0-R3、R12、LR、PC、xPSR → 当前任务堆栈；
（3）RTOS 进入 PendSV 服务函数，手动压栈：R4-R11 → 当前任务堆栈；
（4）保存当前任务的 SP（PSP）到 TCB；
（5）从下一个任务的 TCB 中读取 SP（PSP），恢复为当前 CPU 的 PSP；
（6）RTOS 手动出栈：R4-R11 → CPU 寄存器；
（7）异常返回，硬件自动出栈：R0-R3、R12、LR、PC、xPSR → CPU 寄存器；
（8）下一个任务开始执行。

完整的中断优先级层次
优先级高                                    不可被打断
───────────────────────────────────────
  Reset                                    （最高，固定）
  NMI                                     （固定）
  HardFault                                 （固定）
───────────────────────────────────────  高优先级外设中断（如 DMA、高速 UART）     可抢占 PendSV ✅
中优先级外设中断（如 SPI、I2C）           	可抢占 PendSV ✅
低优先级外设中断（如 GPIO、普通定时器）  	可抢占 PendSV ✅
───────────────────────────────────────  SysTick          （最低优先级）             Tick 心跳
PendSV          （最低优先级）             任务切换
───────────────────────────────────────
优先级低

PendSV 被打断后会怎样
任务A 运行中
│
▼
SysTick 触发 → 判断需要切换到任务B → 挂起 PendSV
│
▼
PendSV 开始执行（保存任务A的寄存器...）
│
├──→ UART 中断来了！打断 PendSV
│ │
│ ▼
│ 处理 UART 数据
│ │
│ ▼
│ UART 中断返回
│
▼
PendSV 继续（...恢复任务B的寄存器）
│
▼
任务B 开始运行

CPU是如何判断需要切换到任务B
答：不是 CPU 判断的，是 FreeRTOS 调度器判断的。 CPU 只是执行指令的机器，调度逻辑是软件实现的。
触发判断的时机
有两种情况会触发"要不要切换任务"的判断：
① SysTick 定时触发（时间片轮转）
每 1ms SysTick 中断一次，FreeRTOS 在里面检查：
// FreeRTOS 源码简化版
void SysTick_Handler(void)
{
xTickCount++; // 系统节拍 +1

// 检查1：有没有任务延时到期了？
// 比如某任务调了 vTaskDelay(100)，100ms 到了
if (有任务从延时列表移到就绪列表) {
需要切换 = true;
}

// 检查2：当前任务的时间片用完了吗？（同优先级轮转）
if (就绪列表中同优先级有其他任务) {
需要切换 = true;
}

if (需要切换) {
// 挂起 PendSV，延迟切换
触发PendSV();
}
}
② 事件触发（立即抢占）
任务主动调用 API 时也会触发判断：
// 释放信号量 → 可能唤醒一个更高优先级的任务
xSemaphoreGive(sem);
→ 内部：把等待这个信号量的任务移到就绪列表
→ 如果它优先级比当前任务高 → 触发 PendSV

// 发送队列消息
xQueueSend(queue, &data, 0);
→ 内部：唤醒等待这个队列的任务
→ 优先级更高？ → 触发 PendSV

// 主动延时
vTaskDelay(100);
→ 当前任务移出就绪列表，放进延时列表
→ 必然需要切换到别的任务 → 触发 PendSV

// 主动让出
taskYIELD();
→ 直接触发 PendSV

调度器怎么决定切哪个任务
FreeRTOS 内部维护了一个就绪列表数组，按优先级分组：
就绪列表（Ready List）
优先级 5: [ 任务E ] ← 最高，优先选这个
优先级 4: [ ] ← 空
优先级 3: [ 任务C ] → [ 任务D ] ← 同优先级轮转
优先级 2: [ ]
优先级 1: [ 任务A ] → [ 任务B ]
优先级 0: [ Idle 任务 ] ← 没事干就跑这个
调度算法（一句话）：从最高优先级往下找，找到第一个非空的列表，取队列头部的任务运行。
总结
一句话：调度器就做一件事 —— 永远让就绪列表中优先级最高的任务运行。

PendSV 的上下文切换是可重入安全的，被打断后恢复执行不会出问题。
"重入" = 一个函数还没执行完，又被再次调用
可重入（安全）：
func() 执行到一半 → 被中断打断 → 中断里又调了 func() → 正常 ✅
不可重入（危险）：
func() 执行到一半 → 被中断打断 → 中断里又调了 func() → 出错

不可重入的典型场景
① 使用静态/全局变量
// ❌ 不可重入 
static int result; 
int square(int x) 
{ 
result = x * x; // 第1步：写入结果 
return result; // 第2步：返回结果 
}

任务A 调 square(3)
→ result = 9 // 第1步
→ 被中断打断！
中断里调 square(5)
→ result = 25 // 覆盖了！
→ return 25 // 中断返回正确
→ return result // 任务A 拿到 25，不是 9 ❌

// ✅ 可重入版本：用局部变量 
int square(int x) 
{ 
int result = x * x; // 局部变量，在栈上，各自独立 
return result; 
}
② 读-改-写操作（非原子）
// ❌ 不可重入 
int g_counter = 0; 
void increment(void) 
{ 
int temp = g_counter; // 读 
temp = temp + 1; // 改 
g_counter = temp; // 写 
}
任务A 调 increment() （g_counter 初始 = 0）
→ temp = 0 // 读到 0
→ temp = 1 // 算出 1
→ 被中断打断！
中断调 increment()
→ temp = 0 // 也读到 0（还没写回呢）
→ temp = 1
→ g_counter = 1 // 写回 1
→ g_counter = 1 // 任务A 也写回 1
结果：调了两次 increment，g_counter 只变成 1，不是 2 ❌

③ 使用 malloc / printf 等库函数
// ❌ malloc 不可重入 
void task_func(void) 
{ 
char *buf = malloc(100); 
// malloc 内部维护一个全局的空闲链表 
// 如果操作链表到一半被打断，中断里又 malloc 
// 链表结构被破坏 → 内存损坏 
}
// ❌ printf 不可重入 
void log_data(int val) 
{ 
printf("value = %d\n", val); 
// printf 内部有静态缓冲区 
// 两个上下文同时写缓冲区 → 输出乱码或崩溃 
}

④ 操作硬件寄存器（多步操作）
// ❌ 不可重入：SPI 发送 
void spi_send(uint8_t *data, int len) 
{ 
GPIO_LOW(CS_PIN); // 拉低片选 
for (int i = 0; i < len; i++) { 
SPI->DR = data[i]; // 发数据 
while (!(SPI->SR & TX_DONE)); 
} 
GPIO_HIGH(CS_PIN); // 拉高片选 
}
任务A 调 spi_send（发给传感器）
→ CS 拉低
→ 发了 3 个字节...
→ 被中断打断！
中断调 spi_send（发给 Flash）
→ CS 拉低（已经是低了，但现在对象变了）
→ 发完数据
→ CS 拉高 // Flash 操作完成
→ 继续发剩下的数据... // 但 CS 已经被拉高了！传感器不认了 ❌
→ CS 拉高

判断函数是否可重入的规则
函数用了以下任何一项 → 不可重入：
✅ 可重入 ❌ 不可重入
─────────────────────────────────
只用局部变量 用了 static 变量
只用参数 用了全局变量
不调不可重入函数 调了 malloc/printf/strtok
不操作硬件 操作共享硬件（SPI/I2C/UART）
无副作用 修改了外部状态

解决方案：
// 方案1：临界区保护
void increment(void)
{
taskENTER_CRITICAL();
g_counter++;
taskEXIT_CRITICAL();
}
// 方案2：消除静态/全局变量，改用参数传入
int square(int x)
{
return x * x; // 纯函数，天然可重入
}
// 方案3：互斥锁（任务间，不能用在中断里）
void spi_send(uint8_t *data, int len)
{
xSemaphoreTake(spi_mutex, portMAX_DELAY);
// ... SPI 操作 ...
xSemaphoreGive(spi_mutex);
}
// 方案4：用可重入版本的库函数
strtok_r() 代替 strtok()
localtime_r() 代替 localtime()
rand_r() 代替 rand()

回到 PendSV
PendSV 可重入安全是因为它只操作栈和 CPU 寄存器，每个任务有独立的栈，不共享任何全局状态：
// PendSV 上下文切换（简化）
void PendSV_Handler(void)
{
    保存当前任务的寄存器到 → 当前任务的栈     // 每个任务栈独立
    从下一个任务的栈 → 恢复寄存器             // 不涉及全局变量
}
// 即使被中断打断，中断用的是 MSP（主栈），任务用 PSP（进程栈）
// 互不干扰 ✅
一句话：函数能不能重入，关键看它有没有依赖"共享的可变状态"。有 → 不可重入，没有 → 可重入

37、TCB任务控制块包含哪些内容
（1）任务堆栈指针：指向任务的堆栈区域，用于保存任务执行过程中的局部变量、函数参数、返回地址以及CPU寄存器状态等。
（2）任务堆栈大小：记录任务堆栈容量，确保在任务执行过程中不会溢出。
（3）任务状态：记录任务当前所处的状态，比如就绪态、运行态、挂起态、等待态（可能因等待某个事件、信号量、互斥锁等原因）。
（4）任务入口地址：当任务开始执行或恢复执行时，这个地址会被引用。
（5）任务优先级：表示任务在系统中的优先级，用于系统调度任务和进行任务切换。
（6）指向下一个任务控制块的指针：形成链表结构，便于操作系统遍历和管理所有任务。

35、最高优先级的任务进行任务切换—进入中断，切换任务堆栈的实现
答：通过任务就绪表机制可以快速查询到当前最高的优先级任务，通过OS_TASK_SW()这个宏可以实现任务上下文的切换，该宏即OSCtxSw()函数通过触发PendSV异常从而完成切换的工作。OSCtxSw()要依次完成如下7项工作：
1）将被终止任务的断点指针保存到任务堆栈中；
2）将CPU通用寄存器的内容保存至任务堆栈中；
3）将被终止任务的任务堆栈指针当前值保存至该任务控制块的OSTCBStkPtr中；
4）获取待运行任务的任务控制块；
5）使CPU通过任务控制块获取待运行任务的任务堆栈指针；
6）将待运行任务堆栈中的通用寄存器的内容恢复到CPU通用寄存器中；
7）使CPU获得待运行任务的断点指针；
 
注：由于ucosii中总是把当前正在运行任务的任务控制块的指针存放在一个指针变量OSTCBCur中，并且对于待运行任务的任务控制块指针也很好获得（OSTCBHighRdy），所以2-6步骤很容易完成，问题是1和7的处理，并没有将断点指针（在PC寄存器中）压入堆栈的指令，从而想到使用引发中断的方式，利用系统在跳转到ISR时会自动将断点指针压入堆栈的功能将断点指针存入堆栈，返回时同理。

36、下面是对 xTaskNotifyWait(0x00000000, 0xFFFFFFFF, &uiNotifiedValue, portMAX_DELAY); 的解释：
（1）0x00000000表示当任务开始等待某个事件或条件时，它不会清除任何现有的通知位。ulBitsToClearOnEntry 是一个参数，用于指定在进入等待状态前需要清除的通知位。在这个情况下，其值为 0x00000000，即不清除任何位。
（2）0xFFFFFFFF表示当任务从等待状态退出时（可能是因为收到了所需的通知，或者是因为等待超时），它会清除所有的通知位。ulBitsToClearOnExit 是一个参数，用于指定在退出等待状态时需要清除的通知位。在这个案例中，其值为 0xFFFFFFFF，意味着所有的通知位都会被清除。
这种机制通常用于同步和通信。任务可能会等待一个或多个条件（由通知位表示）被满足，而当这些条件满足时，任务会从等待状态退出并执行相应的操作。在退出等待状态后，清除所有通知位有助于避免重复处理相同的通知，或者确保下一次等待操作是基于新的条件。
简而言之，这段描述告诉我们任务在等待时不清除任何通知，但在退出等待时会清除所有通知。这种机制有助于确保任务的同步和通信能够正确地进行。

FreeRTOS 本身不依赖操作系统的堆管理（MCU 裸机也没有系统堆），所以提供了 heap_1.c ~ heap_5.c 这 5 个独立的实现文件，你在工程中只需要包含其中一个即可（不能同时包含多个），所有 FreeRTOS 的内存操作（如 pvPortMalloc()、vPortFree()）都会调用对应 heap 的实现。

一、heap_1：最简单的静态堆（仅分配，不释放）
核心特性
底层实现：基于静态数组的 “单向增长” 堆，初始化时在 RAM 中划分一块连续的静态数组作为堆空间；
核心限制：只实现 pvPortMalloc()（分配），完全不实现 vPortFree()（释放）—— 分配的内存永远无法归还；
分配逻辑：按申请顺序从数组起始地址向后分配，不做任何内存复用。
适用场景
仅需要一次性分配内存的场景（如系统初始化时创建任务 / 队列，运行中不再动态创建 / 删除）；
对稳定性要求极高、不允许内存碎片的 MCU 场景（如工业控制、医疗设备）；
资源极度受限的小 RAM MCU（如 <16KB RAM）。
优点	缺点
代码极简，执行速度极快（O (1)）	不支持释放，内存只能单向增长
无内存碎片，绝对稳定	长期运行易导致内存溢出
无线程安全问题（分配时关中断）	内存利用率极低
二、heap_2：可释放但不合并的堆（适配频繁创建 / 删除）
核心特性
底层实现：基于空闲块链表的动态堆，分配时从链表中找 “第一个足够大” 的空闲块，释放时把块归还给链表；
核心限制：释放的块不会和相邻空闲块合并（即不会消除 “内存碎片”）；
分配逻辑：首次适配（First Fit）—— 找到第一个大小≥申请值的空闲块就分配。
适用场景
需要频繁动态创建 / 删除任务 / 队列的场景（如智能家居、物联网设备）；
申请 / 释放的内存块大小固定（如每个任务栈大小相同）—— 此时几乎无碎片；
不适合 “大小随机” 的内存申请（易产生大量碎片）。
优缺点
优点	缺点
支持释放，内存可复用	不合并空闲块，易产生内存碎片
分配速度快（首次适配）	碎片累积后，大内存块可能申请失败
线程安全（关中断保护）	不支持非对齐内存分配（部分 MCU 需手动对齐）
三、heap_3：封装标准库的堆（依赖编译器）
核心特性
底层实现：不实现自己的堆管理，而是封装编译器的标准库函数（如 malloc()/free()）；
核心优化：在分配 / 释放时关 / 开中断，保证 RTOS 多任务下的线程安全（标准库 malloc 本身不线程安全）；
堆空间：使用编译器链接脚本中定义的堆（即你之前问的 ld 文件中 __heap_start__/__heap_end__ 划分的堆）。
适用场景
对内存管理要求不高、追求开发效率的场景；
MCU 编译器提供可靠的标准库堆（如 GCC、ARMCC）；
不适合资源受限的小 RAM MCU（标准库 malloc 效率低、易碎片）。
优点	缺点
代码极简（仅封装），无需关心底层	依赖编译器实现，不同编译器行为不一致
支持完整的分配 / 释放	标准库 malloc 效率低，碎片严重
线程安全（关中断保护）	堆大小受链接脚本限制，无法灵活调整
四、heap_4：可释放且合并的堆（解决碎片问题）
核心特性
底层实现：基于 heap_2 的升级，核心增加了空闲块合并（Coalescing） 功能；
核心优化：释放内存时，会检查当前块的前 / 后是否有连续的空闲块，若有则合并成一个大的空闲块，彻底解决内存碎片；
分配逻辑：首次适配（First Fit），但因合并机制，碎片远少于 heap_2。
适用场景
需要频繁分配 / 释放不同大小内存块的场景（如异步广播的内存池、动态数据缓冲区）；
对内存利用率要求高的 MCU 场景（如车载、工业控制）；
绝大多数 FreeRTOS 项目的首选方案（平衡效率、稳定性、利用率）。
优点	缺点
支持释放 + 合并，几乎无内存碎片	分配速度略慢于 heap_1/heap_2（需检查合并）
内存利用率极高	代码复杂度略高
线程安全，支持对齐分配	仍为连续堆空间，大内存块申请失败后无法复用零散块
五、heap_5：支持非连续内存的堆（适配多 RAM 区 MCU）
核心特性
底层实现：基于 heap_4 的升级，核心支持将多个不连续的 RAM 区域合并成一个堆；
核心场景：适配有多个物理 RAM 区的 MCU（如 STM32H7 有 D1/D2/D3 RAM、NXP i.MX RT 有片内 RAM + 外部 RAM）；
使用前提：初始化时需手动注册所有 RAM 区域的起始地址和大小。
适用场景
MCU 有多个非连续 RAM 区（如大内存 MCU：STM32H7、GD32H7）；
需要最大化利用所有 RAM 空间的场景；
高端嵌入式产品（如车载中控、工业网关）。
优点	缺点
支持非连续 RAM，内存利用率最大化	初始化复杂，需手动配置 RAM 区域
继承 heap_4 的合并特性，无碎片	代码最复杂，调试难度高
适配大内存 MCU 场景	仅支持 FreeRTOS V9.0.0+ 版本
5 种堆的核心对比表（快速选型）
堆类型	支持释放	碎片处理	内存连续性	适用 MCU 场景	优先级（推荐）
heap_1	❌	无碎片（仅分配）	连续	小 RAM、一次性分配	低（仅极简场景）
heap_2	✅	不合并（易碎片）	连续	固定大小分配 / 释放	中（特定场景）
heap_3	✅	依赖标准库（易碎片）	连续	开发效率优先	低（不推荐）
heap_4	✅	合并（几乎无碎片）	连续	绝大多数场景	高（首选）
heap_5	✅	合并（几乎无碎片）	非连续	多 RAM 区大 MCU	高（高端场景）
为什么 heap_4 是 “行业事实默认”？
即使部分模板默认不是 heap_4，开发者也会手动改成 heap_4，核心原因是：
适配性最广：支持动态分配 / 释放，且通过 “空闲块合并” 解决了 heap_2 的碎片问题，几乎能覆盖 90% 的 FreeRTOS 项目场景；
稳定性高：线程安全（关中断 / 挂起任务保护），无内存泄漏风险，满足工业控制、车载等对稳定性要求高的场景；
易调试：FreeRTOS 提供 xPortGetFreeHeapSize() 接口，可实时监控剩余堆空间，方便排查内存溢出问题；
兼容性好：所有 FreeRTOS 版本都支持，无需额外配置（heap_5 需手动注册多 RAM 区，配置更复杂）。

1. FreeRTOS 核心特性？（一句话满分答）
开源实时操作系统，支持抢占式 / 协作式调度，有任务 / 队列 / 信号量 / 互斥锁 / 事件组等组件，支持内存管理、中断管理，适配 MCU / 嵌入式场景，可裁剪、占用资源少。
2. 任务的状态有哪些？（必考）
就绪态（Ready）、运行态（Running）、阻塞态（Blocked，延时 / 等待事件）、挂起态（Suspended，需手动唤醒）。
3. 任务优先级怎么设置？优先级数值和抢占关系？
创建任务时通过 uxPriority 参数设置（0~configMAX_PRIORITIES-1）；数值越大，优先级越高，高优先级任务可抢占低优先级任务执行。
4. 空闲任务（Idle Task）的作用？
系统自动创建（优先级 0），是唯一允许无循环的任务；作用：① 回收已删除任务的堆栈内存；② 执行空闲钩子函数（vApplicationIdleHook）；③ 系统无就绪任务时执行。
5. 任务栈大小怎么配置？如何判断栈溢出？
创建任务时指定栈大小（单位：字，如 STM32 是 4 字节 / 字）；栈溢出判断：① 用 uxTaskGetStackHighWaterMark() 查剩余栈高水位；② 开启栈溢出检测（configCHECK_FOR_STACK_OVERFLOW=1/2），触发时调用钩子函数告警；③ 调试时查看栈填充标记（0x5A）的边界。
任务创建的两种方式（动态 / 静态）？区别？
维度	动态创建（xTaskCreate）	静态创建（xTaskCreateStatic）
内存来源	堆（pvPortMalloc）	静态数组（用户指定）
内存管理	自动分配 / 释放	手动管理，无内存碎片
适用场景	快速开发、内存充足	高可靠性场景（工业 / 车载）
返回值	pdPASS/pdFAIL	返回任务句柄（NULL 表示失败）

35、队列（Queue）的作用？核心特性？
作用：任务间 / 任务与中断间通信（传递数据）；
核心特性：① 先进先出（FIFO），可选后进先出（LIFO）；② 支持多任务读写，阻塞等待；③ 传递 “数据拷贝” 而非指针（保证数据安全）。

35、信号量 vs 互斥锁（Mutex）的区别？（高频易混）
核心区别：① 互斥锁支持优先级继承（解决优先级反转），信号量不支持；② 互斥锁只能由持有锁的任务释放，信号量可由任意任务 / 中断释放；③ 互斥锁用于 “保护共享资源”，信号量用于 “同步 / 计数（计数型信号量）”。

35、什么是优先级反转？FreeRTOS 怎么解决？
核心结论
高优先级任务被低优先级任务阻塞，违背 “高优先级先执行” 的原则，电池 BMS 中极易导致充放电控制延迟：
通俗解释 + 解决方法
举电池场景的例子：
场景：高优先级（充电控制任务）、中优先级（采集任务）、低优先级（日志打印任务）；
过程：① 低优先级任务拿到 “串口打印” 互斥锁，开始打印；② 高优先级任务就绪，抢占 CPU，但需要同一把互斥锁，被阻塞；③ 中优先级任务就绪，抢占低优先级任务的 CPU，低优先级任务无法释放锁；④ 结果：高优先级任务被中 / 低优先级任务阻塞，充电控制延迟，可能导致过充。
解决方法（BMS 中实际用的）：
用 FreeRTOS 的互斥锁（mutex） 替代普通信号量（mutex 自带优先级继承机制）；
优先级继承：低优先级任务拿到 mutex 时，临时提升到高优先级任务的优先级，执行完释放锁后恢复原优先级，避免中优先级任务抢占。
一句话总结
高优先级任务因等待低优先级任务持有的资源被阻塞，用 FreeRTOS 互斥锁的优先级继承可彻底解决，这是电池充放电控制的稳定性保障。


11. FreeRTOS 调度器的调度策略？
① 抢占式调度（默认）：高优先级任务就绪后立即抢占低优先级；② 协作式调度（configUSE_PREEMPTION=0）：任务主动调用 taskYIELD() 才切换；③ 时间片调度（configUSE_TIME_SLICING=1）：同优先级任务按时间片轮询执行（时间片 = configTICK_RATE_HZ）。
12. pvPortMalloc 和标准库 malloc 的区别？（高频）
① 线程安全：pvPortMalloc 内置互斥锁，多线程调用安全；标准 malloc 非线程安全；② 内存来源：pvPortMalloc 基于 FreeRTOS 堆（heap_1~heap_5），裸机可用；标准 malloc 依赖操作系统；③ 碎片处理：pvPortMalloc 的 heap_4/heap_5 支持内存合并，碎片更少；标准 malloc 碎片严重；④ 适配性：pvPortMalloc 可配置内部 / 外部 RAM，适配 MCU；标准 malloc 无此特性。
13. 内存碎片怎么解决？（实战必答）
① 优先用静态内存（xTaskCreateStatic、静态队列 / 信号量），根治碎片；② 动态内存用 heap_4/heap_5（支持内存块合并）；③ 用内存池（Memory Pool）替代普通堆，分配固定大小内存块；④ 减少频繁 malloc/free，一次性分配足够大的内存块内部管理。
14. 任务通知（Task Notification）的优势？为什么替代信号量 / 队列？
优势：① 无需创建额外对象（队列 / 信号量），节省内存；② 效率更高（直接操作任务控制块 TCB）；③ 支持多种模式（计数 / 位操作 / 通知值）；适用场景：单任务等待事件（替代二值信号量 / 计数信号量 / 事件组），不适合多任务通信（队列更合适）。
15.任务删除后内存会自动回收吗？
动态创建的任务：删除后堆栈内存由空闲任务回收（需开启 configSUPPORT_DYNAMIC_ALLOCATION）；静态创建的任务：需手动释放静态栈和 TCB 内存，空闲任务无法回收。

35、DMA是一种允许外设设备与内存之间/存储器与存储器之间直接进行数据传输的技术，无需CPU的干预。这种技术能够显著提高数据传输的效率，从而减轻CPU的负担，使其能够更专注于执行其他任务。

36、嵌入式系统裁剪是根据硬件资源（MCU / 内存）和业务需求，移除系统中不需要的功能模块、代码、组件，只保留核心功能，最终让系统适配硬件、减小体积、提升运行效率的过程。

35、uCOSII/freeRTOS操作系统从存储结构上看，任务是如何组成的?
答：从存储结构上看，任务由任务控制块（任务优先级包含其中）、任务堆栈、任务代码三个部分组成。任务主要由以下部分组成：
（1）任务控制块（TCB）：这是任务的核心数据结构，其中记录着任务运行的基本信息。TCB中包含了诸如栈顶指针、任务的优先级、挂载队列节点等关键参数。这些参数在任务调度和执行过程中起着至关重要的作用。
（2）任务栈（Task Stack）：任务栈是与每个任务紧密相关的一部分内存区域，用于存储局部变量、函数调用的返回地址以及任务执行时的上下文信息。当任务切换时，当前任务的上下文会被保存在其任务栈中，以便在任务恢复执行时能够正确继续。
（3）任务函数（Task Function）：这是任务的执行主体，是一个无限循环的函数。任务函数定义了任务的具体行为，包括要执行的操作和逻辑。当FreeRTOS调度器选择某个任务执行时，它会调用该任务的任务函数。
系统是按照任务就绪表和任务的优先级来调度任务的。执行任务调度工作的是调度器，它负责查找具有最高优先级别的就绪任务并运行它，同时将该任务的TCB指针存放至OSTCBCur(即获取该任务的TCB指针)。通常系统在调用API函数和运行中断服务函数程序之后都要调用OSShed()来进行一次任务调度。

35、与非(NOT-AND)门结构的Flash简称：Nand Flash
或非(NOT-OR )门结构的Flash简称： Nor Flash
Nand Flash的存储技术有哪些？
Floating Gate Flash（FGF）：利用半导体材料即Floating Gate来存储电荷，通过往浮栅极注入电子，实现数据的写入。注入浮栅极的电子被上下两个绝缘体包围， 不会轻易的流失，从而实现数据的存储。浮栅极材料是导体，一般为多晶硅。

36、NAND Flash可以根据存储单元密度来分类，可以分成不同的类型(XLC）：
SLC(单层存储单元),全称Single-Level Cell。单层电子结构，每个cell可以存放1bit数据，SLC达到1bit/cell，写入数据的时候电压变化区间小，P/E寿命较长，理论擦写次数在10万次以上，但是由于成本最高，所以SLC颗粒多数用于企业级高端产品中。
MLC(双层存储单元),全称Multi-Level Cell。使用高低电压的而不同构建的双层电子结构，MLC达到2bit/cell，P/E寿命较长，理论擦写次数在3000-5000次左右，成本相对较高，但是对于消费级来说也可以接受，多用于家用级高端产品中。
TLC (三层存储单元),全称Trinary-Level Cell，三层式存储单元，是MLC闪存延伸，TLC达到3bit/cell，由于存储密度较高，所以容量理论上是MLC的1.5倍，成本较低，但是P/E寿命相对要低一些，理论擦写次数在1000-3000次不等，是目前市面上主流的闪存颗粒。此外还有QLC，PLC。

35、Flash block的组织架构，一个WordLine对应着一个或若干个Page，取决于SLC,MLC或者TLC。对SLC来说，一个WordLine对应一个Page；MLC则对应2个Page，这两个Page是一对：Lower Page 和Upper Page；TLC对应3个Page。一个Page有多大，那么WordLine上面就有多少个存储单元（Cell），就有多少个Bitline。一个Block当中的所有这些存储单元（Cell）都是共用一个衬底的。
nandflash中一个page是2k+64bytes，这64bytes是存放ECC的结果吗？
在 NAND Flash 存储器中，一个 page 通常由数据区和OOB（Out-of-Band，带外）区组成。数据区用于存储用户数据，而OOB区则用于存储一些元数据，如ECC（Error Correction Code，错误校正码）结果、坏块标记等。
你提到的每个 page 是 2K+64 bytes，其中2K（或2048 bytes）很可能是数据区的大小，而64 bytes 则是OOB区的大小。这64 bytes 确实通常用于存放ECC结果，但也可能包含其他信息，比如坏块标记等。

36、


在内存条中，“Data Retention”通常指的是数据保持时间或数据保留能力。具体来说，它描述了在特定的条件下，内存中的数据能够保持其准确性或有效性的时间长度。

在DDR5中，同时设置VDD和VDDQ为相同的值主要是出于以下考虑：
稳定性：VDD和VDDQ分别代表DRAM模块的核心电源电压和I/O电源电压。确保两者电压一致，可以减少由于电压差异可能引起的稳定性和可靠性问题。



35、如果任务A（优先级高）和任务B（优先级低），任务A一直处于运行态，那么就算任务B处于就绪态，任务B有机会得到运行吗？
答：不能运行。FreeRTOS 是抢占式调度，高优先级任务一旦就绪，就会一直占用 CPU。只有当高优先级任务 A 进入阻塞 / 延时 / 等待 时，低优先级任务 B 才有机会运行。

35、在UCOSII中，任务的优先级是通过数字来表示的，并且数字越小，优先级越高。这意味着，优先级的数值从0开始，数值越小，任务的优先级越高。
在FreeRTOS中，任务的优先级同样是用数字来表示的，但与UCOSII不同的是，在FreeRTOS中，数值越大，优先级越高。通常情况下，0是最低优先级，而configMAX_PRIORITIES - 1（其中configMAX_PRIORITIES是在FreeRTOS配置文件中定义的）是最高优先级。
FreeRTOS还支持动态优先级调度，这意味着任务的优先级可以在运行时进行更改。

36、在UCOSII中，空闲任务的优先级是系统预定义的，通常是所有任务中最低的。当OS_LOWEST_PRIO被设置为63时，空闲任务的优先级就是63。这意味着系统中最多可以有64个就绪任务，其中0代表最高优先级，而63代表最低优先级。空闲任务在系统中是一个特殊的任务，当没有其他任务就绪时，CPU将执行空闲任务。空闲任务通常什么都不做，只是简单地等待其他任务变得就绪。
此外，统计任务（如果存在）的优先级通常是OS_LOWEST_PRIO - 1，即比空闲任务高一个级别。
FreeRTOS 的任务优先级数量不是固定值，而是由配置宏 configMAX_PRIORITIES 决定（默认一般是 32），实际可配置范围为 0 ~ configMAX_PRIORITIES - 1，且数值越大，优先级越高。
在FreeRTOS中，空闲任务的优先级是0，它是系统中优先级最低的任务，用于在没有其他任务执行时占用CPU。当FreeRTOS的任务调度器osKernelStart()启动时，会自动创建一个空闲任务。

复杂度:
时间复杂度: 一个算法执行所耗费的时间。
空间复杂度: 运行完一个程序所需内存的大小。

精简指令集（RISC，Reduced Instruction Set Computing）和复杂指令集（CISC，Complex Instruction Set Computing）是计算机处理器设计中的两种主要架构，它们在多个方面存在显著的差异：
（1）RISC架构的指令集相对较小，只包含最基本的指令。而CISC架构则包含大量的指令，且这些指令的种类和功能更为复杂。
（2）RISC的指令设计更为规整，每条指令的执行时间更短，因此其执行速度通常较快。而CISC架构的指令执行过程可能涉及多个微操作，导致执行速度相对较慢

37、C语言代码如何运行的、关于栈的相关
   1、预处理--编译--汇编--链接
      （1）预处理(Preprocessing)：用于将所有的#include头文件以及宏定义替换成其真正的内容；
      （2）编译(Compilation)：将经过预处理之后的程序转换成特定汇编代码的过程；
      （3）汇编(Assemble)：将上一步的汇编代码转换成机器码，产生的文件叫做目标文件(.o)；
      （4）链接(Linking)：链接过程将多个目标文件以及所需的库文件(.so等)链接成最终的可执行文件(.exe)。
   2、程序定义的“局部变量”会放在计算机内存中（栈），栈在数据结构是最基本的操作，只有两个操作，一个是push入栈，另一个是pop出栈。在c的栈中，是从栈顶向下开始存入变量的。

38、指针函数与函数指针的区分
（1）指针函数：简单来说，就是一个返回指针的函数。本质是一个函数，函数返回的类型是一个指针。
普通函数的声明：
 int sort (int a,int b);
指针函数的声明：
 int *sort(int a,int b);
（2）函数指针：本质是一个指针变量，该指针指向这个函数。总结来说，函数指针就是指向函数的指针。
函数指针的声明：
 int  (*sort)(int a,int b);
函数指针的赋值有两种方法：
 sort=&a;
 sort=a;
取地址运算符&不是必需的，因为一个函数标识符就表示了它的地址，如果是函数调用，还必须包含一个圆括号括起来的参数表。

两者的不同
1、定义不同
指针函数本质上是一个函数，其返回值是指针。
函数指针本质上式一个指针，其指向一个函数。
2、写法不同
 指针函数:int *sort(int a,int b);
 函数指针：int (*sort)(int a,int b);

39、链表与数组的区别
链表是一种上一个元素的引用指向下一个元素的存储结构，链表通过指针来连接元素与元素；
链表分为：单向链表、双向链表、循环链表
链表是链式的存储结构；数组是顺序的存储结构
链表通过指针来连接元素与元素，数组则是把所有元素按序依次存储。
总结：
1）数组便于查询和修改，但是不方便新增和删除
2）链表适合新增和删除，但是不适合查询，根据业务情况使用合适的数据结构和算法是在大数据量和高并发时必须要考虑的问题

40、#define宏定义与typedef的区别
# define宏定义是字符替换，typedef是定义类型，是声明一种新的类型，等同自带的基本类型。
#define是宏，处理的时候位于预处理阶段，宏处理器基本上对你的C/C++程序不会有任何的感知。它只处理宏的语法。而编译阶段的“程序”得到的是宏处理完的结果。
typedef是编译阶段的一部分。它的意义是单一的。
宏定义只是简单的字符串代换，是在预处理完成的，而typedef是在编译时处理的，它不是作简单的代换，而是对类型说明符进行重新命令。被命名的标识符具有类型定义说明的功能。

41、预处理器标识#error的目的是什么？
当预处理器预处理到#error命令时将停止编译并输出用户自定义的错误消息

42. 嵌入式系统经常具有要求程序员去访问某特定的内存位置的特点。在某工程中，要求设置一绝对地址为0x67a9的整型变量的值为0xaa55。编译器是一个纯粹的ANSI编译器。写代码去完成这一任务。
这一问题测试你是否知道为了访问一绝对地址把一个整型数强制转换（typecast）为一指针是合法的。这一问题的实现方式随着个人风格不同而不同。典型的类似代码如下：
int *ptr; 
ptr = (int *)0x67a9; 
*ptr = 0xaa55;

一个较晦涩的方法是：
*(int * const)(0x67a9) = 0xaa55;

42. 中断是嵌入式系统中重要的组成部分，这导致了很多编译开发商提供一种扩展—让标准C支持中断。具代表事实是，产生了一个新的关键字__interrupt。下面的代码就使用了__interrupt关键字去定义了一个中断服务子程序(ISR)，请评论一下这段代码的。
__interrupt double compute_area (double radius)
{
double area = PI * radius * radius;
printf(” Area = %f”, area);
return area;
}
这个函数有太多的错误了，以至让人不知从何说起了：
1). ISR 不能返回一个值。如果你不懂这个，那么你不会被雇用的。
2). ISR 不能传递参数。如果你没有看到这一点，你被雇用的机会等同第一项。
3). 在许多的处理器/编译器中，浮点一般都是不可重入的。有些处理器/编译器需要让额外的寄存器入栈，有些处理器/编译器就是不允许在ISR中做浮点运算。此外，ISR应该是短而有效率的，在ISR中做浮点运算是不明智的。
4). 与第三点一脉相承，printf()经常有重入和性能上的问题。如果你丢掉了第三和第四点，我不会太为难你的。不用说，如果你能得到后两点，那么你的被雇用前景越来越光明了。

不可重入函数：指函数在执行过程中，如果被中断（如硬件中断）或多线程抢占，再次进入（重入）该函数时，会导致数据错乱、结果异常的函数。不可重入通常与以下情况相关：
（1）静态变量：如果一个函数内部使用了静态变量来保存状态，并且这个状态在函数的不同调用之间需要保持独立，那么这个函数就是不可重入的。因为静态变量的值在函数调用之间是保持不变的，所以如果有两个线程或进程同时调用这个函数，它们可能会互相干扰对方的静态变量。
（2）全局变量：与静态变量类似，全局变量也可能导致不可重入的问题，因为它们也是在所有函数调用之间共享的。
（3）系统资源：如果一个函数访问或修改了一些共享的系统资源（如文件、网络连接等），并且这些资源在并发访问时没有适当的同步机制，那么这个函数也可能是不可重入的。
（4）中断服务程序（ISR）：在嵌入式系统或实时系统中，中断服务程序需要特别小心处理，以确保它们是可重入的。因为如果ISR在执行时被另一个中断打断，并且没有正确地保存和恢复状态，那么可能会导致系统崩溃或不可预测的行为。
为了确保代码的可重入性，可以采取以下措施：
避免在函数内部使用静态或全局变量来保存状态。
使用线程安全的函数和库。
在访问共享资源时使用适当的同步机制，如互斥锁、信号量等。
在编写中断服务程序时，确保它们能够快速执行并避免长时间的操作，同时保存和恢复必要的状态。

43. 下面的代码输出是什么，为什么？
void foo(void) 
{ 
unsigned int a = 6; 
int b = -20; 
(a+b > 6) puts("> 6") : puts("<= 6"); 
}
这个问题测试你是否懂得C语言中的整数自动转换原则，我发现有些开发者懂得极少这些东西。不管如何，这无符号整型问题的答案是输出是“>6”。原因是当表达式中存在有符号类型和无符号类型时所有的操作数都自动转换为无符号类型。 因此-20变成了一个非常大的正整数，所以该表达式计算出的结果大于6。

44. 评价下面的代码片断：
unsigned int zero = 0; 
unsigned int compzero = 0xFFFF; 
/*1's complement of zero */
对于一个int型不是16位的处理器为说，上面的代码是不正确的。应编写如下：
unsigned int compzero = ~0;
这一问题真正能揭露出应试者是否懂得处理器字长的重要性。在我的经验里，好的嵌入式程序员非常准确地明白硬件的细节和它的局限，然而PC机程序往往把硬件作为一个无法避免的烦恼

45. 尽管不像非嵌入式计算机那么常见，嵌入式系统还是有从堆（heap）中动态分配内存的过程的。那么嵌入式系统中，动态分配内存可能发生的问题是什么？
这里，我期望应试者能提到内存碎片，碎片收集的问题，变量的持行时间等等。这个主题已经在ESP杂志中被广泛地讨论过了（主要是 P.J. Plauger, 他的解释远远超过我这里能提到的任何解释），所有回过头看一下这些杂志吧！让应试者进入一种虚假的安全感觉后，我拿出这么一个小节目：下面的代码片段的输出是什么，为什么？
Char *ptr;
if ((ptr = (char *)malloc(0)) == NULL)
puts(“Got a null pointer”);
else
puts(“Got a valid pointer”);
这是一个有趣的问题。最近在我的一个同事不经意把0值传给了函数malloc，得到了一个合法的指针之后，我才想到这个问题。这就是上面的代码，该代码的输出是“Got a valid pointer”。
1.当malloc分配内存时它除了分配我们指定SIZE的内存块，还会分配额外的内存来存储我们的内存块信息，用于维护该内存块。
2.malloc(0)会返回一个合法的指针并指向存储内存块信息的额外内存，我们当然可以在该内存上进行读写操作，但是这样做了会破坏该内存块的维护信息，因此当我们调用free(ptr)时就会出现错误。
char * ptr = (char * )malloc(0 * sizeof(char)) ;
3.ptr是局部指针变量，存储在栈中，它的值是动态分配的一块堆中的空间的首地址，所以说这个地址是合法的，但是由于malloc的大小是0，故这个地址指向的堆中的存储空间的大小是0。
4.这个指针类似于一个野指针，可以使用的，但是是有风险的，因为不知道这个指针后面的内存空间被谁使用着，要是被核心进程使用，那肯定会造成相应程序的崩溃。

44、__main属于C库的函数，功能是第一个完成全局/静态变量的初始化，第二个初始化堆栈，第三个库函数的初始化，第四个程序跳转进入用户main函数入口；

可屏蔽中断
45、HCU（型号：STM32F407）程序执行流程：非常重要
Main开始：
HAL_Init();
（1）配置Instruction cache, Data cache为了访问MCU内部的FLASH；
（2）设置中断优先级分组，通过中断优先级寄存器组IP[240]来控制，STM32F4只用到了前91个，每个可屏蔽中断占用一个元素，元素大小为8位，且每个可屏蔽中断占用的 8bit 并没有全部使用，而是只用了高 4 位。这高4位由SCB->AIRCR 寄存器的 bit10~8 来定义的，如设置的是抢占优先级占4位，响应优先级占0位，则各个外设的优先级设定范围即为0-15。对SCB->AIRCR寄存器执行写操作时，需要对VECTKEY段写入0x5FA，否则CPU忽略本次写动作；
注：例如组设置为 3，那么此时所有的 91 个中断，每个中断的中断优先寄存器的高四位中的最高 3 位是抢占优先级，低 1 位是响应优先级。每个中断，你可以设置抢占优先级为 0~7，响应优先级为 1 或 0。
（3）使用systick作为时基源，配置1ms（此值最好固定）滴答一次，选择TIM7作为systick的时基源，获取SCB->AIRCR 寄存器bit10~8的值，通过IP[]寄存器组bit[7：4]
配置TIM7的中断优先级（设置TIM7的抢占优先级和响应优先级）；
（3）通过NVIC->ISER[]使能可屏蔽中断（TIM7），使能TIM7的时钟通过设置RCC->APB1ENR寄存器；
（4）定时器7初始化，设置TIMx_CR1为边沿对齐模式，计数器递增计数，定时器计数器实际时钟为不分频，自动重载预装载使能，对TIMx->EGR寄存器的UG位软件置1，此时作用清除计数器CNT的值
（5）通过TIMx_DIER寄存器使能定时器更新中断，通过TIMx_CR1寄存器使能计数器
寄存器	中文名称	核心作用
CNT	计数器寄存器	实时记录当前计数值（从 0 开始递增 / 递减）
ARR	自动重载寄存器	计数器的 “最大值”，计数到这个值就重置
CCR	捕获 / 比较寄存器	电平切换的 “阈值”，CNT 和它比较决定输出电平
边沿对齐（递增）模式下，CNT 从 0 数到 ARR，数到 CCR 之前输出高，数到 CCR 到 ARR 之间输出低，到 ARR 后归零重新数，最终输出占空比 = CCR/ARR 的 PWM 波。

46、0.9度的42步进电机失步怎么办？
（1）Allego公司的A3983理论上支持四细分，细分并不是说每个脉冲（一个步距角一个脉冲）电机走0.9度/4=0.225度，而是可以让电机停在1/4步上，但是在新产业经过老化验证
，发现A3983的四细分电机并不能停在1/4步上，选用trinamic公司的TMC5160它理论上可以支持256细分，经过实测发现最多支持四细分，即电机可以停止1/4步上；
（2）由于失步可能是设置电机的力矩输出大于电机本身能支持的，通俗的讲就是电机跟不上，因此减小电机的力矩可以一定程序上改善失步；
（3）若选用的电机是带有编码器的，因此可通过统计单位时间内两次编码器脉冲数是否相等，这样可以判定是否失步或者过冲；

47、梯形调速算法中，为了防止一开始设置的加速度过大导致的电机抖动（个人理解：从静止到突然以大加速度启动，会有一定的抖动），可分段进行加速度设置，即运行一段时间把加速度改变，相当于加速度处于递增的一个过程，可在一定程序上减小抖动；

48、如何选择步进电机？
根据实际工况进行选择，比如想要的电机转速是500转每分钟，那么为了留20-30%的余量，也就是最大650转每分钟，此时转换成对应频率，结合矩频特性曲线，参考力矩，同时如果工况是轨道（电机不是一直处于运行中），那么电机的工作电流也可以设置为额定电流的120-150%之间，具体看电机发热以及是否影响电机寿命这两个指标为准，选择合适的电机；

49、编码器中如何判定方向？
方向是由人为设定，把A相跟外部中断（边沿触发）挂钩，B相跟外部中断（电平触发）挂钩，当A相处于上升沿时，B相处于高电平则为正方向；当A相处于上升沿，B相处于低电平则为反方向；

45、在电路板设计中，TOP层和BOTTOM层指的是电路板的两面。
TOP层（顶层）：也称为组件面（Component Side），是电路板的顶部层。这一层通常用于放置和布局电子元件，如芯片、电阻、电容等。在顶层，你可以看到元件的引脚、连线、标识符和标记。
BOTTOM层（底层）：也称为焊盘面（Solder Side），是电路板的底部层。这一层通常用于连接电子元件的引脚，并与顶层通过适当的连线进行连接。在底层，你可以看到焊盘、过孔、连线以及可能的接地和电源平面。

46. Typedef 在C语言中频繁用以声明一个已经存在的数据类型的同义字。也可以用预处理器做类似的事。例如，思考一下下面的例子：
#define dPS struct s * 
typedef struct s * tPS;
以上两种情况的意图都是要定义dPS 和 tPS 作为一个指向结构s指针。哪种方法更好呢？为什么？
这是一个非常微妙的问题，任何人答对这个问题（正当的原因）是应当被恭喜的。答案是：typedef更好。
typedef和define都是替一个对象取一个别名，以此增强程序的可读性，区别如下：
（1）原理不同
#define是C语言中定义的语法，是预处理指令，在预处理时进行简单而机械的字符串替换，不做正确性检查，只有在编译已被展开的源程序时才会发现可能的错误并报错。
typedef是关键字，在编译时处理，有类型检查功能。它在自己的作用域内给一个已经存在的类型一个别名，但不能在一个函数定义里面使用typedef。
（2）功能不同
typedef用来定义类型的别名，起到类型易于记忆的功能。另一个功能是定义机器无关的类型。如定义一个REAL的浮点类型，在目标机器上它可以获得最高的精度：typedef long double REAL， 在不支持long double的机器上，看起来是这样的，typedef double REAL，在不支持double的机器上，是这样的，typedef float REAL
#define不只是可以为类型取别名，还可以定义常量、变量、编译开关等。
（3）作用域不同
#define没有作用域的限制，只要是之前预定义过的宏，在以后的程序中都可以使用，而typedef有自己的作用域。

47. C语言里面有一些令人震惊的结构,下面的结构是合法的吗，如果是它做些什么？
Int a = 5, b = 7, c; 
c = a+++b;
这个问题将做为这个测验的一个愉快的结尾。不管你相不相信，上面的例子是完全合乎语法的。问题是编译器如何处理它？水平不高的编译作者实际上会争论这个问题，根据最处理原则，编译器应当能处理尽可能所有合法的用法。因此，上面的代码被处理成：
c = a++ + b;
根据C/C++的运算符优先级和结合性规则，首先解析 ++ 和 + 运算符。自增运算符 ++ 的优先级高于一元加号 +，所以 a++ 作为一个整体被解析。
因此, 这段代码持行后a = 6, b = 7, c = 12。

48.描述一下嵌入式基于ROM的运行方式和基于RAM的运行方式有什么区别。
ROM的特点是速度慢价格低，掉电不丢失
RAM是速度快，价格高，字节存取，掉电丢失。
芯片复位一定是从ROM开始的，然后一些操作为了获得更快的处理速度和一些特殊处理。将ROM的代码拷贝到RAM然后转到RAM执行。

RAM与ROM区别（详细）
内存RAM (Random Access Memory)
SRAM (Static RAM)
静态RAM（SRAM），它的执行速度非常快，是目前读写最快的存储设备，价格非常昂贵，所以只在要求很苛刻的地方使用，譬如CPU的一级、二级缓存。
特点是不需要程序来初始化，上电就可以使用。
DRAM (Dynamic RAM)
动态RAM（DRAM）保留数据的时间短，速度比SRAM慢，比ROM都快，DRAM比SRAM要便宜很多，计算机内存就是DRAM。特点是上电后需要软件初始化，才能使用。
DRAM分为很多种，常见的主要有FPRAM / FastPage、EDORAM、SDRAM、DDR RAM、RDRAM、SGRAM以及WRAM等。
DDR RAM（Double-Date-Rate RAM）也称作DDR SDRAM，这种改进型的RAM和SDRAM是基本一样的，不同之处在于它可以在一个时钟期间读写两次数据，这样就使得数据传输速度加倍。
51系列单片机，stm32单片机等，它对内存的需求小，同时为了降低开发难度，适合使用SRAM。它们的结合是SRAM+NORFlash,所以单片机上电从来不初始化内存就是这个原因

电脑对内存的开销要求大，适合全部使用DRAM。它一般组合是NORFlash（BIOS）+DRAM+硬盘

嵌入式系统,两种方式，一是NORFlash+NAND方式，二是NAND+DRAM+CPU内置的SRAM。后一种方式便宜，所以很多厂商使用后面的配置。

49.引用与指针有什么区别？
（1）引用必须被初始化，指针不必。
（2）引用初始化以后不能被改变，指针可以改变所指的对象。
（3）不存在指向空值的引用，但是存在指向空值的指针。
int a = 10; int& ref = a; // ref是a的引用，即ref和a指向同一个内存地址

50. 请判断以下代码是否存在问题。
char * s1=“hello”;
char * s2=“world”;
char * s3=strcat(s1,s2);
有问题，s1与s2都为常量指针，其内容不可修改，运行就会产生段错误
解释：在C语言中，字符串字面量（如 "hello" 和 "world"）是存储在只读内存区域（通常是程序的文本段或常量段）中的，因此它们是不可修改的。当您尝试使用 strcat 函数将 s2 指向的字符串追加到 s1 指向的字符串时，您实际上是在尝试修改一个只读内存区域，这会导致未定义行为，并且很可能会导致段错误（segmentation fault）。

51. 请问下面的代码有什么问题？
void test1()
{
char string[10];
char* str1 = “0123456789”;
strcpy( string, str1 );
}
字符串str1的末尾是以’ \0 ’结尾的，所以他的长度是11，而string的长度不够

52. 请解释以下三个变量声明的含义。
① int const *p;
② int * const p;
③ int const *p const;
int const*p=const int *p; 
这里，const 修饰的是 int，表示 p 是一个指向常量整数的指针。这意味着你不能通过这个指针去修改它所指向的整数值，但你可以修改 p 本身，让它指向另一个地址。
换句话说，*p 是只读的，但 p 是可以修改的。
int * const p;
在这里，const 修饰的是 p，表示 p 是一个常量指针，它指向一个整数。这意味着你不能修改 p 的值（即它指向的地址），但你可以通过这个指针去修改它所指向的整数值。
换句话说，p 是只读的，但 *p 是可以修改的。
int const *p const;
这里结合了前两个声明的特点。const 修饰了 int 和 p，表示 p 是一个指向常量整数的常量指针。这意味着你既不能通过这个指针去修改它所指向的整数值，也不能修改 p 的值（即它指向的地址）。
换句话说，*p 和 p 都是只读的。
三、Linux操作系统题目
1、 Linux 内核五大子系统：
进程管理、内存管理、文件系统、设备驱动、网络。

2、Linux 系统由「应用程序、系统调用 / 库、内核」三大部分组成。

3、用户空间与内核通信方式有哪些？
·  系统调用：open/read/write/ioctl 等
·  设备文件（/dev/xxx）：通过驱动提供的接口通信
·  mmap（内存映射）：用户与内核共享一段内存（零拷贝）
·  netlink：内核与用户异步通信（最常用）
·  procfs / sysfs：内核暴露信息给用户
·  uevent：内核主动通知用户（热插拔）

4、系统调用 vs 普通函数调用
系统调用
（1）通过软中断 / 异常指令（如 syscall/svc）进入内核，触发用户态 → 内核态切换。
（2）会发生栈切换：用户栈 → 内核栈。
（3）属于内核接口，依赖操作系统，可移植性较差。
（4）有上下文切换开销，开销较大。
（5）是应用程序进入内核的唯一合法入口。
普通函数调用
（1）使用 CALL / RET 指令，在同一状态、同一栈上执行。
（2）无特权级切换，无状态切换。
（3）在用户空间内部执行，开销很小。
（4）只是函数间跳转，不进入内核。

5、内核态与用户态的区别
1. 权限不同
用户态：权限低（非特权模式）只能执行普通指令，不能操作硬件、不能访问内核地址空间。
内核态：权限最高（特权模式）可以执行所有指令，管理硬件、内存、进程等。
2. 运行的代码不同
用户态：运行用户应用程序代码。
内核态：运行内核代码、驱动程序、中断处理程序。
3. 地址空间访问
用户态：只能访问用户空间，不能直接访问内核空间。
内核态：可以访问用户空间 + 内核空间。
4. 堆栈不同
用户态使用用户栈。
进入内核态时，会切换到该进程独有的内核栈。
5. 切换方式
用户态 → 内核态：通过 系统调用、中断、异常 进入。
内核态 → 用户态：执行完返回，恢复用户上下文。

6、 bootloader、内核 、根文件的关系
启动顺序：bootloader->linux kernel->rootfile->app
Bootloader全名为启动引导程序，是第一段代码，它主要用来初始化处理器及外设，然后调用Linux内核。Linux内核在完成系统的初始化之后需要挂载某个文件系统作为根文件系统（RootFilesystem），然后加载必要的内核模块，启动应用程序。（一个嵌入式Linux系统从软件角度看可以分为四个部分：引导加载程序（Bootloader），Linux内核，文件系统，应用程序。）

7 、Bootloader启动的两个阶段：
Stage1:汇编语言
1）基本的硬件初始化（关闭看门狗和中断，MMU（带操作系统），CACHE。配置系统工作时钟）
2）为加载stage2准备RAM空间
3）拷贝内核映像和文件系统映像到RAM中
4）设置堆栈指针sp
5）跳到stage2的入口点
Stage2:c语言
1）初始化本阶段要使用到的硬件设备（led uart等）
2）检测系统的内存映射
3）加载内核映像和文件系统映像
4）设置内核的启动参数
嵌入式系统中广泛采用的非易失性存储器通常是Flash，而Bootloader就位于该存储器的最前端，所以系统上电或复位后执行的第一段程序便是Bootloader。

source  ~/.bashrc 指令全解析
部分	核心含义
source	Linux 内置命令（等价于 .，如 . ~/.bashrc），作用是在当前 Shell 进程中执行指定脚本的内容，执行后修改立即生效；
~	代表当前用户的主目录（如 /home/ubuntu），是 Linux 的路径简写；
.bashrc	Bash Shell 的核心配置文件，存储当前用户的 Shell 个性化配置（环境变量、别名、函数、终端提示符等），以.开头表示隐藏文件；
（1）核心作用（嵌入式开发中高频用途）
嵌入式开发中修改.bashrc后，必须执行这条指令让配置生效，典型场景包括：
配置交叉编译器路径：export PATH=$PATH:/opt/arm-none-eabi-gcc/bin（让系统识别 ARM 交叉编译器）；
设置命令别名：alias make='make -j4'（加快代码编译速度）；
配置 Git / 开发工具环境变量：export GIT_EDITOR=vim；
自定义终端提示符：PS1='[\u@\h \w]\$ '（方便查看当前路径）。
（2）执行机制（关键理解）
若不执行source ~/.bashrc：修改后的.bashrc仅在新打开的 Shell 窗口中生效，当前窗口仍用旧配置；
执行source ~/.bashrc：无需重启 Shell / 终端，当前窗口立即加载最新的.bashrc配置。

“当前 shell 进程 = 当前终端窗口” 的直观解释
1. 可视化理解
你打开 1 个终端窗口 → 系统创建 1 个 bash 进程（PID 唯一，可通过echo $$查看）；
你在这个窗口执行source ~/.bashrc → 配置仅作用于这个 PID 对应的 bash 进程；
若再打开 1 个新终端窗口 → 系统创建新的 bash 进程（新 PID），这个新窗口不会继承前一个窗口的source配置（除非重启终端让.bashrc自动加载）。


8 、一个程序从开始运行到结束的完整过程（四个过程）
预处理（Pre-Processing）、编译（Compiling）、汇编（Assembling）、链接（Linking）

9、内存泄漏和内存溢出
（1）内存泄漏（Memory Leak）
核心定义：程序申请了内存但不再使用时，没有释放，导致这部分内存永远被占用，系统可用内存随时间推移持续减少，最终耗尽。
本质：内存 “占着不用也不还”，是 “存量” 问题（慢慢累积）；
关键特征：不会立即崩溃，长期运行后才会出问题（比如 MCU 运行几天死机）；
嵌入式典型场景：
异步广播中，任务上下文用完后没调用 bcr_task_pool_free() 归还内存池；
MCU 中 malloc 动态申请内存后，异常分支（如return/goto）没执行 free；
RTOS 任务创建后没删除，任务栈内存永远占用。

（2）内存溢出（Memory Overflow / Out of Memory）
核心定义：程序申请的内存总量超过了系统 / 区域的可用内存上限，导致申请失败（返回 NULL）或直接触发硬件异常。
本质：“要的内存太多，系统给不起”，是 “总量” 问题；
关键特征：通常会立即报错（如malloc返回NULL）或崩溃（如 RAM 溢出）；
嵌入式典型场景：
MCU 的 ld 文件中堆只分配 2KB，但程序单次malloc(4KB)；
栈 / 堆反向生长导致 “碰撞”，总占用超过 RAM 总大小；
异步广播的内存池大小（如 MAX_TASK_CTX=8），但同时有 10 个广播任务要分配上下文。

（3）内存越界（Memory Out of Bounds）
核心定义：程序访问了超出自己申请 / 分配的内存范围的地址（读 / 写），是 “访问权限” 问题。
本质：“踩了别人的地盘”，分两种：
读越界：读取不属于自己的内存（大概率拿到随机值）；
写越界：修改不属于自己的内存（会破坏其他数据，最危险）；
关键特征：可能偶现 BUG（读越界），也可能立即崩溃（写越界）；
嵌入式典型场景：
数组下标越界：char buf[10]; buf[10] = 0;（下标最大为 9）；
异步广播中，拷贝数据时长度计算错误：memcpy(ctx->data, pkg, 1024) 但ctx->data只有 512 字节；
（4）内存踩踏（Memory Trample / Corruption）
核心定义：是内存越界 / 非法写操作的结果，指一块内存被其他代码 “意外改写”，导致数据被破坏（也叫 “内存污染”）。
本质：“你的数据被我改了”，是结果，不是原因；
关键特征：BUG 极具隐蔽性，偶现、无规律，最难定位；
嵌入式典型场景：
栈溢出导致栈上的函数返回地址被改写（栈踩踏）；
异步广播中多个任务同时写同一块内存池（线程不安全）；
MCU 中断中改写全局变量，未加 volatile / 关中断，导致主程序读取到错误值；

MCU 中直接操作超出 RAM 范围的地址（如 RAM 到 0x20005000，却写 0x20005001）。

内存溢出问题是 C 语言或者 C++ 语言所固有的缺陷，它们既不检查数组边界，又不检查类型可靠性(type-safety)。众所周知，用 C/C++ 语言开发的程序由于目标代码非常接近机器内核，因而能够直接访问内存和寄存器，这种特性大大提升了 C/C++ 语言代码的性能。只要合理编码，C/C++ 应用程序在执行效率上必然优于其它高级语言。然而，C/C++ 语言导致内存溢出问题的可能性也要大许多。

10、死锁的原因和条件
产生死锁的原因主要是：
（1） 因为系统资源不足。
（2） 进程运行推进的顺序不合适。
（3） 资源分配不当等。
如果系统资源充足，进程的资源请求都能够得到满足，死锁出现的可能性就很低，否则就会因争夺有限的资源而陷入死锁。其次，进程运行推进顺序与速度不同，也可能产生死锁
这四个条件是死锁的必要条件，只要系统发生死锁，这些条件必然成立，而只要上述条件之一不满足，就不会发生死锁。
（1） 互斥条件：一个资源每次只能被一个进程使用。
（2） 请求与保持条件：一个进程因请求资源而阻塞时，对已获得的资源保持不放。
（3） 不剥夺条件:进程已获得的资源，在末使用完之前，不能强行剥夺。
（4） 循环等待条件:若干进程之间形成一种头尾相接的循环等待资源关系。

11、硬链接与软链接
链接操作实际上是给系统中已有的某个文件指定另外一个可用于访问它的名称。对于这个新的文件名，我们可以为之指定不同的访问权限，以控制对信息的共享和安全性的问题。如果链接指向目录，用户就可以利用该链接直接进入被链接的目录而不用打一大堆的路径名。而且，即使我们删除这个链接，也不会破坏原来的目录。
1>硬链接
硬链接只能引用同一文件系统中的文件。它引用的是文件在文件系统中的物理索引(也称为inode)。当您移动或删除原始文件时，硬链接不会被破坏，因为它所引用的是文件的物理数据而不是文件在文件结构中的位置。硬链接的文件不需要用户有访问原始文件的权限，也不会显示原始文件的位置，这样有助于文件的安全。如果您删除的文件有相应的硬链接，那么这个文件依然会保留，直到所有对它的引用都被删除。
2>软链接（符号链接）
软连接，其实就是新建立一个文件，这个文件就是专门用来指向别的文件的（那就和windows 下的快捷方式的那个文件有很接近的意味）。软连接产生的是一个新的文件，但这个文件的作用就是专门指向某个文件的，删了这个软连接文件，那就等于不需要这个连接，和原来的存在的实体原文件没有任何关系，但删除原来的文件，则相应的软连接不可用。

软链接删除原文件后就不可用，硬链接删除原文件后可用把
答：对，理解完全正确！简单总结一下：

软链接 vs 硬链接
软链接（Symbolic Link）：                硬链接（Hard Link）：

  软链接文件                               硬链接文件
     │                                      │
     │ 存的是"路径字符串"                     │ 直接指向数据
     ↓                                      ↓
  原文件名 ──→ 【数据 inode】            【数据 inode】 ← 原文件也指向这里
                                         （引用计数 = 2）
  
  删除原文件后：                          删除原文件后：

  软链接 → ❌ 路径失效（悬空指针）         硬链接 → ✅ 数据还在


FreeRTOS 原生支持 Tickless 模式
FreeRTOS 从 V7.3.0 开始就内置了 Tickless Idle 功能，这是官方提供的低功耗核心特性。

🔧 如何开启
在 FreeRTOSConfig.h 中设置一个宏即可：
#define configUSE_TICKLESS_IDLE    1

⚙️ 工作原理
正常模式（Tickless 关闭）：
Tick:  ⚡ ⚡ ⚡ ⚡ ⚡ ⚡ ⚡ ⚡ ⚡ ⚡ ⚡ ⚡    ← 每 1ms 中断一次
CPU:   😴...😵...😴...😵...😴...😵...        ← 反复被唤醒

Tickless 模式（开启）：
              下一个任务要在 500ms 后才醒
              ↓
Tick:  ⚡ ────────── 停止 ────────── ⚡     ← 关闭 Tick 中断
CPU:   💤💤💤💤💤 深度睡眠 💤💤💤💤💤       ← 持续睡眠 500ms
              醒来后补偿 Tick 计数 ↗
核心流程：
（1）调度器发现所有任务都阻塞了
（2）进入 Idle 任务
（3）计算下一个任务最早的唤醒时间（如 500ms 后）
（4）停止 SysTick 定时器
（5）配置一个低功耗定时器（或 RTC）在 500ms 后唤醒
（6）CPU 进入 Sleep / Deep Sleep
（7）定时器到期或外部中断唤醒 CPU
（8）补偿 Tick 计数（让 FreeRTOS 知道过了多久）
（9）恢复正常调度

📝 关键 API / 钩子函数
FreeRTOS 提供了两个可移植层函数（Port Layer）：
// 进入低功耗模式（FreeRTOS 内核自动调用）
void vPortSuppressTicksAndSleep(TickType_t xExpectedIdleTime);
// 用户可选：Idle Hook（在进入睡眠前做自定义操作）
void vApplicationIdleHook(void);

当 configUSE_TICKLESS_IDLE = 2 时，你需要自己实现 vPortSuppressTicksAndSleep()，完全控制低功耗行为。



13、中断和异常的区别
内中断：同步中断（异常，）是由cpu内部的电信号产生的中断，其特点为当前执行的指令结束后才转而产生中断，由于由cpu主动产生，其执行点必然是可控的。
外中断：异步中断是由cpu的外设产生的电信号引起的中断，其发生的时间点不可预期。

14、
单片机中断处理的基本流程：
（1）中断源触发：当中断源（如外部设备、定时器溢出等）产生中断请求时，它会向单片机的中断控制系统（如NVIC，NVIC是ARM Cortex-M系列内核中的中断控制器）发送一个中断信号。
（2）中断请求：单片机的中断控制系统接收到中断信号后，会根据中断优先级和中断屏蔽设置，判断是否需要响应这个中断请求。
（3）中断响应：如果中断请求被接受，单片机会暂停当前正在执行的程序（即主程序），保存当前程序计数器（PC）的值到堆栈中，以便中断处理完成后能继续执行被中断的程序。
（4）中断服务程序执行：单片机跳转到对应的中断服务程序（ISR）入口地址，开始执行中断服务程序。中断服务程序通常用于处理与中断源相关的事件或数据。
（5）中断返回：当中断服务程序执行完毕后，单片机会从堆栈中恢复之前保存的程序计数器（PC）的值，并继续执行被中断的程序。

15、在有数据cache情况下，DMA数据链路为：
外设-DMA-DDR-cache-CPU

16、硬实时系统和软实时系统
软实时系统：
Windows、Linux系统通常为软实时，当然有补丁可以将内核做成硬实时的系统，不过商用没有这么做的。
硬实时系统：
对时间要求很高，限定时间内不管做没做完必须返回。
VxWorks，uCOS，FreeRTOS，WinCE，RT-thread等实时系统；

17、不支持MMU的单片机能加载动态库吗？
动态库，又称为共享库，是一种可执行文件，它的文件后缀通常为.DLL或.SO。动态库存放在系统某个特定的位置，所有的进程都可以共享或者引用这一份代码的内容。
然而，动态库的运行和加载需要操作系统的支持，特别是内存管理和地址映射等复杂操作。在不支持MMU的单片机上，这些高级的内存管理功能可能无法提供。MMU的主要职责是管理虚拟内存和物理内存之间的映射，以及处理内存保护等任务。没有MMU，单片机可能无法有效地加载和执行动态库，因为它缺乏必要的内存管理机制来支持动态库的加载和运行。

18、单片机中的栈空间通常位于单片机的内部RAM（随机存取存储器）中。栈是一种特殊的数据结构，它按照后进先出（LIFO）的原则进行数据存储和取出。在单片机程序中，栈主要用于保存程序的执行现场，如函数调用时的返回地址、局部变量等，以便在程序跳转或中断返回时能够恢复执行状态。
单片机中的堆空间也是位于单片机的内部RAM（随机存取存储器）中。与栈空间不同，堆空间是由程序员自己开辟和释放的，通常使用如malloc、calloc等函数进行开辟，使用free函数进行释放。

29、SPI Flash的概念：
    SPI Flash是一种使用SPI通信的Flash
SPI Flash的组成：
SPI Flash 由SPI NOR Flash和SPI NAND Flash组成
SPI NAND Flash与NAND Flash的区别：
    （1）主控也可以不需要传统NAND控制器，只需要有SPI接口接口操作访问，从而降低成本。
（2）SPI NandFlash封装比传统的封装也小很多，故节省了PCB板的空间。

30、SPI（Serial Peripheral Interface），即串行外部设备接口，是一种全双工、高速、同步的串行通信总线。它利用单独的数据线和单独的时钟信号，来保证发送端和接收端的完美同步。SPI总线支持一主多从模式，一个SPI总线可以有多个从设备，每个从设备对应一个片选信号（CS/SS或NSS），由主设备控制来选择与哪个从设备进行通信。这种通信模式使得主设备可以与多个从设备独立通信，提高了系统的灵活性和效率。
在SPI通信中，主设备负责产生时钟信号（SCK），并通过时钟信号和片选信号来控制与从设备的通信。主设备和从设备之间的数据传输是同步的，通过时钟信号来协调数据的发送和接收。这种同步通信方式保证了数据传输的准确性和可靠性。
因此，SPI通信确实支持一主多从的通信模式，使得多个从设备可以与主设备进行独立且同步的通信。

31、


例如：
CPOL=0，CPHA=0：此时空闲态时，SCLK处于低电平，数据采样是在第1个边沿，也就是SCLK由低电平到高电平的跳变，所以数据采样是在上升沿，数据发送是在下降沿。
CPOL=0，CPHA=1：此时空闲态时，SCLK处于低电平，数据发送是在第1个边沿，也就是SCLK由低电平到高电平的跳变，所以数据采样是在下降沿，数据发送是在上升沿。
CPOL=1，CPHA=0：此时空闲态时，SCLK处于高电平，数据采集是在第1个边沿，也就是SCLK由高电平到低电平的跳变，所以数据采集是在下降沿，数据发送是在上升沿。
CPOL=1，CPHA=1：此时空闲态时，SCLK处于高电平，数据发送是在第1个边沿，也就是SCLK由高电平到低电平的跳变，所以数据采集是在上升沿，数据发送是在下降沿。


SSPSR 是 SPI 设备内部的移位寄存器(Shift Register). 它的主要作用是根据 SPI 时钟信号状态, 往 SSPBUF 里移入或者移出数据, 每次移动的数据大小由 Bus-Width 以 及 Channel-Width 所决定。
SPI的移位寄存器每次移动的数据量是一个字节（8位）。SPI的工作原理是通过主机和从机之间的串行移位寄存器来完成的。主机通过向它的SPI串行寄存器写入一个字节来发起一次传输。这个字节通过MOSI（主器件数据输出，从器件数据输入）信号线传送给从机，同时从机也将自己的串行移位寄存器中的内容通过MISO（主器件数据输入，从器件数据输出）信号线返回给主机。这样，两个移位寄存器中的内容就被交换了。因此，每次SPI的移位寄存器移动的数据量是一个完整的字节。
请注意，SPI的传输方式和具体的数据量可能会根据具体的硬件实现和应用需求有所差异。

32、SPI NAND产品构成：
主控芯片+SLC NAND flash，主控用的是复旦微FM或公司自主研发的DM7281主控
封装：一般常见的有WSON和BGA封装，SPI NAND 主要由WSON8和LGA8封装；
协议：走SPI协议，一般是4线模式（标准模式）：片选、时钟、数据写、数据读；还有其他两种模式：Dual/Quad SPI；
SPI NAND 支持的SPI模式：
标准SPI  :SCK、CS#、SI、SO、WP#、HOLD#
双管 SPI:SCK、CS#、SIO0、SIO1、WP#、HOLD#
四管 SPI:SCK、CS#、SIO0、SIO1、SIO2、SIO3



33、
（1）当启用内部ECC时，用户可以对整个128字节的备用区域的前64个字节进行编程，而整个备用区域的后64个字节不能进行编程，用户可以读取整个128字节的备用区域。
为什么备用区域的后64个字节不能进行编程：
通常，ECC值会存储在备用区域的一部分中。由于ECC值需要保持不被用户更改，以确保其能够正确地检测和纠正数据错误
（2）当禁用内部ECC时，用户可以读取和编程整个128字节的备用区域。
SPI NAND 的内存映射：


CA:列地址。12位地址可以寻址0到4095字节；但是，只有0到2175字节有效。每一页的字节2176到4095是“越界的”，设备中不存在，无法寻址。
RA：行地址。RA<5:0>选择块内的页面，RA<16:6>选择块。

34、步距角：对应一个脉冲信号，电机转子转过的角位移用θ表示。θ=360度/（转子齿数J*运行拍数），以常规二、四相，转子齿为50齿电机为例。四拍运行时步距角为θ=360度/（50*4）=1.8度（俗称整步），八拍运行时步距角为θ=360度/（50*8）=0.9度（俗称半步）。
细分数就是指电机运行时的真正步距角是固有步距角(整步)的几分之一。
更为准确地描述驱动器细分特性的是运行拍数,运行拍数指步进电机运行时每转一个齿距所需的脉冲数。86BYG250A电机有50个齿,如果运行拍数设置为160,那么步进电机旋转一圈总共需要50×160=8000步;对应步距角为360°÷8000=0.045°。　　
细分功能完全是由驱动器靠精确控制电机的相电流所产生的,与电机无关
编码器脉冲为高低电平组合的一个周期。
步进电机整步等价于一步，而步距角为0.9度，即一步运行0.9度，以八分之一步运行相当于0.9度/8=0.1125度，400个脉冲相当于以设置的八分之一步运行400次，即400*0.1125=45度。

区别：
1、单极性驱动电路的电机结构包含两组带有中间抽头的线圈，整个电机共有六条线与外界连接。
2、双极性驱动电路可以同时驱动四线式或六线式步进电机，虽然四线式电机只能使用双极性驱动电路，它却能大幅降低量产型应用的成本。

1、单极性驱动电路使用四颗晶体管来驱动步进电机的两组相位。
2、双极性步进电机使用八颗晶体管来驱动两组相位。
双极性步进电机驱动电路的晶体管数目是单极性驱动电路的两倍，其中四颗下端晶体管通常是由微控制器直接驱动，上端晶体管则需要成本较高的上端驱动电路。

37、单片机的中断服务函数会被新发生的中断打断吗？
单片机的中断服务函数是否会被新发生的中断打断，主要取决于单片机的中断优先级设置以及中断控制器的配置。
在大多数单片机系统中，中断优先级被用来决定当多个中断同时发生时，哪个中断应该首先得到响应。如果新发生的中断优先级高于当前正在处理的中断，那么新中断可能会打断当前的中断服务函数，转而执行优先级更高的中断服务函数。这被称为中断嵌套。
然而，如果单片机的中断控制器被配置为禁止中断嵌套，或者新发生的中断优先级与当前中断相同或更低，那么新中断就不会打断当前的中断服务函数。此时，新中断的请求将被暂时挂起，直到当前中断服务函数执行完毕后再进行处理。
此外，为了确保中断服务函数的完整性和原子性，通常在中断服务函数的开始和结束处会关闭和开启全局中断。这可以防止在中断服务函数执行过程中被其他中断打断。

38、原子操作（atomic operation）是计算机科学中的一个基础概念，它指的是由多步操作组成的一个操作，在执行过程中不会被其他线程打断。如果该操作不能原子地执行，则要么执行完所有步骤，要么一步也不执行，不可能只执行所有步骤的一个子集。一旦原子操作开始，就会从头到尾执行完毕，中间不会有任何停顿。

37、每一个节点都可以接收到来自其他节点的发送的帧。但具体要不要接收，由你决定。通常是通过配置CAN控制器的过滤器寄存器来设置。一旦设置好， CAN控制器会根据你的设置，自动去判断要不要接收，完全由硬件实现，这个判断要不要接收的过程，也叫过滤 。
错误帧,过载帧和帧间隔都是由硬件实现，我们无法用软件控制 。其中帧起始,CRC段,ACK段,帧结束由硬件实现 。
我们做软件的时候只要设置ID,RTR,IDE,数据长度和具体的数据，这些都有相关的寄存器供我们操作。CAN控制器硬件会负责把这些东西打包成帧格式 。

正常的位时间=1/波特率。比如： 1M波特率。一个位的时间就是1微秒 。
CAN 报文一旦发送出去，就会产生发送中断和接收中断（如果中断被使能）。
详细点说CAN发送中断是指CAN的三个发送邮箱中的其中一个由满到发送数据完毕后空才产生的中断，当原来发送邮箱本来就是空的则不会产生发送中断。

通常，验收滤波器关闭模式被用在初始化过程中。在这个模式下，可以对所有的寄存器和查找表 RAM 进行无条件访问。在验收滤波器关闭模式下，不接收 CAN 报文，因此报文不会存放到有效 CAN 控制器的接收器缓冲器中。 

自测试可用于测试硬件是否正常.局部自测试会产生一个发送中断一个接收中断，属于自发自收。全局自测试发送一个报文则在发送完毕之后产生一个发送中断，接收一个报文则产生一个接收中断。CAN正常操作模式下与全局自测试相类似。
CAN通信协议数据段长度为0—8个字节，即传输一次最多传输8个字节。

CPU内部结构：
CPU内部主要由运算器、控制器和寄存器组组成，运算器用来对数据进行各种算术运算和逻辑运算。控制器是CPU的指挥中心，它能对计算机指令进行分析，产生各种控制型信号。寄存器组用来临时存放参加运算的数据和计算的中间结果。
CPU内部有3个主要组成部分：指令寄存器，指令译码器，指令执行单元（包括ALU（算术逻辑单元）和通用寄存器组）。

Arm和DSP有什么区别
DSP主要是用来进行计算的，如加密解密、调制解调等，具有强大的数据处理能力和较高的运行速度。而MCU则适合不同信息源的多种数据的处理诊断和运算，侧重于控制，速度并不如DSP。
MCU区别于DSP的最大特点在于它的通用性，反应在指令集和寻址模式中。
ARM更侧重于控制，而DSP则更侧重于运算。

在ARM处理器中一条指令的执行分为取指、译码、执行三个阶段

开漏和推挽区别在于：
开漏：输出端相当于三极管的集电极，要得到高电平状态需要上拉电阻才行。 适合于做电流型的驱动，其吸收电流的能力相对强(一般20ma以内)。
推挽输出：可以输出高，低电平，连接数字器件。

#define X  123.45
"define定义的不占内存" 指 X 不占内存. 仍然要 为 123.45 分配单元，放在常量区。
所以 X 不占内存，123.45 要占 内存。

typedef 只对已有的类型进行别名定义，不产生新的类型；
#define 只是在预处理过程对代码进行简单的替换。

ROM与RAM的主要区别是（）。
断电后，RAM内保存的信息会丢失，而ROM则可长期保存、不会丢失
ROM是只读存储器，而RAM是随机存储器。


队列（queue）是指允许在一端进行插入操作，而在另一端进行删除操作的线性表。

队列是一种先进先出（First In First Out）的线性表，简称FIFO。允许插入的一端称为队尾，允许删除的一端称为队头。
一般来说如果在可以确定队列长度最大值的情况下，建议用循环队列，如果我们无法估计队列的长度的时候，一般采取链队列。


宏定义是在编译的哪个阶段被处理的？
宏定义是在编译预处理阶段被处理的

关键字volatile的作用是什么？给出三个不同的例子。
作用：告诉编译器不要去优化这个变量的值，因为这个变量可能会被意向不到的改变。精确地说就是，编译器在用到这个变量时每次都必须小心地重新读取这个变量的值，而不是使用保存在寄存器里的备份。
例子；1.并行设备的状态寄存器；
2.一个中断服务子程序中会访问到的非自动变量；
3.多线程应用中被几个线程共享的变量。

关键字extern的作用是什么？
用于跨文件引用全局变量，即在本文件中引用一个已经在其他文件中定义的全局变量。

关键字auto的作用是什么？
用来定义自动局部变量。
其实普通局部变量就是自动局部变量，只是省略了auto这个关键字。

关键字register的作用是什么？使用时需要注意什么？
作用：编译器会将register修饰的变量尽可能地放在CPU的寄存器中，以加快其存取速度，一般用于频繁使用的变量。
注意：register变量可能不存放在内存中，所以不能用&来获取该变量的地址；只有局部变量和形参可以作为register变量；寄存器数量有限，不能定义过多的register变量。


C语言编译过程中，关键字volatile和extern 分别在哪个阶段起作用？
Volatile在编译阶段，extern在链接阶段。
C语言编译过程分为预处理、编译、汇编、链接。

Const和#define的异同：
异：const有数据类型，编译器可以做静态类型检查；而宏定义没有类型，可能会导致类型出错。
同：两者都可以用来定义常数。

步距角：对应一个脉冲信号，电机转子转过的角位移用θ表示。θ=360度/（转子齿数J*运行拍数），以常规二、四相，转子齿数为50齿电机为例。四拍运行时步距角为θ=360度/（50*4）=1.8度（俗称整步），八拍运行时步距角为θ=360度/（50*8）=0.9度（俗称半步）。
细分数就是指电机运行时的真正步距角是固有步距角(整步)的几分之一。
更为准确地描述驱动器细分特性的是运行拍数,运行拍数指步进电机运行时每转一个齿距所需的脉冲数。86BYG250A电机有50个齿,如果运行拍数设置为160,那么步进电机旋转一圈总共需要50×160=8000步;对应步距角为360°÷8000=0.045°。　　
细分功能完全是由驱动器靠精确控制电机的相电流所产生的,与电机无关
编码器脉冲为高低电平组合的一个周期。
步进电机整步等价于一步，而步距角为0.9度，即一步运行0.9度，以八分之一步运行相当于0.9度/8=0.1125度，400个脉冲相当于以设置的八分之一步运行400次，即400*0.1125=45度。

所谓矩频特性曲线，是指步进电机扭矩与输入信号脉冲频率关系的曲线，其实相当于其他电机如伺服电机的转矩-转速特性曲线。随着输入频率的升高，步进电机扭矩下降很快。这应该可以解释步进电机“失步”的现象，当输入频率升高后，电机输出减少，响应就跟不上了，于是就出现了“失步”。
通过不同驱动电压下的矩频特性曲线来选择步进电机。

增量式编码器不能返回位置信息，它只能返回脉冲，告知自己是否在转，速度多少，以及正反转等信息。
编码器轴每旋转一圈，A相和B相都发出相同的脉冲个数，但是A相和B相之间存在一个90°（电气角的一周期为360°）的电气角相位差，可以根据这个相位差来判断编码器旋转的方向是正转还是反转，正转时，A相超前B相90°先进行相位输出，反转时，B相超前A相90°先进行相位输出。
测量单位时间内的A相输出的脉冲数来获得速度信息。 

编码器的分辨率是指编码器轴转一圈所输出的位置数，比较常用的是增量式光电编码器，它的分辨率又称为线数，比如2500线4倍频，那么它的分辨率就是2500*4=10000个脉冲。编码器的分辨率越高说明电机的最小刻度就越小，那么电机旋转的角位移也就越小，控制的精度也就越高。

编码器的精度：360度/最大分辨率，例：若400线的编码器，最大4倍频，则最大分辨率为1600，即360度/1600=0.225度
编码器的位置（加加或减减）跟步进电机的细分数无关，即不管细分数为多少，编码器还是随电机运动一段位移才会当前位置加加或减减。


项目里你做了什么？怎么做的？为什么要那样做？
我拿我在深圳新产业公司做的MAGLUMI X6项目来说说吧。我主要负责X6多模块大部件，其中包含的小部件有试剂盘，混合中转盘，试剂样本仓，扫码器，试剂样本仓指示灯，水冷板，TIP抽屉，洗液托盘，底物盒，TIP垃圾箱，反应杯垃圾箱等）。工作内容为这些部件的电路设计，PCB制板，主芯片驱动控制代码编写等。这里我重点讲下试剂盘吧，由于抓手机构需要放杯或者抓杯到试剂盘对应杯位，因此要求试剂盘有非常高的定位精度，我们设计方案给定的是0.125mm,试剂盘机械机构上保证0.1mm的定位精度，电子保证0.025mm的定位精度，电子的定位精度是经过理论计算的，试剂盘采用的是带传动，电机选用的是0.9的步进电机，皮带长度840mm,步进电机转8400步皮带才能转一圈，因此每一步走过的位置是0.1mm,如果选择步进电机的四细分，那么定位精度就能够达到0.025mm。一开始选用Allego的A3983电机驱动芯片，选择四细分，但是经过实测，发现定位精度只能达到0.1mm。由于机械结构的要求，也不能去提高它的减速比，然后考虑是不是电机编码器的精度不够，我选用的是400线的编码器，它的精度理论上是可以达到0.0225mm，即编码器是满足使用要求的，因此排除编码器的影响。后面选用了我们公司用的另一款步进电机驱动芯片Trinamic TMC5160，该芯片具有256细分，在256细分时对应的定位精度是0.1mm,即只需要电机停在四分之一步即可达到0.025mm的定位精度，该芯片还可提供闭环检测和偏差报警，最终经过测试，试剂盘达到了0.125mm的定位精度。后面经过对Allego的A3983进行测试，发现该芯片只能提高电机运行的稳定性，并不能提高电机的定位精度。

串口丢帧的情况怎么解决：1.帧头帧尾；2.BCC校验；3.使用循环缓冲区；                

任务间的同步依赖于任务间的通信。 在 UCOSII 中，是使用信号量、邮箱（消息邮箱）和消息队列这些被称作事件的中间环节来实现任务之间的通信的。   

为了把描述事件的数据结构统一起来， UCOSII 使用叫做事件控制块(ECB)的数据结构来描述诸如信号量、邮箱（消息邮箱）和消息队列这些事件。      

信号量是一类事件。使用信号量的最初目的，是为了给共享资源设立一个标志，该标志表示该共享资源的占用情况。这样，当一个任务在访问共享资源之前，就可以先对这个标志进行查询，从而在了解资源被占用的情况之后，再来决定自己的行为。
信号量可以分为两种：一种是二值型信号量，另外一种是 N 值信号量。
UCOSII 将二值型信号量称之为也叫互斥型信号量，将 N 值信号量称之为计数型信号量，也就是普通的信号量。                  

在多任务操作系统中，常常需要在任务与任务之间通过传递一个数据（这种数据叫做“消息”）的方式来进行通信。          

Tout= ((arr+1)*(psc+1))/Tclk；就可以了，比如Tout=(4999+1)*(7199+1)/72000000=0.5s,也就是说你的中断时间为0.5s。其中arr表示自动重装载寄存器，psc表示预分频器，计数器从0开始计数，当达到arr设定值后，触发一次中断；

#define GPJ2CON (*(volatile unsigned long *) 0xE0200280)
volatile是一个修饰符，告诉编译器此段代码不要优化；因此，(volatile unsigned long *)变量，意思是未优化指针类型的变量；例如：(volatile unsigned long *)i ，语句中 i 即为 (volatile unsigned long *)的一个指针；
(volatile unsigned long *) 0xE0200280，此处把变量换成了固定地址，意思是(volatile unsigned long *)指针指向固定的地址 0xE0200280


一个字符加一个整型等于：
该字符的ASCII码值与整型数据相加，结果为一个整型


                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 






